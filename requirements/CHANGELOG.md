## 2026-09-22 — Earn rules lazy-load (EARNRULE-0030)

- Migration `20260922120100_earn_rules_lazy_load.sql`: paginated BFF reads for advanced earn rates and uncovered stores.
- Migration `20260922180000_earn_rules_uncovered_perf.sql`: faster covered-store resolution for uncovered list.

## 2026-09-23

- **Display Settings (Shopify landing side image Fill)** — Hero "Fill" is now a full-bleed split (image full section height, width from its aspect ratio clamped 35–60%, crop only beyond the clamp); Referrals gains the same Side image fit setting via shared `FillSplit`. Config key `side_image_object_fit` on section JSON, no migration. rewarding-shopify landing bundle + loyalty-admin landing section form. See `requirements/Display_Settings.md` §Shopify landing (Side image fit).

- **Display Settings (brand scheme unification v2.3)** — `merchant_display_settings.shopify_brand_scheme` → **`brand_scheme`**; merge/validate/admin RPCs renamed (`admin_get/upsert_brand_scheme`, `fn_brand_scheme_*`); v2.3 adds `tokens.accent` and `cards.radius_px`; member **`get_display_settings_fast`** / batch derive `primary_color`, `secondary_color`, and radius strings from the scheme; dropped flat MDS colour/radius columns and legacy `admin_*_shopify_brand_scheme` wrappers (backup `merchant_display_settings_brand_backup`). Migrations `20260923140000_brand_scheme_unification_a.sql`, `20260923141000_brand_scheme_unification_b.sql`, `20260923142000_brand_scheme_unification_c.sql` (live). loyalty-admin brand modal + logo; rewarding-shopify landing `--rl-card-radius` from `cards.radius_px`. See `requirements/Display_Settings.md` §Concept / §Rules / §System.

- **Signup & Login (member entry)** — Returning members opening from the LINE Rich Menu with a leftover access-token cookie but no loaded member no longer land on a dead guest homepage: LIFF re-runs silent login, web reloads the summary once then opens the auth drawer; post-LIFF refetch no longer skipped when the payload lacks a user id. loyalty-user `92cc770`. See `requirements/Signup_Login.md` §Rules / §Member journey.

- **Display Settings (Shopify widget cache)** — Widget settings are cached only in loyalty-cache-api (Redis scope `widget`): new plain `api_get_widget_settings`; dropped `api_get_widget_settings_cached`, `fn_invalidate_widget_settings_cache` and the four legacy widget invalidator triggers; `bff_get_widget_settings` no longer caches; added `widget` purge triggers on `tier_conditions`, `earn_conditions`, `earn_factor_group`. `/v1/widget-settings` is public (per-IP limit, `no-store`) and accepts shop-domain merchant codes; storefront bundles rebuilt to call it. Migrations `20260923040000_widget_settings_plain_read.sql`, `20260923041000_widget_settings_drop_sql_cache.sql` (live). See `requirements/Display_Settings.md` §System.

## 2026-09-22

- **Reward (admin list perf)** — Reward list is server-paginated via `bff_list_rewards_paged` (+ `fn_reward_admin_list_base`, trigram index on `reward_master.name`); redeemed/used counts leave the list and become header pills on Reward settings via `bff_get_reward_redemption_stats`; all admin reward pickers (group add, content-library button target, Front Line push, campaign/referral/lifecycle) use debounced server search instead of a full-catalog fetch. Referral settings loads the ledger only on the History tab. Migration `20260922210000_reward_list_paged_and_redemption_stats.sql` (live). See `requirements/Reward.md` §Journeys / §System.
- **Event Promotion (perf)** — `bff_list_event_promos` computes `applicable_events_count` set-wise (2.6s → 0.16s for Syngenta); indexes `idx_event_promo_merchant`, `idx_event_promo_rule_promo`. Output unchanged. Migration `20260922220000_event_promos_list_perf.sql` (live).
- **Earn Rules (admin)** — Read-only BFFs `bff_get_earn_rule_rates_details`, `bff_search_earn_rule_entities`, `bff_list_uncovered_stores` plus SQL store-coverage helpers and trigram search indexes. Migration `20260922120100_earn_rules_lazy_load.sql` (live). FE: loyalty-admin `ProjectDocs/FE_docs/EarnRules.md`.

## 2026-09-20

- **Display Settings (Shopify brand scheme v2.1)** — `tokens.brand` renamed `tokens.primary` to match Shopify Horizon's colour-scheme role; theme import is now a literal field copy (`primary` ← scheme `primary`, Dawn/palette fallback to primary button fill) with no colour heuristics; import picker leads with the primary swatch; onboarding brand step shows Primary (editable) + Background / Text / Primary button (read-only) and "Text on primary". Merge accepts legacy `brand`; `primary_color` synced from `tokens.primary`. Migration `20260920071500_shopify_brand_scheme_v2_1_primary_token.sql`; storefront bundle reads `primary ?? brand`. See `requirements/Display_Settings.md` §Concept / §Rules / §Journeys / §System.
- **Display Settings (Shopify brand scheme v2)** — Scheme stored as eight chosen fields on `merchant_display_settings`; SQL merge/validate only; dropped SQL colour resolvers; landing/widget cached payloads expose `brand_scheme` and slim landing `theme`; admin brand RPCs return `{ scheme, updated_at }`. Storefront bundle owns all derived colours. Migration `20260920040307_shopify_brand_scheme_v2.sql`. See `requirements/Display_Settings.md` §Concept / §Rules / §System.

## 2026-09-19

- **Mission** — Claim-limit binding documented: `fn_mission_claim_quota` + `bff_get_mission_detail` / `bff_claim_mission` expose `claim_limit_scope` / max / time unit; member exhausted copy differs mission-wide (`total`, `store`, `user_store`) vs per-member `user`. See `requirements/Mission.md` §Rules / §Member journey.

## 2026-09-18

- **Reward (enhancement CR1–CR6)** — Shared redemption quote (`fn_reward_redemption_quote`, member/admin BFFs); canonical `reward_group_member` gallery order; `reward_master.allow_member_mark_used`; mission claim-limit scope copy on claim; admin reward list redeemed/used totals, per-reward **Redemptions** tab + async CSV (`reward_redemptions_export` via `admin-user-export-csv`). See `requirements/Reward.md` §Journeys / §System.

- **Product catalog weekly manual run** — `loyalty.persona.entitlements` status `ga` → `planned` with `Shelved_Demo_Features.md` source refs; bulk `last_verified_at` refresh for active catalog rows (`weekly_catalog_run_2026_09_18_manual`). Narrative: planned callouts on Packages and Persona Entitlements. Commercial `FEATURES_SUMMARY` EN/TH persona entitlements line tagged planned.

## 2026-09-17

- **CRM Knowledge daily sync** — `requirements/` committed to git; `scripts/daily-requirements-publish-and-reconcile.sh` (21:00 local launchd) commit/push docs then `doc-knowledge-reconcile.mjs`. Env: `~/.config/rocket/supabase-crm.env`. See `scripts/README-doc-knowledge.md`.
- **CRM Knowledge embed drain** — Disabled pg_cron `process-internal-knowledge-embeddings`; nightly job runs `drain-doc-knowledge-embeddings.mjs` → `doc_knowledge_drain_embeddings`. See `scripts/README-doc-knowledge.md`.

- **Shelved / demo features (`requirements/Shelved_Demo_Features.md`)** — New portfolio index for launch status and routing (Package, Persona entitlements, Store credit, Stored value cards); row added to `domains/_index.md`. No duplicate feature rules.

- **Package, Persona Entitlement, Store Credit, Stored Value Cards** — Aligned canonical docs with shelved/demo/partial launch status in Concept; demo gates in Rules/Journeys; `### Known gaps` launch checklists in System; cross-links to shelf index (verified tables/RPCs via Supabase MCP + registry grep; loyalty-admin `/package-builder`, Front Line store credit, empty Contract Builder; loyalty-user wallet Packages tab).

- **Action Macro (`requirements/Action_Macro.md`)** — Full spine enrichment for shared CS/AMP infrastructure: Concept (macro vs context binding vs variables; four `context_type`s), Rules (three guardrail layers, approval, narrowing-only constraints), Admin/Operator journeys (member none; loyalty-admin macro UI gap noted), System with verified `action_macro` / `action_macro_context` columns (incl. `domain`), BFFs, `fn_execute_macro` dispatch catalog vs live `CASE`, `resource_content` / `cs_messages` / `fn_execute_amp_action` ties; Related links to CS_Actions, Resource_Content, Central_Outcome_Dispatcher (grants only), AMP docs.

- **RFM Scoring (`requirements/RFM_Scoring.md`)** — Full spine enrichment: merged technical reference into System; verified `rfm_config` / `rfm_user_score` and RFM BFFs + `fn_rfm_compute_scores` via registry/MCP; admin journeys for `/rfm-settings` and reports; Known gap for `rfm-daily-compute` cron not in `REGISTRY_RENDER.md`.

- **Member Freeze (`requirements/Member_Freeze.md`)** — Full spine enrichment: Concept/Rules/Journeys/System from flat doc; verified `user_accounts.is_freeze` and `bff_admin_set_member_freeze` via MCP; aligned Customer 360 admin path and loyalty-user `ACCOUNT_RESTRICTED` gate to live apps.

- **Leaderboard (`requirements/Leaderboard.md`)** — Full spine enrichment: added header + spine sections; preserved native vs legacy routes and view contract under System; verified `campaign_leaderboard` RPCs via `REGISTRY_SUPABASE.md`.

- **Funnel (`requirements/Funnel.md`)** — Full spine enrichment: removed duplicate technical-reference H1; merged stage/audience model, reconcile cadence, metrics RPC, and admin routes into System; verified `funnel_*` tables and BFFs via registry.

- **Event Promotion (`requirements/Event_Promotion.md`)** — Full spine enrichment: flat Scope/Data/Functions doc → Concept/Rules/Journeys/System; verified `event_promo*` tables and promo BFFs/fns via Supabase MCP (incl. folder BFFs); aligned loyalty-admin **Event promotions** list/editor and order preview; cross-ref **Order_Booking.md**.

- **Custom Webhooks (`requirements/Custom_Webhooks.md`)** — Full spine enrichment: restructured HookDeck-replacement doc into Concept/Rules/Journeys (ops-only; member none)/System; verified `custom_integration_events` table + `custom_webhook_events` view and live Edge slugs via Supabase MCP; documented Jorakay triggers, Dr.PONG router/replay, audit skip for filtered events; cross-ref `Outbound_Integrations.md`.

- **Consent (`requirements/Consent.md`)** — Full spine enrichment: removed duplicate “technical reference” H1 (~680 lines); merged notice vs consent, channels×topics, data model, BFF contracts, and flows into Concept/Rules/Journeys/System; verified `consent_versions` / `user_consent_ledger` columns and consent RPC signatures via Supabase MCP; aligned admin **Consent settings** and member signup PDPA + **Consent Management** drawer to loyalty-admin / loyalty-user; documented `api_get_consent_documents_cached` for Shopify storefront chrome and Known gaps (registry lag, audit mode UI).

- **CS Feature Spec (`requirements/CS_Feature_Spec.md`)** — Umbrella-only enrichment: removed ~690-line Phase-1 technical reference / duplicate H1; expanded Concept/Rules/Journeys with module map and loyalty-admin CS sidebar routing; System holds live rename map, verified `cs_*` table inventory (Supabase MCP), BFF/`cs_fn_*` patterns, ingress/AI/human flows, Edge services from `REGISTRY_RENDER.md`; modular truth delegated to sibling `CS_*.md` files.

- **CS Channels (`requirements/CS_Channels.md`)** — Full spine enrichment: merged technical reference (identity, memory, Lazada CS IM, DooPage Shopee, LINE fan-out, Integration Hub scope model) into System; verified `cs_contacts` / identities / memory columns via Supabase MCP; admin journeys aligned to **CS Channels** and **CS Customers**; phone/Twilio detail routed to `CS_Phone_Number_Purchasing.md`.

- **CS Conversations (`requirements/CS_Conversations.md`)** — Full spine enrichment: merged technical reference into System (live columns incl. `modality`, `ticket_id`, CSAT fields via Supabase MCP); threading/session rules, lifecycle, BFF/pipeline flows; admin **CS Inbox** / **CS Case Logs** journeys.

- **CS Unified Inbox (`requirements/CS_Unified_Inbox.md`)** — Full spine enrichment: folded plan §2 product spec into spine + Known gaps vs shipped; verified `cs_tickets` / `cs_ticket_events` columns and inbox BFF usage in loyalty-admin `use-inbox.ts`; ticket BFFs from registry; roadmap items (Kanban, bulk, merge) explicitly marked unverified.

- **CS Knowledge Base (`requirements/CS_Knowledge_Base.md`)** — Merged technical reference into System (sources table, embed pipeline, BFF catalog); admin **CS Knowledge** + inbox search journeys; verified `cs_knowledge_sources` columns via Supabase MCP.

- **CS Procedures (`requirements/CS_Procedures.md`)** — Merged AOP format, flexibility, compile/graph tiers, and deployed `cs_bff_*` / `cs_fn_*` into spine; retired duplicate technical H1.

- **CS AI Pipeline (`requirements/CS_AI_Pipeline.md`)** — Deployed contract doc: merged Render/Inngest/MCP/chat+voice flows; parallel prepare-context; explicit win over vision doc.

- **CS AI System (`requirements/CS_AI_System.md`)** — Vision-only enrichment: removed ~760-line plan §4 duplicate H1; composition model + routing table to modular docs; Known gaps for retired aspirational prose.

- **CS Rules Engine (`requirements/CS_Rules_Engine.md`)** — Merged trigger/condition/action catalog into System with deployed vs planned labels; `cs_fn_evaluate_rules` + CS Workflows journey.

- **CS Actions (`requirements/CS_Actions.md`)** — Merged marketplace/loyalty bridge matrix; marked custom API + computer-use as vision; MCP/registry focus.

- **CS SLA (`requirements/CS_SLA.md`)** — Merged policy/business-hours/reporting spec into System; live `cs_fn_assign_sla` / breach helpers.

- **CS Live Assist (`requirements/CS_Live_Assist.md`)** — Shipped vs planned split; inbox content panel BFFs documented; roadmap copilot features in Known gaps.

- **CS Analytics (`requirements/CS_Analytics.md`)** — Deployed `cs_bff_get_analytics_*` + CSAT BFFs; Watchtower/auto-QA marked roadmap.

- **CS Platform Features (`requirements/CS_Platform_Features.md`)** — Live Web Push + teams/roles/brand config; §8–10 roadmap folded to Known gaps.

- **CS Channel Connectors (`requirements/CS_Channel_Connectors.md`)** — Live edge/path table + capability matrix with deploy verification caveat.

- **CS Phone Number Purchasing (`requirements/CS_Phone_Number_Purchasing.md`)** — Merged Twilio adapter architecture, table, Integration Hub, flows; outbound SMS gap noted.

- **CS Voice (`requirements/CS_Voice.md`)** — Merged voice tables, edges, graph executor, POC status into spine; removed duplicate technical H1.

- **CRM Event-Driven Architecture (`requirements/CRM_Event_Driven_Architecture.md`)** — Full spine enrichment: retired ~500-line CDC/Kafka/Confluent incident doc; replaced with Concept/Rules/Journeys/System for chokepoint → `chokepoint_event_outbox` → dual publishers (OutboxPublisher → Inngest `inngest-event-router-serve` routers vs integration cursors on same outbox); verified outbox columns and `chokepoint_*` signatures via Supabase MCP + `crm-event-processors/src/workers/outbox-publisher.ts`; documented allowlist/default topics, bounce-back to engine serves, expiry-reminder cron exclusion, and Known gaps (registry/README Kafka drift, referral router deploy, `crm.events.referral` env). Updated `architecture/event-chokepoints.md` pointer, `architecture/System_Map.md` runtime row, `domains/_index.md` keywords. Partner/Shopify bus delta cross-ref `Outbound_Integrations.md` (no full Part 3 duplicate).

- **Translation System (`requirements/Translation_System.md`)** — Full spine enrichment: removed legacy technical-reference H1 (TOC/exec summary/conclusion); merged architecture, APIs, cache, admin vs member static UI, and entity-type catalog into Concept/Rules/Journeys/System; verified tables (`translations`, `ui_translations`, `ui_translation_admin`, `merchant_languages`) and translation RPC signatures via Supabase MCP; aligned entity types to live `get_entity_type_config()` (missions, earn channels, display blocks, leaderboards, tags); admin languages on **Global Settings** (`bff_upsert_merchant_languages`); member init via loyalty-user `get_ui_translations`; Known gap for standalone translation hub UI (no Shopify reference MD scope).

- **Tag & Persona (`requirements/Tag_and_Persona.md`)** — Full spine enrichment: merged ~1.1k-line legacy technical reference (duplicate H1, fictional REST API, migration/status/future tails, stale `config`/`user_personas` schema prose) into Concept/Rules/Journeys/System/Related; verified persona/tag tables and assign BFF signatures via Supabase MCP; admin journeys aligned to loyalty-admin Tier Personas, User Tags, Customer 360; Known gaps for `bff_upsert_persona_master` vs group upsert and Open API assign coverage.

- **Syngenta Events (`requirements/Syngenta_Events.md`)** — Full spine enrichment: merged duplicate “technical reference” H1 into Concept/Rules/Journeys/System (data model, auth, functions, flows, external services, known gaps); verified `syngenta_*` / `event_*` tables and event BFF signatures via Supabase MCP + `REGISTRY_SUPABASE.md`; aligned Sales Events / choose-events tabs, registration import wizard, and Inngest chunk path to loyalty-admin; documented FE-only store allowlist, orphaned promos tab, and `bff_get_event_details` team-gate gap. No Shopify reference MD scope.

- **Store Attribute Classification (`requirements/Store_Attribute_Classification.md`)** — Full spine enrichment: merged ~1.1k-line legacy “technical reference” into Concept/Rules/Journeys/System; verified taxonomy and store tables (`is_deleted`, `is_store_channel`, `is_store_property`, set members incl. `store_id`) and core RPCs via Supabase MCP; aligned admin journeys to loyalty-admin (`store-attributes-master`, `store-attributes-sets`, `stores-master`) and member receipt pickers to loyalty-user; removed non-existent helper functions and corrected purchase `store_id` UUID drift; Known gaps for broken `get_store_attribute_sets_list` and direct-table assignment writes.

- **Spin Wheel (`requirements/Spin_Wheel.md`)** — Full spine enrichment: merged ~1.3k-line legacy doc (TOC, exec summary, duplicate API dumps, “future user wrapper” drift) into Concept/Rules/Journeys/System/Related; verified four `spin_wheel*` tables and eight RPCs via Supabase MCP + `REGISTRY_SUPABASE.md`; member journey aligned to loyalty-user `spin-wheel/[id]` + `bff_user_*`; admin to `spin-wheel-list` / `spin-wheel-settings`; presentation `display_config` contract folded under System; Known gaps (no Frontline spin, no earn-channel card).

- **Resource Content (`requirements/Resource_Content.md`)** — Full spine enrichment: Concept/Rules/Journeys/System/Related; folded legacy flat sections (tables, block types, platform matrix, functions) into spine; verified `resource_content` / `resource_content_category` columns (`trigger_patterns`, `platform_content` live) and BFF/CS/fn resolver signatures via Supabase MCP; admin **Content Library** + CS send + workflow picker aligned to loyalty-admin; deprecated `allowed_channels` documented; edge `browse-platform-catalog` in System (no Shopify reference MD scope).

- **FuturePark (`requirements/FuturePark.md`)** — Full spine enrichment: removed TOC/`SECTION:` hub and duplicate front matter; added Concept/Rules/Journeys and System indexes (data model, functions, flows, external services, known gaps); folded systems map, duplicates, e-directory, OCR prompts/eval under `## System`; verified `purchase_receipt_upload` and FuturePark custom tables + receipt RPCs via Supabase MCP; fixed pipeline-summary duplicate drift to receipt-number-primary RPC; Related points to `CHANGELOG.md` for domain history (inline doc changelog removed).

- **Receipt Upload Earning (`requirements/Receipt_Upload_Earning.md`)** — Full spine enrichment: merged legacy Overview/schema/function-reference monolith into Concept/Rules/Journeys/System/Related; verified `purchase_receipt_upload` columns (incl. `seller_id`, `images`, CRM sync fields), upload/approve BFF signatures, and `feature_config` upload_receipt keys via Supabase MCP; fixed drift (one row per receipt with multi-page `images[]`, retired “one image per row” rule); admin **Receipts Upload** + settings + Front Line upload journeys; Future Park OCR/deferred CRM routed to `FuturePark.md`; receipt LINE chokepoint retained in System.

- **Order Booking (`requirements/Order_Booking.md`)** — Full spine enrichment: merged duplicate “technical reference” H1 into Concept/Rules/Journeys/System; verified `api_create_manual_purchase_order` (incl. `p_chosen_freebies`), event order BFFs, and purchase columns via Supabase MCP; aligned Choose Events admin journey to loyalty-admin (`create-order-modal`, line completion, pool validation); corrected `Event_Promotion.md` cross-link; Known gaps for seller id not passed from UI and earnings preview status vs pending earn.

- **Central Outcome Dispatcher (`requirements/Central_Outcome_Dispatcher.md`)** — Full spine enrichment: merged duplicate “technical reference” H1 into Concept/Rules/Journeys/System; verified `outcome_distribution_log`, `outcome_type_enum`, and `fn_dispatch_outcome` signature/return (`redemption_ids`, reward-branch Shopify sync) via Supabase MCP; expanded caller table (spin wheel, referral, tier entry rewards); corrected referral and check-in source-type drift; Known gaps for orphaned `publish-currency-event` and registry lag on tier entry rewards.

- **AMP Workflows (`requirements/AMP_Workflows.md`)** — Full spine enrichment: replaced Scope/flat technical sections with Concept/Rules/Journeys/System/Related; folded audience membership, static/dynamic/imported types, reconciliation cron, batch/scheduled dispatch, condition evaluation alignment, node stats + engagement, lifecycle automations vs Workflow List; verified tables/columns (`audience_type` incl. `imported`), BFFs, pg_cron jobs, and Edge fns via Supabase MCP + `REGISTRY_*` + loyalty-admin (`audience-builder`, `workflow-list`, `lifecycle-automations`); orchestration detail cross-ref `AMP - Rule Based.md` (no Shopify reference MD scope).

- **Activity-Based Earning (`requirements/Activity_Based_Earning.md`)** — Full spine enrichment: merged ~1.3k-line legacy technical reference (API dumps, WeWeb grid tail, duplicate H1) into Concept/Rules/Journeys/System; verified `activity_master`, `activity_currency_config`, `activity_upload_ledger` columns and activity RPC signatures via Supabase MCP; admin **Activity Uploads** + `bff_edit_activity_upload` delta path aligned to loyalty-admin; member **Upload Activities** to loyalty-user; Known gaps (no loyalty-admin matrix settings UI, member cancel, conflation with activity-types/attribution).

- **Analytics (`requirements/Analytics.md`)** — Full spine enrichment: folded legacy “technical reference” H1 into `## System` (Data model, Functions, Flows, External services, Known gaps); verified live Edge `analytics-query` (ACTIVE, in-function JWT + merchant resolve), report catalog in `supabase/functions/analytics-query/reports/registry.ts`, BQ project/datasets `rocket-prod-analytics.serving_loyalty` + `raw_supabase_prod`, export BFF signatures via Supabase MCP, loyalty-admin report pages → report ids; added `E: analytics-query` to `REGISTRY_RENDER.md`.

- **Asset (`requirements/Asset.md`)** — Full spine enrichment: merged legacy “technical reference” H1 into `## System` (data model, functions, flows, external services, known gaps); deduplicated admin/member journeys (loyalty-admin `/assets`, Front Line Asset Group, loyalty-user Exclusive Parking); verified asset tables + BFF/helper signatures via Supabase MCP and `apply_tier_change_core` → `fn_asset_enforce_quota_after_tier_change`; Open API / `bff_get_user_assets` split documented.

- **Open API (`requirements/Open_API.md`)** — Full spine enrichment: folded ~1.9k-line legacy technical reference (TOC/duplicate H1 removed) into `## System` › HTTP endpoint reference; verified gateway Supabase project `mabioklchbkanhjwgibj` edges (`api-users`, `api-assets`, `api-purchases`, `api-redemptions`, `api-wallet`) and CRM `api_*` / `validate_api_key` / `redeem_reward_with_points` signatures via Supabase MCP; admin API Keys journey; Known gaps (`api_use_entitlement` no gateway route, no public purchase cancel, ticket catalog).

- **Purchase Import System (`requirements/Purchase_Import_System.md`)** — Full spine enrichment: merged ~2k-line legacy technical reference into Concept/Rules/Journeys/System; verified deployed `inngest-bulk-import-purchase-serve` (sku_code/store_code resolution, phone→user_id, 50-purchase chunks, idempotency `batch_id`) and `bulk_insert_purchases_with_items` → `chokepoint_post_purchase_event`; retired Render `crm-bulk-import` / `sku_id` / whole-file atomicity drift; Known gaps for no loyalty-admin UI and `admin-user-import-kickoff` not routing purchases.

- **Customer Import System (`requirements/Customer_Import_System.md`)** — Full spine enrichment: merged ~1.5k-line legacy technical reference into Concept/Rules/Journeys/System; verified live path loyalty-admin BFFs → staging → `admin-user-import-kickoff` → Inngest app `crm-bulk-import-system` / `inngest-bulk-import-customers-serve` → `process_user_import_chunk` (retired Render `crm-bulk-import` + CSV-on-event drift); export on `crm-user-export` / `admin-user-export-csv`; Known gaps for domain-index upload edge and shared-worker assumption.

- **Bulk Import Currency (`requirements/Bulk_Import_Currency.md`)** — Full spine enrichment: merged duplicate technical reference into Concept/Rules/Journeys/System; verified `bulk_import_currency` signature and `bulk_import_batches` columns via Supabase MCP + deployed `inngest-bulk-import-currency-serve` (validate-then-100-row-chunk flow, `skip_emit`, batch statuses); documented ops-only trigger (no loyalty-admin UI in repo) and Known gaps (Inngest payload at 10k rows).

- **Mission (`requirements/Mission.md`)** — Full spine enrichment from legacy Architecture/Overview (~530 lines): Concept/Rules/Journeys/System/Related; verified tables and mission RPCs via Supabase MCP; live progress path documented as chokepoint outbox → OutboxPublisher (Inngest) → `inngest-event-router-serve` mission routers → `mission/evaluate` → `inngest-mission-serve` (retired Debezium/Upstash/Confluent `MissionConsumer` drift); Known gaps for form/referral condition types without chokepoint routers; admin (`mission-list`, `mission-settings`, Front line claim) and member journeys aligned to loyalty-admin / loyalty-user.

- **Marketplace (`requirements/Marketplace.md`)** — Full spine enrichment: merged legacy technical reference into Concept/Rules/Journeys/System; verified live `webhook-marketplace-*`, `MARKETPLACE_INGEST_MODE=inngest`, Inngest batch/throttle (`process-shop-orders`), `get_shop_credentials` / Lazada seller_id routing, claim thresholds + `get_claimable_statuses`, and `claim_marketplace_order` rules via Supabase MCP + edge sources; admin Marketplace Settings + member Earn journeys; Kafka/batch-checker marked legacy in Known gaps. `Ecommerce_Marketplace_Integration.md` supersession banner (BigCommerce / historical only).

- **Forms (`requirements/Forms.md`)** — Full spine enrichment: merged legacy architecture/API/WeWeb sections into Concept/Rules/Journeys/System/Related; verified dual field systems (`user_field_config` → `user_accounts`/`user_address` vs USER_PROFILE `form_submissions`/`form_responses`), survey `form_category`, submission status/limit rules, and profile BFFs (`bff_get_user_profile_template`, `bff_save_user_profile`, admin config upsert) against Supabase MCP + loyalty-admin/user surfaces.

- **Check-in (`requirements/Checkin.md`)** — Exemplar accuracy pass: admin journeys aligned to `checkin-list`, `checkin-settings`, and Front line tab (history/reverse RPCs); verified tables (`checkin`, `checkin_outcomes`, `checkin_ledger`) and eight check-in RPCs via Supabase MCP; corrected member journey (loyalty-user has history labels only — no self-service check-in route; APIs used from Front line).

- **Reward (`requirements/Reward.md`)** — Exemplar accuracy pass: no legacy tail below Related; added **Campaign reward relation layer** (referral / `tier_entry` / lifecycle slots, `fn_campaign_reward_slot_*`, `fn_reward_references`, attach BFFs) aligned to live DB + reference MD Part 1; Shopify discount types (`percentage`, `fixed_amount`, `free_shipping`, `free_product`) and `shopify-upsert-reward-discount` vs enrichment path verified.

- **Purchase Transaction (`requirements/Purchase_Transaction.md`)** — Full spine enrichment: replaced ~1.2k-line legacy Overview/Technical doc with Concept/Rules/Journeys/System/Related; verified dual ledger (`purchase_ledger` + `purchase_items_ledger` vs `order_ledger_mkp` pre-claim), chokepoint-only writes + `crm.events.purchase*` earn path (retired `trigger_process_purchase_currency` / `create_purchase_via_api` drift), marketplace claim via `fn_claim_marketplace_order_service` → `api_create_purchase`, seller line `quantity_completed` rollup; referral purchase settlement cross-ref `Referral.md` / reference MD Part 1 § Purchase referral; `domains/purchase-transaction.md` index updated.

- **Authentication (`requirements/Authentication.md`)** — Full spine enrichment: folded legacy ops/troubleshooting monolith into Concept/Rules/Journeys/System/Related; verified three credential models (admin Supabase JWT + `custom_access_token_hook`, member `issueMemberSession` 30d access, `merchant_api_keys` / `validate_api_key`); corrected drift (24h member access → 30d; `auth-line-login` tombstone); Shopify Part 2 deltas (`shopify-proxy` `/auth/*`, extension-api, proxy referral paths). Reference MD in sync with Downloads. Routing row in `Shopify.md`.

- **Tier (`requirements/Tier.md`)** — Full spine enrichment: folded ~2.2k-line legacy into Concept/Rules/Journeys/System/Related; verified live `tier_program_config` program clock, `tier_pending_upgrades` + daily apply (retired Inngest-as-pending and per-condition window/maintain rows), `apply_tier_change` + `chokepoint_post_tier_change`, entry rewards (`tier_entry_rewards` / `fn_grant_tier_entry_rewards`); Shopify Part 2 deltas (landing VIP, hub tier card, widget `per_tier` earn, entitlements). Reference MD in sync with Downloads.

- **Signup / Login (`requirements/Signup_Login.md`)** — Full spine enrichment: folded ~1k-line technical reference (WeWeb/JS tail removed) into Concept/Rules/Journeys/System; verified `bff-auth-complete` `next_step`/`verify_*` + JWT-on-link, `bff_get_user_profile_template` params, OTP/`otp_requests`, profile save tables, and Shopify identity (proxy/extension vs `shopify_email`) against live edge + Supabase MCP; admin journeys (global auth, profile form, signup codes); Part 2 identity/gateway deltas under Journeys/System › Shopify; cross-refs `Authentication.md`.

- **Currency (`requirements/Currency.md`)** — Full spine enrichment: removed exec summary/TOC/legacy monolith (~3.4k lines → Concept/Rules/Journeys/System/Related); verified wallet **chokepoint_post_wallet_transaction**, expiry (**process_currency_expiry_batch**, Render **currency-expiry-daily**, reminder cron), and award path (outbox → **inngest-event-router-serve** → **inngest-currency-serve** + CDC consumer note); fixed drift (**user_wallet.points_balance**, retired QStash/pg_cron expiry); Shopify Simple/Advanced earn under Journeys › Shopify.

- **Platform Plan Feature Registry (`requirements/Platform_Plan_Feature_Registry.md`)** — Full spine enrichment: merged legacy technical reference into Concept/Rules/Journeys/System; verified live taxonomy counts, Shopify plan quotas/limits/off-rows, resolver + BFF consumers via Supabase MCP and loyalty-admin entitlements contract; Shopify matrix + embedded gating (`display.shopify_landing_page`, `EARN_SECTIONS`, `REFERRAL_SECTIONS` vs plan keys) aligned to reference MD Part 2; Known gaps and billing AND rule retained.

- **Earn Channel (`requirements/Earn_Channel.md`)** — Full spine enrichment: merged canonical layers, translation/write semantics, BFF contracts, receipt upload adjacency, member `earn_method` routing; Shopify deltas (landing/hub/widget enrichment, `campaign:referral` vs referral program gate) aligned to reference MD Parts 1–2; verified `earn_channel` / `earn_channel_registry` columns and earn BFF signatures via Supabase MCP. `Earn_Channel_Canonical.md` and `Earn_Channel_FE_Guide.md` reduced to cross-ref pointers.

- **Outbound Integrations (`requirements/Outbound_Integrations.md`)** — Full spine (webhook + Klaviyo v1): verified `chokepoint_event_outbox` dual path (OutboxPublisher→Inngest vs integration cursors on `crm-event-processors`); `event_key` map aligned to `event-keys.ts`; admin journeys, Klaviyo properties/metrics, pg_cron log purge; Shopify delta from reference MD Part 3; legacy flat sections folded into spine; registry gaps noted in Known gaps.

- **Third-Party Integrations (`requirements/Third_Party_Integrations.md`)** — Full spine enrichment (Part 3 reference MD); Judge.me / Gorgias / Klaviyo connect flows, `merchant_credentials`, admin `/integrations` routes, live integration Edge slugs verified via Supabase MCP; legacy numbered developer standard folded into spine; Klaviyo outbound detail cross-ref `Outbound_Integrations.md`.

- **Display Settings (`requirements/Display_Settings.md`)** — Full spine enrichment: merged legacy technical reference into `## System`; Shopify on-site touchpoints (hub, landing, widget, product points, banners, wishlist) mapped to Journeys with loyalty-admin routes and `shopify-extension-api` / rewarding-shopify verified against reference MD Part 2.

- **Referral (`requirements/Referral.md`)** — Full spine enrichment: merged technical reference into Concept/Rules/Journeys/System; Part 1 Shopify ND (repo copy newer than Downloads); verified `referral_program`, claim/mint, `member_code`, SMART attribution, campaign reward slots, embedded `REFERRAL_SECTIONS` against loyalty-admin, rewarding-shopify, Supabase MCP.

- **Shopify (`requirements/Shopify.md`)** — Full spine (Concept → Rules → Journeys → System → Related); `SECTION:` legacy removed; expanded feature routing table (Referral, Display_Settings, Third_Party, Outbound, Earn_Channel). Landing/hub/wishlist/Judge.me detail trimmed to hub → feature doc pointers; platform plumbing + verified Edge/RPC names retained. Cross-refs updated in `Display_Settings.md`.
- **Docs (enrichment workflow)** — `workflows/requirements-doc-enrichment/REFERENCE.md` + `DOMAIN_THREADS.md` (per-domain copy-paste threads). Shopify referrals/on-site/integrations reference MD copied to `requirements/reference/SHOPIFY_REFERRALS_ONSITE_INTEGRATIONS.md` (master: `~/Downloads/…`).

- **Docs (mechanics closeout)** — Added `requirements/architecture/System_Map.md` (repos, runtime tiers, surfaces, auth, writer read order). `Writing Principles/INDEX.md` row points at `13-requirements-writing.mdc`. Ran `doc-knowledge-reconcile.mjs` (3580 active chunks / 152 files; 247 upserted, 31 deactivated). Requirements unification Batch 0 complete. See `.cursor/plans/requirements-doc-unification-plan-20260917.md`.

- **Batch 5 (Wave 5)** — Requirements unification: spines on loyalty/ops + import satellites (`Customer_Import_System`, `Bulk_Import_Currency`, `Purchase_Import_System`, `ACTIVITY_WALLET_INTEGRATION`) and all modular `CS_*.md` (umbrella `CS_Feature_Spec` / `CS_AI_System` already done); `Referral.md` technical reference rewritten to live `referral_program` / claim model (retired invite-code draft removed). `_index.md` CS umbrella row.

- **Batch 4 (Wave 4)** — Spine prepend on `Open_API.md`, `Analytics.md`, `Package.md`, `Persona_Entitlement.md`, `Store_Credit.md`, `Order_Booking.md`, `Central_Outcome_Dispatcher.md`, `Platform_Plan_Feature_Registry.md` (technical reference retains OpenAPI bodies / schema detail). Requirements unification plan Wave 4.

- **Batch 3 (Wave 3)** — Spine prepend on `Display_Settings.md`, `CS_Feature_Spec.md`, `CS_AI_System.md`, `Asset.md`, `RFM_Scoring.md`, `Syngenta_Events.md`, `Marketplace.md` (legacy bodies under technical reference; Shopify deltas cross-ref only). Requirements unification plan Wave 3.

- **Batch 2** — `Shopify.md` feature routing table; MEMBER-ALIGN / earn / landing VIP pointers trimmed to domain deltas. `Signup_Login.md`, `Currency.md`, `Tier.md` spines + Journeys/System › Shopify (storefront identity, earn UI, tier landing/widget/hub + entitlement keys).

- **Reward (`requirements/Reward.md`)** — Replaced 2.2k-line monolith + merged `feature-docs/rewards.md`, `reward-groups.md`, `reward-sourcing-services.md` onto spine (groups and sourcing as `###` subsections; Shopify delta under Journeys/System). Guides archived under `requirements/archive/feature-docs/`. Batch 1.2 of requirements unification plan.

- **Check-in (`requirements/Checkin.md`)** — Merged `feature-docs/checkin.md` onto Concept → Rules → Journeys → System spine; guide archived at `requirements/archive/feature-docs/checkin.md`. Batch 1.1 of requirements unification plan.

- **Docs** — requirements writing standard adopted (spine, surface deltas), feature-guide rule retired, chunker heading paths. See `.cursor/plans/requirements-doc-unification-plan-20260917.md`.

- **`fn_ops_daily_recon`** — Fix daily merchant recon timeouts: single-pass 28-day activity aggregates (sargable `created_at` window), split native/migrated ledger sums, 5m local `statement_timeout`, `VOLATILE`. See `docs/OPS_DAILY_RECON.md`.

## 2026-09-14

- **Member sessions (Shopify + loyalty app)** — One shared Edge module `issueMemberSession` (custom HS256 JWT, 30-day `sub` = `user_accounts.id`). Shopify proxy/extension cut over from Supabase Auth magic-link mint; widget retries proxy login once on RPC 401. See `requirements/Authentication.md` and `Shopify.md` MEMBER-ALIGN.

## 2026-09-12

- **Jorakay receipt webhook** — `jorakay_notify_receipt_webhook` skips `net.http_post` when `purchase_ledger.skip_cdc` is true so historical/backfill inserts do not sync to HubSpot. Live completed purchases unchanged. See `requirements/Custom_Webhooks.md`.

## 2026-09-06

- **Outbound integrations (webhook + Klaviyo v1)** — Shared outbox cursor + `merchant_credentials`; partners are separate modules (not Inngest). Signed HTTPS webhook (one URL/merchant) and Klaviyo OAuth + profile properties + v1 metrics + Sync all. Workers on `crm-event-processors` behind `INTEGRATION_WEBHOOK_ENABLED` / `INTEGRATION_KLAVIYO_ENABLED` (default off). See `requirements/Outbound_Integrations.md`.

## 2026-09-05

- **Campaign reward relation layer** — One slot `{ source, target }` for referral / tier entry / lifecycle. `fn_campaign_reward_slot_attach` / `_detach`, `fn_reward_references`, `bff_attach_campaign_reward` / `bff_detach_campaign_reward`, `bff_upsert_campaign_reward_atomic`. Change detaches only; `admin_delete_reward` refuses while referenced. Lifecycle action nodes keep `workflow_node.id` on upsert. See `requirements/Reward.md` §Campaign reward relation layer.

- **Tier / Entry rewards** — Per-tier welcome package on tier config (`tier_entry_rewards` + `tier_entry_reward_grants`). Granted on `apply_tier_change` upgrades only via `fn_dispatch_outcome` (`source_type=tier_entry_reward`); once per member per line. Independent of display Perks and of Earn-rules lifecycle automations. See `requirements/Tier.md` §Entry Rewards.

## 2026-09-04

- **Referral fraud gates** — Claim: Gmail-style canonicalize + local disposable denylist + existing shopper (member / prior order / Shopify Admin, fail-open). Order: withhold referrer payout when buyer is the referrer or not first order (`referral_ledger.status=blocked`). Friend copy for self / throwaway / existing is *This offer isn’t available.* See `requirements/Referral.md` §Fraud gates.

- **Referral / Notifications** — `chokepoint_post_referral_event` emits `crm.events.referral` (`applied` / `claimed` / `settled` / `clawed_back`). Catalog `referral.completed` + `referral.friend_rewarded`; `notification-referral-router` on `inngest-event-router-serve`. No `referral.shared` (copy-link is not a send). Requires Render `OUTBOX_PUBLISH_TOPICS` include `crm.events.referral`. See `requirements/Referral.md` §Notifications and `requirements/Notification_Service.md`.

## 2026-09-03

- **Docs / event pipeline** — Clarified live chokepoint → outbox → Inngest path vs historical CDC/Kafka; outbox batch-50 is API chunking not throttling; per-router Inngest concurrency; event vs function vs serve (`Inngest_Primer.md`, `CRM_Event_Driven_Architecture.md`). Filled `Currency.md` §Complete System Flow Diagram. Split **notifications** (event adapters) vs **reminders** (clock scan → `processNotification`); expiry reminder pacing and defaults (`Notification_Service.md`).

- **Notifications** — Points/reward “expiring soon” on the shared engine (clock scan, not the expiry job). Catalog `currency.expiring_soon` / `redemption.expiring_soon`, merchant local time + timezone, batched Inngest find+send. See `requirements/Notification_Service.md` §Reminders — expiry clock scan.

- **Missions** — Unclaimed for access/claim is `outcomes_distributed`. After `end_date`, detail still loads while the claim window is open (`after_end`) instead of “Mission not found.” List still hides non-claimable rows. See `requirements/Mission.md` §bff_get_mission_detail. Member `bff_claim_mission` takes `p_quantity` (default 1) so a partial claim does not pay every remaining completion.

## 2026-09-02

- **Tier / expiry cache** — Documented cache-first progress and expiry display with eval wrappers (standard vs custom calculators, cron off request path). New `requirements/LOYALTY_PROGRESS_EXPIRY_CACHE.md`; summaries in `Tier.md` §Progress Tracking and `Currency.md` §Points expiry display cache. Render crons: `loyalty-cache-dirty-5m` (`*/5 * * * *`) and `loyalty-cache-catchup-daily` (`0 19 * * *` UTC, **5000** users per `fn_loyalty_cache_catchup_chunk` RPC).

## 2026-09-01

- **Signup / profile** — `editable_by_user = false` is write-once: first empty→value from the member is saved; later member updates are stripped. Completeness still requires required fields. See `requirements/Frontline_Admin_Actions.md` §Field Editability Configuration and `requirements/Signup_Login.md` §6.

- **Admin roles / Team** — `admin_roles.config.store_scoped` (custom default `true`). `false` is All stores. `fn_admin_role_is_store_scoped` is the single rule for Team, store options, profile, and invite. See `requirements/Admin_Panel.md` §admin_roles.

- **Missions** — Period reset (`fn_batch_reset_due_missions`) runs at merchant midnight, clears unclaimed + progress, and treats null `reset_mode` as `global`. Once completions are lifetime-capped in `fn_update_mission_progress`. See `requirements/Mission.md` §Reset Frequency, §Reset Mode, §Loop missions.

- **Currency** — Repair-only `_effective_created_at` on `chokepoint_post_wallet_transaction` (expiry + `_skip_emit` + `_repair_run_id` only; never future). Wrapper `fn_repair_wallet_expiry_burn` always silences outbox. Daily expiry job unchanged. See `requirements/Currency.md` §Database Functions.

- **Reward / claim links** — QR/claim now uses `redeem_reward_with_points` `p_mode='calc'` (same points cost as catalog; 0 only when the brand set 0). Preview returns `points_cost`. Shipping address gate unchanged. Admin push / missions stay `direct`. See `requirements/Reward.md` §Admin Push & Claim Links.

- **AMP / workflows** — Audience-based entry (`entry_type=audience` + `audience_id`, empty groups) now compiles through `fn_amp_resolve_entry_groups` into `is_member_of`, so Activate → Full scan enrolls current audience members. See `requirements/AMP_Workflows.md` §Condition Evaluation.

## 2026-08-31

- **Currency** — FIFO burn allocation moved into `chokepoint_post_wallet_transaction` (`util.fn_allocate_fifo_burn`); expiry now zeros `deductible_balance` and posts one audit burn per wallet/type after all batches. Dropped `trg_fifo_burn_tracking`. See `requirements/Currency.md` §Currency Expiry Processing and §Database Functions.

## 2026-08-28

- **Front Line / Customer 360** — `bff_admin_search_members` compact-phone regex no longer throws `invalid character range`; identifier phone prefix matches `0` and `+66`. See `requirements/Purchase_Transaction.md` §Admin Member 360 Purchase History RPC.

- **Front Line booth** — Slim header `bff_admin_get_frontline_member` + `fn_wallet_today_stats`; catalog `p_user_id` pricing; search btree/name ILIKE; set-based `bff_admin_create_frontline_receipt_batch`; `admin_get_user_purchases` store labels once per distinct store; create-member optional `p_profile`/`p_image` in one write. See `requirements/Purchase_Transaction.md` §Admin Member 360 Purchase History RPC.

- **LINE / Integrations** — `webhook-line` fans out the original LINE webhook body (plus `x-line-signature`) to `credentials.webhook_fanout_destinations` on the LINE Messaging integration after HMAC. Admin: Integrations Hub → LINE Messaging → Webhook fan-out. See `requirements/CS_Channels.md` §LINE Messaging webhook fan-out.

- **Marketplace** — `marketplace-orders-backfill` dry-run returns `missing_order_sns`; `stage_order_sns` (max 50) skips list and writes staging only. Dropped per-call `skipped_exists` staging upserts. See `requirements/Marketplace.md` §Historical backfill.

- **Receipt Upload** — Earn-channel toggles `receipt_number` / `purchase_date` (`{ enabled, required }`) ask the member to enter those values on upload. Saved on `purchase_receipt_upload`; list/approve prefills from them. Channel/Store stay on existing `receipt_selection`. See `requirements/Earn_Channel.md` §Receipt Upload Config and `requirements/Receipt_Upload_Earning.md` §upload_receipts.

- **Shopify / rewards** — Paid order webhooks now mark loyalty-issued discount codes as used (`fn_mark_redemptions_used_from_discount_codes` from `shopify-webhooks`). Merchant / referral codes with no ledger match are skipped. See `requirements/Shopify.md` SECTION: WEBHOOKS and `requirements/Reward.md` §Two-Stage Status Tracking.

- **Signup / login** — `bff-auth-complete` no longer treats `is_signup_form_complete=true` as `next_step=complete`. After auth methods are satisfied it still evaluates the profile template; existing members with missing required fields or mandatory consents get `complete_profile_existing` and missing-only `missing_data`. See `requirements/Signup_Login.md` §4 and Scenario 4.

- **Signup / login** — `bff-auth-complete` now loads `bff_get_user_profile_template` with the member JWT so Front Line–saved profile values pre-fill `missing_data`. Overlay also copies `user_accounts.id_document_type` onto `id_card`. See `requirements/Signup_Login.md` §4 and `requirements/Forms.md` §User Profile Template RPCs.

- **Display Settings** — `link_type` `online_store` with pages `store_home` (`/store`), `store_products` (`/store/products`), `store_specific` (`url` on the headless store). Catalog: `fn_get_display_link_destinations`. See `requirements/Display_Settings.md` §Link destinations.

## 2026-08-27

- **Reward / claim links** — Shipping rewards claimed via QR/link still issue at 0 points (`direct` + `claim_link`) but now require a saved delivery address (`ADDRESS_REQUIRED`). Preview includes `fulfillment_method`. Admin push / missions unchanged. See `requirements/Reward.md` §Admin Push & Claim Links and Shipping Fulfillment.

- **Leaderboard** — Native `api_get_leaderboard_rows` appends the signed-in member’s source-view row when they fall outside `top_x`, so personal quota is not stuck at `0`. Ranked table still shows `top_x`. See `requirements/Leaderboard.md`.

## 2026-08-26

- **Event promotions** — `bff_set_event_promo_folder` assigns existing promos to a folder (or Uncategorised) without rewriting rules/mappings. Admin list bulk Move / Add promotions. See `requirements/Event_Promotion.md`.

- **Integrations** — Renamed audit table `custom_webhook_events` → `custom_integration_events`; added `event_kind` (`forward` / `inbound` / `outbound` / `replay`). Old name remains an updatable view. See `requirements/Custom_Webhooks.md`.

- **FuturePark E-Directory** — Product writeup in `requirements/FuturePark.md` (SECTION: EDIRECTORY-STORE-SYNC / IDENTITY / MAPPING / EMAIL / OUT-OF-SCOPE) for CRM Knowledge ingest. See those sections.

- **FuturePark E-Directory** — Assignment/store paging now orders by `id` and unique-key insert falls back to update so email `changed` is not a cache hole. `custom-futurepark-edirectory-sync` `FUNCTION_VERSION` 8. See `requirements/FuturePark.md` SECTION: EDIRECTORY-STORE-SYNC.

- **Mission / Front Line** — Mission Summary Activity table is the claimed mission only. `fn_mission_claim_extra_activities` no longer appends point/ticket grant lines (wallet/ticket history still records the grants). See `requirements/Mission.md`.

## 2026-08-25

- **Analytics export** — Email CSV on loyalty reports: fact extracts (not chart totals) via `bff_admin_start_analytics_export` + Inngest `export/analytics-csv`. BQ for purchases/redemptions/points/members; PG for activity/surveys/RFM/funnels/campaign attributions/AMP. See `requirements/Analytics.md`.

- **Reports / Points snapshot** — `currency.points_snapshot` + `.rows` closing, holders, opening, and member closing are ledger as-of EOD (Asia/Bangkok), not live `user_wallet`. Current wallet stays on rows for Diff. See `requirements/Analytics.md`.

- **RFM / Reports** — `/reports/rfm` period revenue is one `purchase_ledger` scan joined to `rfm_user_score` (`bff_get_rfm_distribution`, `bff_report_rfm_segment` monetary bands). Same envelope. See `requirements/RFM_Scoring.md` §Functions.

- **Surveys** — Native Analytics report `/reports/surveys` (`bff_report_surveys`, menu `report-surveys`). Completions, unique members, and per-question answers. Config stays on `/survey`. See `requirements/Forms.md`.

- **Surveys** — Optional per-survey header image (`form_templates.banner_url`). Admin configures it on survey settings; member survey header hides when empty. `bff_get_form_details`, `bff_upsert_form`, `bff_get_form_details_user`, `bff_user_get_surveys`. See `requirements/Forms.md`.

- **FuturePark E-Directory** — Location/Merchandise assignments now update when Oracle changes; duplicate `external_ref` skip looks at active CRM rows only; email §2 only when Oracle differs. `custom-futurepark-edirectory-sync` `FUNCTION_VERSION` 7. See `requirements/FuturePark.md` SECTION: EDIRECTORY-STORE-SYNC.

- **Demo / Ausiris** — Three AMP workflows (Welcome, Congratulations, Win-back) plus 6-month `workflow_log` / click series for AMP analytics. Triggers stay inactive. See `demo/ausiris-demo/README.md`.
- **Demo / Ausiris** — Sales channels + business units on stores; purchases stamped to Next / Plus / Express / POS / vending / marketplace / Live with matching SKUs. See `demo/ausiris-demo/README.md`.
- **Rewards / Promo codes** — Merchant-scoped indexes so `v_reward_promo_code_list` seeks this merchant’s codes instead of scanning the global ledger: `idx_rrl_merchant_promo_code`, `idx_reward_promo_code_merchant_reward`. See `requirements/Reward.md`.

- **Rewards / Shopify** — Free-product discount value is editable. Amounts left at/above catalog max still follow product price changes; lowered amounts do not. Column `reward_master.shopify_free_product_sync_price`. See `requirements/Reward.md`.

- **Demo / Ausiris** — Partner-voucher promo codes on 5 e-vouchers (360 codes: available / claimed / used / expired). See `demo/ausiris-demo/README.md`.
- **Demo / Ausiris** — PDPA privacy notice + membership terms (Thai + English) via `custom_ausiris_demo_bootstrap_consent()`. See `demo/ausiris-demo/README.md`.
- **Demo / Ausiris** — One-shot 10k-member seed on merchant Ausiris (`custom_ausiris_demo_*`, table `custom_ausiris_demo_config`). No cron. rocket-demo and New CRM untouched. See `demo/ausiris-demo/README.md`.


## 2026-08-24

- **Mission / Front Line** — One Claim Mission / auto-claim transaction grants each outcome once (`grant_qty = completions × amount`). Non-promo rewards write one ledger row. List missions now return ticket `entity_name`; Mission Summary snapshots ticket/points lines. See `requirements/Mission.md`.

- **Marketplace** — `marketplace-orders-backfill` `refresh_existing` walks with `refresh_after_transaction_date` + `refresh_after_order_sn` and returns `next_cursor`. Same query → platform API → `update_marketplace_order_status` path. See `requirements/Marketplace.md`.

- **FuturePark E-Directory** — Cron moved to midnight Bangkok (`0 17 * * *` UTC). Email copy is 00:00 / รอบเที่ยงคืน. `custom-futurepark-edirectory-sync` `FUNCTION_VERSION` 6. See `requirements/FuturePark.md` SECTION: EDIRECTORY-STORE-SYNC.

- **FuturePark E-Directory** — Sync skips any CustomerNumber that matches more than one CRM `external_ref`, then emails an HTML table report (summary first) to Kankamon / Prakan / Rangwan. `custom-futurepark-edirectory-sync` `FUNCTION_VERSION` 5. See `requirements/FuturePark.md` SECTION: EDIRECTORY-STORE-SYNC.

- **FuturePark E-Directory** — Noon sync creates missing Merchandise/Location attributes and sub-attributes from Oracle `Merc`/`SubMerc`/`Building`/`Floor`. Does not create categories or run until the next cron. `custom-futurepark-edirectory-sync` `FUNCTION_VERSION` 4. See `requirements/FuturePark.md` SECTION: EDIRECTORY-STORE-SYNC.

- **FuturePark E-Directory** — Noon sync inactivates a matched store when Oracle `LeaseEndDate` + 1 month (Bangkok) has passed. Does not auto-reactivate or touch OCR/utility rows. `custom-futurepark-edirectory-sync` `FUNCTION_VERSION` 3. See `requirements/FuturePark.md` SECTION: EDIRECTORY-STORE-SYNC.

- **Marketplace / Inngest** — `process-shop-orders` now `throttle`s 30/min/shop (FIFO queue) instead of `rateLimit` (dropped over-cap runs). Batch 50 + concurrency 10 unchanged. `timeouts.start` not available on inngest@3.22.0 (no SDK bump on live ingest). See `requirements/REGISTRY_RENDER.md` E: inngest-marketplace-serve.

- **Marketplace** — Delivered webhooks can no longer leave `order_ledger_mkp` stuck at `confirmed`: webhook writes `update_marketplace_order_status` immediately; Inngest upsert keeps the further-along of webhook vs API status; Lazada normalizer no longer uses `statuses[0]`. Ops: `marketplace-orders-backfill` `refresh_existing`. See `requirements/Marketplace.md`.

- **Rewards** — `require_points_match` is a hard unmatched block (fallback ignored). Off uses fallback, or 0 if unset / no condition rows. See `requirements/Reward.md`.

## 2026-08-23

- **Referral** — Rewrote `requirements/Referral.md` (and domain index) for the live program: purpose, signup vs purchase journeys, Shopify embed section-gating, Rocket `/r` share hop, SMART claim attribution. Replaces the origin first-purchase / mission writeup. See `requirements/Referral.md`.

## 2026-08-22

- **Currency / Admin** — Advanced Earn: `merchant_master.advanced_earn_enabled` + `advanced_earn_config` (stackable + ordered properties). Super admin `/superadmin/advanced-earn`. Merchant row table on Earn rules; Studio hidden when enabled. Stack is all-or-nothing. See `requirements/Currency.md`.

- **Currency** — Earn conditions drop merchant AND/OR/EACH: cards in a group are always ALL; IDs on one card match any line; thresholds apply only on multiplier eval (EACH per listed entity). Rate+threshold writes rejected. See `requirements/Currency.md`.

## 2026-08-21

- **FuturePark OCR** — Copy/reprint slips (`RE-PRINT`, `พิมพ์ซ้ำ`, `สำเนา`, boxed `Copy`) route to Manual via Call 1 `is_reprint_or_copy` → reason `receipt_copy_or_reprint`. One class: not the original. See `requirements/FuturePark.md` SECTION: PREVIEW-VALIDATION.

- **Front Line** — Redemption History (`admin_get_user_redemptions` `p_filter=history`) keeps cancelled rows (status Cancelled). Processing-receipt delete is not a wallet event: `bff_admin_get_member_wallet_history` no longer unions 0-point `frontline_receipt_void` rows. See `requirements/Reward.md`, `requirements/FuturePark.md`.

- **Mission** — `tickets_earned` now counts mission-outcome ticket earns (`source_type='mission'`). Filter is `ticket_type_id` vs wallet `target_entity_id` only; `earn_source_type` stays on `points_earned`. Recursion stays on progress/claim loop limits. One-shot backfill applied matching `wallet_ledger` earns onto open NULL-`earn_source_type` tickets_earned missions. See `requirements/Mission.md` Condition Types.

- **Mission / Front Line** — Progress Limit leftover at cap is discarded (not parked at target for the next window). Auto-claim writes `frontline_mission_claim_log`. Claim Transaction ID is unique per insert (`mission_claim_transaction_number_seq`); Reward Redemption Summary uses ledger `RW…` codes, not `RED-`. See `requirements/Mission.md`.

- **FuturePark E-Directory store sync** — Daily 12:00 Bangkok cron `custom-futurepark-edirectory-sync` upserts `store_master` from Oracle (`external_ref` = `CustomerNumber`). Does not write OCR/receipt-upload columns or utility stores. Snapshots: `custom_futurepark_edirectory_store_snapshots`. See `requirements/FuturePark.md` SECTION: EDIRECTORY-STORE-SYNC.

- **Rewards** — Visibility `user_only`: member catalog only. Staff cannot spend points (`admin_frontline`) or push-gift (`admin_gift`) on Front Line; mark-used of an existing voucher still works. Existing `user` stays member + Front Line. `api_get_rewards_full_cached` / detail / points preview include `user` + `user_only`. Engine gate `fn_reward_blocks_admin_issue`. See `requirements/Reward.md`.

- **Tier / Customer 360** — Manual member tier assign + per-user downgrade lock: `bff_admin_adjust_member_tier` (`customer-360.update` only) writes via `apply_tier_change(..., 'manual')` and `tier_lock_downgrade`. Daily `evaluate_and_apply_chunk` skips locked downgrades (rolls the maintain deadline); upgrades still apply. Tags stay on `assign_tag` via `bff_admin_assign_member_tag` (C360 or Front Line), never on the eval path. See `requirements/Tier.md`, `requirements/Frontline_Admin_Actions.md`, `requirements/Tag_and_Persona.md`.

- **Front Line / Customer 360** — Staff remarks on a member: `user_notes` (append-only, `created_at` + author). `bff_admin_create_user_note` writes; `bff_admin_get_member_360` returns `data.notes`. Displayed on Front Line header and C360 Profile; Edit Profile adds a remark. See `requirements/Frontline_Admin_Actions.md`.

- **Store classification / Front Line** — `store_attribute_categories.is_store_property` (exclusive per merchant) chooses which dimension prints as Front Line Store Property. `fn_store_property_label` / `fn_store_property_labels` return assigned **attribute names** (no sub-attributes). `get_store_attributes_hierarchy` / `upsert_store_attributes` persist the flag; `admin_get_user_purchases.store_property_label` uses the helper. Future Park Merchandise is flagged. Independent of `is_store_channel`. See `requirements/Store_Attribute_Classification.md`.

- **Rewards** — Admin Reward List can show inactive rewards and Activate/Deactivate (`bff_list_rewards(p_include_inactive)`; `bff_set_reward_active` writes `reward_master.active_status`). Other `bff_list_rewards` callers stay active-only. See `requirements/Reward.md`.

- **Rewards** — Admin Reward List pin writes `reward_master.is_featured` (`bff_set_reward_featured`; `bff_list_rewards` returns and sorts pinned first). Member catalog already shows featured public rewards first as Special Reward. See `requirements/Reward.md`.

- **Rewards** — Admin can manage `reward_category` (name + code) from Rewards → Categories. `bff_list_reward_categories`, `bff_upsert_reward_category`, `bff_delete_reward_category` (blocked while rewards still reference the category). See `requirements/Reward.md`.

- **Front Line / FuturePark** — Processing void writes `notes` = staff reason. Adjust Points Point History shows that text via `bff_admin_get_member_wallet_history` (unions `frontline_receipt_void` rows; settled reverse already uses `wallet_ledger.metadata.reason`). See `requirements/FuturePark.md`.

- **Front Line / FuturePark** — Processing void stays a `rejected` tombstone but is visible: Front Line Cancel tag at 0 points/amount, Approve Receipts Reject + `notes` reason, reprint still omits. `admin_get_user_purchases` unions `frontline_void` queued rows; `v_receipt_upload_batch_summary` / `get_receipt_batch_details` no longer hide them. See `requirements/FuturePark.md`.

- **Front Line / Customer 360** — `bff_admin_search_members` uses `fn_member_search_text` + GIN trigram `idx_user_accounts_merchant_search_trgm` (Mongo-style substring search: name, phone 0/+66, member code, ID/passport). Inner limit before wallet/tier joins. See `requirements/Purchase_Transaction.md` Admin Member 360 Purchase History RPC.

- **Customer 360 / Front Line** — Admin LINE unlink (`line_id: ""` on `bff_admin_update_member_profile`) is honored even when `line_id` is inactive in `user_field_config`. See `requirements/Frontline_Admin_Actions.md` Edit Profile.

## 2026-08-20

- **CS Inbox** — Agent Web Push v1: `cs_agent_push_subscriptions` + `cs_bff_upsert_push_subscription` / `cs_bff_delete_push_subscription`; triggers enqueue Edge `cs-web-push` (`verify_jwt: false`, shared vault secret) on `cs_conversation_events.assigned` and inbound `cs_messages` (`sender_type=contact`) when the chat has an assignee. Standalone admin only. See `requirements/CS_Platform_Features.md` §9.1.

- **CS Inbox** — Chat forward to a person or CS team logs `cs_conversation_events.assigned` (including unassign / team-only). `cs_bff_get_conversation_details` returns `events`. Optional `p_assignment_note`. See `requirements/CS_Conversations.md` Admin BFF.



- **Mission** — Progress/Claim Limit windows now honor `time_value` (`fn_mission_calendar_boundary_start` 4-arg). Multiple completion frequency may store `max_loops_per_transaction` NULL (unlimited). See `requirements/Mission.md` `fn_check_progress_limits`.

- **FuturePark OCR** — `belongs_to_futurepark = no` is no longer downgraded to `uncertain` when `branch_name` is a non-FP mall (CentralWorld / Central Westgate) even if the brand matches an FP store. `upload-receipts-auto-preview` v185, `receipt-preview-v2` v153 (`verify_jwt: false`). Approve Receipt copies `approval_rule_name` through `get_receipt_batch_details`.

- **AMP / Content Library LINE** — Workflow send now uses `fn_render_blocks_as_line_messages`: authored `text` + `image` go out as sequential LINE messages (full-width), not a kilo Flex carousel. Card/carousel blocks stay Flex. `inngest-amp-serve` `resolveResourceMessages`.


- **FuturePark / purchase limits** — First-fit now keeps the highest estimated points (`floor(amount / baht_per_point)`), not the better earn rate. Native amount cap (฿100,000/day) ignores stores with a null earn rate; quantity 10/day and 3/store still count those bills. `fn_preview_purchase_limits` + `fn_check_purchase_limits`; nightly `futurepark-receipt-crm-sync` v74. See `requirements/FuturePark.md` SECTION: PURCHASE-LIMITS.

- **Open API / Currency** — Wallet tickets: `api_post_wallet_transaction` / `api_get_wallet_transactions` accept `currency=ticket` plus `ticket_type_id` or `ticket_code`; `api_get_user` adds `ticket_balances[]`. Credit ticket types stay rejected. Edge `api-wallet` v2. See `requirements/Currency.md` §Open API wallet transactions and `requirements/Open_API.md` §13–14.

- **Display Settings / Integrations** — Signed outbound handoff: Integration Hub type `outbound_handoff`, Display Settings `link_type=handoff`, Edge `start-outbound-handoff` mints a partner JWT and returns `{ redirect_url }`. See `requirements/Display_Settings.md` Link destinations.

- **AMP** — Workflow executor no longer walks into `interaction_router` / `awaiting_interaction` after a failed send. See `inngest-amp-serve`.

## 2026-08-17

- **Customer Export** — Zero-row `users_export` jobs (e.g. condition preview with no matches) finalize a header-only CSV instead of hanging on a missing part. `onFailure` now reads the wrapped Inngest `batch_id` and marks the batch failed. Progress RPC returns `batch_name`. See `requirements/Customer_Export.md` Flow.

- **Reward** — `bff_get_reward_history` now excludes cancelled redemptions (`cancelled IS NOT TRUE`), matching `admin_get_user_redemptions`. Front Line cancel drops the row from member Activity History / Wallet Rewards. See `requirements/Reward.md` RPC Functions.

- **Mission** — Progress Limit now caps completions written in the merchant-TZ window (`fn_check_progress_limits` + `fn_update_mission_progress`). Claim Limit no longer caps loops; payout stays on `fn_check_claim_limits` at claim / auto-claim. Store-set evaluate accepts `store_master.id` UUID as well as store code. See `requirements/Mission.md` §4. PostgreSQL Functions.

- **Mission** — `bff_upsert_mission` persists `earn_source_type` from that jsonb key (admin Earn source is `points_earned` only). `admin_delete_mission` returns `MISSION_HAS_CLAIM_HISTORY` instead of a raw FK when Front Line claim logs exist. See `requirements/Mission.md` Condition Types / `admin_delete_mission`.

- **Product Feature Catalog / Canonical Views** — Added Leaderboard as a campaign-month consumption row (`loyalty.campaign.leaderboard`); EN+TH pricing sheet and features summary list it under Campaigns. Knowledge index now includes `requirements/Leaderboard.md`. See `requirements/Leaderboard.md` §Concept.


- **Leaderboard** — Campaign copy (header, description, column labels, quota label) is on the global dynamic translation system (`entity_type = leaderboard`). Admin edits other languages on Translation → Leaderboards. `api_get_leaderboard_campaign` takes `p_language` and returns one localized payload. Static member chrome was already `ui_translations` `page_key = leaderboard`. See `requirements/Leaderboard.md` and `requirements/Translation_System.md`.

- **Leaderboard** — One config (`campaign_leaderboard`) now has `source_kind` `native` | `legacy`. Native pastes an allowlisted `v_lb_*` / `mv_lb_*` view, Checks columns, and serves `/lb/{path}` from the loyalty session (`api_get_leaderboard_rows`, `api_join_leaderboard`). Legacy keeps Metabase card IDs and `/leaderboard/{path}` + old CRM token. Existing rows backfilled to `legacy`. See `requirements/Leaderboard.md`.

## 2026-08-16

- **Front Line / FuturePark** — Approve-receipts hides Front Line Processing voids (`ocr_suggestions.frontline_void`) from `v_receipt_upload_batch_summary` and `get_receipt_batch_details`. Soft-reject tombstone stays so nightly sync and the duplicate matcher still skip the row. See `requirements/FuturePark.md` SECTION: ADMIN-APPROVE-FLOW.

- **Front Line / FuturePark** — Staff can void queued Front Line Processing receipts (`bff_admin_cancel_queued_frontline_uploads`); History trash uses `can_reverse` on those rows. Front Line duplicate check is a hard block against ledger + active uploads (including Processing). See `requirements/FuturePark.md` SECTION: DUPLICATE-STAGES.

- **Canonical Views** — Authoring home moved to `rocket-agent-plugins/plugins/rocket-sales/commercial/`. Catalog job reports a handoff; this repo keeps requirements, Knowledge index, catalog, and Product Narrative. See `workflows/canonical-views/REFERENCE.md` (pointer) and `workflows/product-feature-catalog/REFERENCE.md` §9.

- **Product Feature Catalog / Canonical Views** — Admin portal and reports is one group after the member journey (PDPA + languages fold into admin; Member 360 is an admin area; 30+ reports). Front Line is its own store-staff group. Catalog run_id `a6d2e8f1-4c70-4b19-9e33-20260816c004`. See `workflows/canonical-views/REFERENCE.md` §3 and `Writing Principles/CANONICAL_VIEW_COPY_PRINCIPLES.md` §3.

## 2026-08-14

- **CRM Knowledge** — `search_docs` default corpus is `requirements/` only; `docs/PRODUCT_NARRATIVE.md` is ingested for `get_section` by heading (sales). See `docs/CRM_KNOWLEDGE_MCP_ARCHITECTURE.md`.
- **Event Promotion** — Admin promo folders (`event_promo_folder` + `event_promo.folder_id`); list/filter only, engine unchanged. See `requirements/Event_Promotion.md`.
- **Order Booking** — Event New Order: merchant-wide member search, inline spot capture, catalogue + freebie gate, Other payment note, staff discount on the ledger after promos. See `requirements/Order_Booking.md`.
- **Purchase Transaction** — `fn_apply_manual_staff_discount` applies cashier discount after event promos via chokepoint `promo_applied`. See `requirements/Purchase_Transaction.md`.
- **Resource Content** — Comma-separated AMP action keys (max 5); `webhook-line` honours Interaction Router `selection_mode` (first key vs fan-out). See `requirements/Resource_Content.md` §Button Actions.

- **Syngenta Events** — `admin_get_my_events` now returns `opened_at` and `closed_at` on each event. Sales App home (`/choose-events`) is a Pending vs Closed/Done summary; click opens `/choose-events/[eventCode]`. Closed/Done = not currently open and `closed_at` is set. See `requirements/Syngenta_Events.md`.

- **Canonical Views** — Unit column is billing period (month / receipt / seat), not member band. SMS price is Free with `{{sms_included}}` quota in the bullet; workflows “10 active” in copy; phone row omitted from the sheet. See `workflows/canonical-views/list-prices.json` and `REFERENCE.md` §3.1 / §4.

- **Product Feature Catalog / Canonical Views** — Core/Advanced rebundle (lifecycle + Front Line actions + promo codes + import + tier earn → Core); new rows flash rewards, LINE/SMS to a segment, LINE Flex; tickets/event promo omitted from views; list prices in `workflows/canonical-views/list-prices.json` (run_id `c8e4f1a2-9b30-4d71-8e22-20260814c003`). See `workflows/canonical-views/REFERENCE.md` §1 / §4 and `workflows/product-feature-catalog/REFERENCE.md` §8.1.

- **Mission / Front Line** — `bff_get_user_missions` returns `claim_exclusivity_group` so Claim Mission Group view can bucket exclusive siblings. No Group Mission master. See `requirements/Mission.md` §bff_get_user_missions.
- **Referral** — Purchase share hop no longer uses `{merchant_code}.rocket-loyalty.app` when the code has dots (Shopify `*.myshopify.com`). `fn_referral_share_hop` hosts on the first DNS label and keeps the full code in `/r/{merchant_code}`. See `requirements/Referral.md`.

## 2026-08-13

- **Product Feature Catalog / Canonical Views** — Thai localize pass after EN sales rewrite: catalog `name_th`/`summary_th` (run_id `e4c8a7b2-1d5f-4a90-9c3e-20260813c002`, 63 updated / 91 rows) and TH pricing sheet + features summary (89/89, same keys as EN; stored value omitted). See `workflows/product-feature-catalog/REFERENCE.md` §6.1 and `workflows/canonical-views/REFERENCE.md` §1 Locales.

- **Referral** — Shopify share URL is now the Rocket `/r/{merchant}?p=shopify&r=` hop: social crawlers get OG title/description from `shopify_wrapper`; humans 302 to landing URL. See `requirements/Referral.md`.

- **Referral** — One domain, two conversions (signup + purchase Shopify/Shopee). Program on/off on `referral_program`; SMART attribution via `referral_claim`; claim mint at Edge (`referral-claim`, `shopify-proxy /referral/claim`). See `requirements/Referral.md`.

- **Product Feature Catalog / Canonical Views** — Shopify is a public module (plugin rows: order earn, member matching). Receipt upload is Core; stored value `planned`; Open API coverage rewrite; pricing sheet sections = modules, rows = plan grouping and/or fee model. See `workflows/canonical-views/REFERENCE.md` §3.1 / §4.

- **Canonical Views** — Section = sellable product (not every `commercial_nature` split): MA one section; CS Software vs AI Customer Service Agent; phone folded in; Shopify as plugin section; stored value omitted (not launched); receipt upload on Core earn methods; Open API as members/purchases/points/redemptions/assets. See `workflows/canonical-views/REFERENCE.md` §4 and `CANONICAL_VIEW_COPY_PRINCIPLES.md` §4.

- **Canonical Views** — Split catalog craft vs view-jump playbook (`CANONICAL_VIEW_COPY_PRINCIPLES.md`); views are written (not flattened). Rewrote EN features summary as shorthand (89/89) and remaining pricing-sheet lines. Thai baselines still stale pending localize. See `workflows/canonical-views/REFERENCE.md` §3 / §7.

- **Canonical Views / Catalog copy** — Added sales-feature copy playbook; rewrote EN pricing-sheet bullets as sales explanations; renamed jargon catalog `name`/`summary` rows (burn, tiers, store directory, signup form, Earn page). Thai baselines still mirror the old EN and need a localize pass. See `Writing Principles/SALES_FEATURE_COPY_PRINCIPLES.md`, `workflows/canonical-views/REFERENCE.md` §3.1, `workflows/product-feature-catalog/REFERENCE.md` §6.

## 2026-08-12

- **Canonical Views** — Dual-locale baselines (EN + TH): Thai pricing sheet + features summary under `docs/canonical-views/th/`, HTML under `docs/canonical-views/export/th/`, npm `export:*:th` / `build:features-md:th`. Sales runs pick `Language: en|th` in brief and copy the matching baseline. See `workflows/canonical-views/REFERENCE.md` §1 Locales.
- **Product Feature Catalog** — Added `commercial_nature` / `consumption_unit` on `internal_product_feature`, rewrote EN/TH sales copy for all 89 features, moved signup form into Signup & login, and exposed TH + commercial columns on `v_internal_product_feature_catalog`. See `workflows/product-feature-catalog/REFERENCE.md` §6.0 / §8.1.

## 2026-08-11

- **AMP Workflows** — `inngest-amp-serve` v66: re-enrollment gate skips postback/router continuations (`parent_workflow_log_id`, `start_node_id`, or `trigger_data.source = line_postback`) so LINE Interaction Router taps are not blocked as `already_enrolled`. See `requirements/AMP_Workflows.md` §Condition Evaluation (re-enrollment).
- **Mission / Front Line** — Persist claim print payloads in `frontline_mission_claim_log` from `bff_frontline_claim_mission`; add `bff_admin_get_mission_claim_history` for History + Re-Print of Mission Summary.

## 2026-08-10

- **Mission** — `bff_upsert_mission` no longer throws `cannot extract elements from a scalar` when FE sends empty/non-array eligibility fields; parses via `fn_jsonb_uuid_array` (null/non-array/[] → all members). See `requirements/Mission.md` §bff_upsert_mission.
- **Mission** — Frontline Claim Mission empty list: `bff_get_user_missions` admin override used removed `admin_users.role`; now `active_status = true` + merchant match (same pattern as `bff_get_mission_detail` / claim-on-behalf). See `requirements/Mission.md` §bff_get_user_missions.

## 2026-08-09

- **Envelope i18n** — `fn_normalize_ui_language`; `redeem_reward_with_points(p_language DEFAULT 'en')` + Thai personal-limit title `คุณได้แลกเกินสิทธิ์ตามจำนวนที่กำหนด`; `inngest-reward-serve` forwards event `language`. Callers omit → English. Broader `bff_*`/`api_*` rollout tracked in `.cursor/plans/envelope-i18n-inventory-20260809.md`. See `requirements/Reward.md` §Member envelope i18n.
- **Envelope i18n** — Member BFFs `bff_save_user_profile`, `bff_validate_signup_code`, `bff_user_update_birth_date`, `bff_get_user_missions` take `p_language DEFAULT 'en'`; titles via `fn_member_envelope_message`.
- **Envelope i18n** — Asset member BFFs (`bff_user_*_asset*`, parking QR), `upload_receipts` (all overloads), `submit_form_public`, receipt admin BFFs (`bff_approve_receipt_upload`, upload config/approval), and mission BFFs (`bff_get_mission_detail`, `bff_upsert_mission`, `bff_list_rewards_for_mission_outcomes`) take `p_language DEFAULT 'en'`; titles via `fn_member_envelope_message` / `fn_mission_envelope_message`.
- **Envelope i18n** — Admin helper `fn_admin_envelope_message`; audience BFFs (create/update/activate/deactivate/delete/refresh/list/analytics/members), deletes (`bff_delete_earn_channel`, `bff_delete_reward_group`, `bff_delete_signup_codes`), `bff_reset_earn_channel_override`, `bff_get_tags`, `bff_list_agents` take `p_language DEFAULT 'en'`. See `requirements/AMP_Workflows.md` §Audience Membership and `.cursor/plans/envelope-i18n-inventory-20260809.md`.
- **Envelope i18n** — Admin package/contract/merchant gets + `bff_admin_cancel_benefit` / `bff_admin_adjust_entitlement` / `bff_admin_get_user_benefits` take `p_language DEFAULT 'en'` via `fn_admin_envelope_message`.
- **Envelope i18n** — Admin batch2: `bff_admin_get_user_packages` / `get_team` / `get_earn_channels` / `get_consent_config` / `get_user_profile_config` / `change_member_mobile` / `change_member_persona` / `adjust_currency` / `assign_benefit` / `assign_package` take `p_language DEFAULT 'en'` via `fn_admin_envelope_message`. See `.cursor/plans/envelope-i18n-inventory-20260809.md`.
- **Envelope i18n** — Scope C inventory complete (96/96): remaining admin upserts/getters take `p_language DEFAULT 'en'`; `fn_admin_envelope_message` wrapper+core; `bff_get_agent_full` consolidated to `(p_agent_id, p_mode, p_language)`. See `.cursor/plans/envelope-i18n-inventory-20260809.md`.
- **Envelope i18n** — Admin batch3: member profile/consent/profile-config upserts + small upserts (`general_config`, `tier_display`, `menu_order`, `notification_settings`, `tag`, `store_master`, `ticket_type`, `persona_master`, signup prefill schema) take `p_language DEFAULT 'en'`. See `.cursor/plans/envelope-i18n-inventory-20260809.md`.

- **Forms & User Profile** — ID card/passport: merchant `id_document_mode` (`id_only`/`passport_only`/`both`) + `enforce_unique_id_document` on `user_field_config.config`; write-time validate/normalize via `fn_prepare_id_document` + trigger (no digit-shape type inference); chokepoint/BFF pass `id_document_type`; admin get/template enrich config. Unique index deferred (legacy dup groups). See `requirements/Forms.md` §ID Card / Passport.

- **Reward** — Lucky Draw thermal slip: `reward_master.physical_draw_name` / `physical_draw_description`; admin get/upsert BFFs; `lucky_draw_slip` on redeem + `api_get_redemption` / `get_redemption_details` when `fulfillment_method=printed`. See `requirements/Reward.md` §Lucky Draw thermal slip.

- **CRM Knowledge v2** — Doc-index over `requirements/**/*.md` (`doc_knowledge_chunks` + `search_docs`/`get_section` MCP). Daily/manual reconcile via `scripts/doc-knowledge-reconcile.mjs`. Torn down typed `internal_knowledge_*` packs/RPCs (kept `internal_knowledge_runtime_secret` for embed Vault). Embedding cron paused until OpenAI credits restored.

- **Requirements corpus** — Wave 13+: Activity/Notification/Import/Order Booking/Tag/Store/Translation/AMP/CS/Package stub + Platform Plan — Kafka/CDC/`NotificationConsumer`/`AmpConsumer`/wrong import edge-fn names corrected to outbox→Inngest; CS Phase-1 table names bannered; deleted sales-only `CS_Competitor_Matrix.md`. See `.cursor/plans/requirements-audit-execution-status-20260809.md`.

- **Requirements corpus** — Removed confirmed-legacy Kafka/CDC archives and stale diagrams (Ecommerce/MARKETPLACE_SETUP/INDEX_FUNCTION dump/Tier_Simplification/amp_analysis); restored live `AMP - Cache Layer.md`; core Tier/Reward/Currency/Purchase docs now describe outbox→Inngest only.


- **Requirements corpus** — Audit batches 0–7: domain-map gaps filled; promoted `Open_API.md` + created `Analytics.md` / `Checkin.md` / `Stored_Value_Cards.md`; Marketplace live expand + Ecommerce/MARKETPLACE_SETUP archived (content retained); Earn Channel ownership clarified; Kafka-era diagrams labeled historical in Tier/Reward/Currency/Purchase; feature-docs duplicates reconciled to archive; `INDEX_FUNCTION` quarantined. See `.cursor/plans/requirements-audit-checkpoint-20260809.md`.


- **Asset** — Member-app BFFs: `bff_user_get_assets`, `bff_user_upsert_asset`, `bff_user_set_asset_status`, `bff_user_delete_asset`, `bff_user_get_parking_entrance_qr` (+ `fn_asset_resolve_auth_member`). Same quota/plate rules as admin; QR is CRM opaque `CRM-PARKING-V1.*` rotating every call, TTL 600s. See `requirements/Asset.md`.
- **Asset** — Fix `fn_asset_config_is_vip`: treat non-array `tags` (incl. jsonb `null`) as non-VIP so Frontline `bff_admin_get_user_assets` no longer fails with "cannot extract elements from a scalar".
- **Asset** — Admin/frontline asset BFFs (quota sections, plate unique, soft-delete, toggle, list/detail, local sync stub) + downtier FIFO hook on `apply_tier_change`. See `requirements/Asset.md`.

- **Mission** — E2 `crm.events.mission` completed emit on `mission_log_completion` insert; E3 dropped eval queue + milestone processor, replaced `mission_log_outcome_distribution` table with shared-log view; OutboxPublisher topics include `mission`. See `requirements/Mission.md` §Operational Notes.

- **Mission** — Gap closure Part I (S0+G1–G6): shared `fn_distribute_mission_completion`, progress-event idempotency, stacked calendar limits, direct-table evaluator (MV dropped), access-state helper, schedule/eligibility/carry-over fields, weekly due-reset cron, upsert validation. See `requirements/Mission.md` §Operational Notes and `.cursor/plans/mission-gaps-closure-plan-20260809.md`.

## 2026-08-06

- **Reward** — Admin 1-to-1 push + QR claim links: table `reward_claim_link`; BFFs `bff_admin_push_reward`, `bff_admin_upsert/list/set_reward_claim_link_*`, `bff_get_reward_claim_preview`, `bff_claim_reward_via_link`; sources `admin_gift`/`claim_link`. See `requirements/Reward.md` §Admin Push & Claim Links.

- **Marketplace** — Historical OpenAPI backfill: `stg_marketplace_backfill` + `fn_promote_marketplace_backfill` (insert-only, claim-ready, skip go-live dupes) and edge `marketplace-orders-backfill` (Shopee/Lazada/TikTok paced list→detail→staging). See `requirements/Marketplace.md` §Historical backfill.

- **Notification Service** — Ticket wallet LINE coalesce: `notification-wallet-router` skips `unit_index != 1` and renders `${amount}` from `unit_total` so one chokepoint ticket award call → one LINE (points unchanged). Deployed `inngest-event-router-serve` v22. See `requirements/Notification_Service.md` §Consumer Event Mapping.

## 2026-08-05

- **Open API** — Renamed detailed REST guide to `docs/Rocket Loyalty Open API Documentation.md` (removed Euro Creations branding); added redemptions + wallet sections with curls/examples. See that file and `docs/openapi/PLATFORM_API_REFERENCE.md`.

- **Open API** — Canonical platform reference updated for live custom domain `https://open-api.rocket-loyalty.com/functions/v1` + full five-edge surface including wallet. See `docs/openapi/PLATFORM_API_REFERENCE.md`.

- **Open API / Currency** — Phase 2 wallet: CRM `api_post_wallet_transaction` / `api_get_wallet_transactions`; Open API edge `api-wallet` (project `mabioklchbkanhjwgibj`, `verify_jwt: false`); points earn/burn + history via chokepoint. See `requirements/Currency.md` §Open API wallet transactions, `docs/openapi/PLATFORM_API_REFERENCE.md`.

- **Marketplace / Lazada** — Fixed New CRM Lazada webhook ingest: OAuth+refresh store numeric `seller_id` as `external_id` (email in `credentials.account`); `get_shop_credentials` shop_id fallback; signed LazadaAdapter `/order/get`+`/order/items/get`; claimable statuses include `confirmed`/`packed`. Backfilled 6 active Lazada shops. See `requirements/Marketplace.md`.

## 2026-07-29


## 2026-08-01

- **Admin Panel / Loyalty setup** — Action-oriented step titles/descriptions in `fn_loyalty_setup_mastery_text` (e.g. Set your purchase earn rules, Create your first redeemable reward). See `requirements/Admin_Panel.md` §Loyalty homepage setup mastery.

- **Admin Panel / Loyalty setup** — `bff_get_loyalty_setup_mastery(p_language)` returns en/th display copy via `fn_loyalty_setup_mastery_text` (`title`, `progress_label`, `action_label`, `priority_label`). See `requirements/Admin_Panel.md` §Loyalty homepage setup mastery.

- **Admin Panel / Loyalty setup** — Dropped `display_settings` from `bff_get_loyalty_setup_mastery()` (Shopify widget + member surfaces use defaults); checklist is earn → rewards → tiers; `is_launch_ready` = earn+rewards. See `requirements/Admin_Panel.md` §Loyalty homepage setup mastery.

- **Analytics / BQ dashboards** — Added `analytics-query` report `home.metrics` (home KPI strip: last-30d `member_added`/`earn`/`redeemer`/`driven_revenue` + all-time `total_members`). See `docs/BQ_PARTITION_AND_CLUSTERING.md`.
- **Admin Panel / Loyalty setup** — Added `bff_get_loyalty_setup_mastery()` homepage checklist (earn basic, rewards, display×2, tiers×2); `merchant_master.default_burn_rate` DEFAULT 0; award/expiry already column-defaulted. See `requirements/Admin_Panel.md` §Loyalty homepage setup mastery.

- **Rocket Demo / Analytics** — Lifestyle store channels (20 stores / 6 CHANNEL attrs), daily coupon-usage seed (`used_at`/`used_store_id`), `custom_internal_demo_bootstrap_store_channels` + `custom_internal_demo_enrich_redemption_usage`; `bff_report_redemptions` + `analytics-query` `rewards.redemptions` add `by_used_store` / `by_used_channel`. See `demo/rocket-demo/README.md`.
- **Reward** — Member redeem envelopes (`title`/`description`) localized th/en via `fn_redeem_member_message`; language = merchant default (`merchant_languages`). Fixes raw `all_time` in limit sentences; `redemption_failed` broadcast inherits RPC copy. See `requirements/Reward.md` §Member envelope i18n.
- **Reward** — Reward variants (Tier A): `reward_master.variant_config` + ledger `selected_variants` snapshot; admin/user BFFs + `redeem_reward_with_points(p_selected_variants)`; `inngest-reward-serve` + Render `crm-api` (`7baecf4`) forward `selected_variants` on `POST /redemptions`. See `requirements/Reward.md` §Reward Variants.
- **Rocket Demo** — Engagement seed layer for merchant `rocket-demo`: personas/tags, Earn Studio (`earn_factor_group` + conditions + weekend time boost), 3 missions, 2 spin wheels, 2 surveys, 5 AMP audiences + 2 agents; historical enrich + daily `custom_internal_demo_generate_day_engagement`. Canonical SQL under `demo/rocket-demo/waves/04–06_*`. See `demo/rocket-demo/README.md`.

## 2026-07-26

- **Currency / Tier** — Merchant-global points→discount burn: `merchant_master.default_burn_rate` + optional `tier_master.burn_rate` override; `fn_resolve_burn_rate` used by `get_user_burn_rate`, point-discount preview/create, BigCommerce profile; currency BFFs read/write default. See `requirements/Tier.md` §Burn Rate, `requirements/Currency.md` §Merchant Configuration, `docs/BURN_RATE_FE_BRIEF.md`.

- **Tier** — Program owns the clock, tiers own only thresholds. New `tier_program_config` (metric, period_type, rolling_months / period_start_month, upgrade_timing) + derived `v_tier_ladder`; backfilled 49 `(merchant, user_type)` groups. Dropped `tier_conditions.metric/window_type/window_months/window_start/upgrade_evaluation_timing/frequency/window_start_field/upgrade_evaluation_value`, `tier_master.ranking/entry_tier`, `tier_pending_upgrades.to_tier_ranking`, `amp_analysis_member_attrs.tier_ranking`, `tier_evaluation_tracking.maintain_condition_ids`. See `.cursor/plans/tier-program-config-plan.md`.
- **Tier** — Engine rewrite: new `fn_tier_window` / `fn_tier_ladder_for_user`; `calculate_maintain_deadline` re-keyed to `(merchant, user_type, achieved_date)`; `apply_tier_upgrade` → `apply_tier_change(p_change_type)` so downgrades no longer log as upgrades; maintain success now advances `maintain_deadline`. Deleted `calculate_tier_window_dates`, `calculate_next_period_end`, `calculate_upgrade_effective_date`, `reorder_tiers`, `set_entry_tier`.
- **Tier** — Ladder order and entry tier are derived from active upgrade amount, not stored. A member can only be granted an entry tier that has no upgrade threshold, so unearned high tiers are never assigned at signup. New `bff_get_tier_program_config` / `bff_upsert_tier_program_config`; `bff_upsert_tier_with_conditions` rejects deprecated fields and requires program config first. Unique active upgrade amount per `(merchant, user_type)` enforced by trigger.
- **Tier** — Program-level `maintain_mode` (`lifetime`|`period`); maintain bar = current rung upgrade amount; dropped per-tier maintain conditions. See `requirements/Tier.md` and `docs/TIER_PROGRAM_CONFIG_FE_BRIEF.md`.

## 2026-07-24

- **Authentication** — Multi-merchant admin metadata: `sync_admin_to_user_metadata` no longer stomps selected merchant; `admin_set_active_merchant` writes `merchant_name`; `sync_admin_metadata_after_login` prefers `active_merchant_id` / newest; hook emits matching `merchant_name`. See `requirements/Authentication.md` §Multi-merchant admin selection.
- **Member Freeze** — `user_accounts.is_freeze` wired: returned on `get_user_summary` + `bff_admin_get_member_360`; admin toggle `bff_admin_set_member_freeze`; self-service guards via `resolve_target_user` / spin / referral / mission claim. See `requirements/Member_Freeze.md`.
- **CS Channels / DooPage** — Shopee CS chat via DooPage: live ingest on `webhook-doopage-cs` → `cs-ingest`; shop link OAuth (`doopage-cs-get-auth-url` / `doopage-cs-exchange-token`); Browser-SDK outbound (`cs-send-message` + `doopage-cs-sign-send`); `webhook-shopee` → 410. See `requirements/CS_Channels.md` §DooPage and `requirements/architecture/doopage-partner-handoff.md`.

## 2026-07-22

- **Rocket Demo / Internal** — New merchant `rocket-demo` (`fae172a5-de90-440e-a766-db6a5982cc9b`) with `custom_internal_demo_config` + generator functions (`custom_internal_demo_bootstrap_merchant`, `custom_internal_demo_generate_day`, `custom_internal_demo_seed_range`, `custom_internal_demo_daily_job`). 6-month backfill (~2500 members) + pg_cron `custom-internal-demo-daily`. Canonical sources in `demo/rocket-demo/`. See `demo/rocket-demo/README.md`.

## 2026-07-20

- **Receipt Upload / Future Park** — Deferred CRM sync failures now use structured `crm_sync_error_code`+`crm_sync_error_params`; quota → member Reject with dynamic limits; infra/API → keep approved+`crm_sync_status=failed` and member history shows pending/Processing. Edge `futurepark-receipt-crm-sync` v20. See `requirements/Receipt_Upload_Earning.md` §purchase_receipt_upload / §get_receipt_upload_history.
- **Analytics / BQ dashboards** — Edge Function `analytics-query` (public JWT check inside): named reports (`transactions.purchases[.rows]`, `rewards.redemptions[.rows]`, `members.overview[.rows]`, `currency.points_*`) query `rocket-prod-analytics.serving_loyalty` facts + `raw_supabase_prod` dims; secrets `BQ_SERVICE_ACCOUNT_JSON`, `BQ_PROJECT_ID`, `BQ_LOCATION`. Loyalty-admin `/reports/*` swapped off `bff_report_*` for wave-1. See `docs/BQ_PARTITION_AND_CLUSTERING.md`.
- **Analytics / BQ dashboards** — Added named report `overview.loyalty` (composite purchases/redemptions/members/points summaries); loyalty-admin `/reports/exec-overview` rewired to it. See `docs/BQ_PARTITION_AND_CLUSTERING.md`.

- **Receipt Upload / Notifications** — New `chokepoint_post_receipt_event` (`submitted`/`rejected`/`approved` stub) emits `crm.events.receipt`; wired from `upload_receipts`, `api_create_receipt_upload`, and reject in `bff_approve_receipt_upload`; Thai system templates + `notification-receipt-router` on `inngest-event-router-serve`. Requires Render `OUTBOX_PUBLISH_TOPICS` include `crm.events.receipt`. See `requirements/Receipt_Upload_Earning.md` §Receipt LINE notifications.

## 2026-07-17

- **Integrations / Custom Webhooks** — Edge loggers skip inserts when `filter_matched === false` (Yuedpao/Dr.PONG/Kao/4Care + marketplace TikTok/Shopee/Lazada deployed). Matched/errors still audited; Dr.PONG replay loses new filtered Waiting rows. See `requirements/Custom_Webhooks.md` §Audit table.

## 2026-07-15

- **Syngenta Events** — Coordinators can search and upsert participating retailers (`bff_search_admin_stores` / `bff_upsert_event_stores` role gate now `owner | coordinator | superadmin`, matching list). See `requirements/Syngenta_Events.md` §Participating retailers / §Auth model.

- **Currency / Reward / Frontline** — Customer 360 wallet ledger + used redemptions: new `bff_admin_get_member_wallet_history` (paginated `wallet_ledger`, `all|points|ticket`, `customer-360.read`); `admin_get_user_redemptions` adds `p_filter='used'`. See `requirements/Frontline_Admin_Actions.md` §Customer 360 Integration, `requirements/Reward.md` §Core Redemption Functions.

## 2026-07-14
- **Activity Log & Attribution** — Custom campaign identifiers now resolve: `mkt_campaign_identifier.key_name` (required for `key_type=custom`); `fn_log_user_activity` matches `p_attribution->>key_name = key_value` after voucher/line/link/utm; `bff_get`/`bff_upsert_mkt_campaign` round-trip `key_name` and validate against active attribution fields. See `requirements/Activity_Attribution.md` §mkt_campaign_identifier / §Business rules.
- **Activity Log & Attribution / Reports** — B5/B6: extended `bff_report_purchases`/`_rows` (attributed campaign + acquisition UTM filters/columns) and `bff_report_members`/`_rows` (acquisition, RFM segment, funnel stage columns + filters); loyalty-admin `/reports/purchases` and `/reports/members` wired. Signatures unchanged. See `requirements/Activity_Attribution.md` §Functions / §Admin UI.

- **Activity Log & Attribution** — Added `bff_report_mkt_campaigns` (date-ranged campaign performance: KPIs/series/drills; ROI only for full-window ranges; cohort acquisition revenue). Loyalty-admin campaign form Performance panel wired. See `requirements/Activity_Attribution.md` §Functions / §Admin UI.
- **Activity Log & Attribution / Reports** — Phase 2 A2: `/reports/marketing-campaigns` cross-campaign ROI report (reuses `bff_report_mkt_campaigns`); admin menu `report-mkt-campaigns`; drill deep-link to campaign Performance. See `requirements/Activity_Attribution.md` §Admin UI.
- **Activity Log & Attribution / Reports** — Phase 3 A3: `bff_report_acquisition` + `/reports/acquisition` (cohort lead-source KPIs, UTM/campaign mix, TTFP, member drill). Menu `report-acquisition`. See `requirements/Activity_Attribution.md` §Functions / §Admin UI.
- **Funnel / Reports** — Phase 4 A4: extended `bff_get_funnel_metrics` (custom from/to, list mode, optional series, conversion_rate/KPIs) + `/reports/funnels`. Existing 7/30/90 builder callers unchanged. See `requirements/Funnel.md` §Functions.
- **RFM / Reports** — Phase 5 A5/B3: extended `bff_get_rfm_distribution` (kpis/coverage/risk pool, optional segment×revenue + R×F heatmap) + `/reports/rfm`; settings panel shows coverage + link to report. See `requirements/RFM_Scoring.md` §Functions.
- **Activity Log & Attribution / Reports** — Restored `bff_report_acquisition` (A3 + A7 sections `activity_volume`/`activity_no_purchase`); Phase 7 `/reports/activity` + `/reports/exec-overview` (composes existing BFFs); Phase 6 B4 FE surfaces `attributed_campaigns` / `funnel_stage` / campaign stamp on Customer 360. Menu ids `report-activity`, `report-exec-overview`. B5/B6 purchases/members columns deferred. See `requirements/Activity_Attribution.md` §Functions / §Admin UI.

## 2026-07-13

- **Receipt Upload (admin list)** — Added `bff_list_receipt_uploads` cursor-paginated BFF (status/search/date/store filters, status counts). Top-N on `purchase_receipt_upload` then light joins; no wallet laterals. Next admin `/receipts-upload` uses this RPC with infinite scroll; no longer queries `v_receipt_upload_list`. See `ProjectDocs/FE_docs/ReceiptUploads.md`.

## 2026-07-10

- **Signup/Login** — `get_user_summary()` now returns additive `member_code` from `user_accounts.member_code` (nullable text). Unblocks My QR / login summary consumers that expected it in the payload. Signature unchanged. See `requirements/Signup_Login.md` §`bff-auth-complete` / member code.
- **Display Settings** — Canonical link destination catalog (`fn_get_display_link_destinations`) including `drawer_my-qr`; exposed on `get_display_settings_menu` as `{ blocks, link_types, pages }` and on link-capable blocks via `ui_config.page_options` / `link_type_options`. See `requirements/Display_Settings.md` §Link destinations.

## 2026-07-07

- **Tier / Mission / AMP-Outcome / Notification / Infra** — Remaining chokepoint consumer routers ported to `inngest-event-router-serve` v2 (14 Inngest functions): tier (`process_tier_event`, per-user ordering via concurrency key), mission (`mission/evaluate` fan-out; DB RPC gate replaces Redis mission cache), outcome attribution (`amp_outcome_attribution` insert), and LINE notification (resolve → flex render → push → `notification_log`, single-step so LINE tokens never persist in Inngest state). AMP realtime dispatch remains the last un-ported consumer. Manual steps pending: re-sync Inngest app + expand `OUTBOX_PUBLISH_TOPICS` to wallet/tier_change/user/redemption. See `requirements/REGISTRY_RENDER.md` E: inngest-event-router-serve and `.cursor/plans/confluent-deactivation-refactor-20260707.md` §8b.
- **Currency / Infra** — Purchases → currency pipeline cut over from Kafka to Inngest fan-out (Confluent deactivation): `crm-event-processors` OutboxPublisher repointed to Inngest (PR #6: topic → `crm/<domain>.event`, deterministic event ids, `OUTBOX_PUBLISH_TOPICS` allowlist, `KAFKA_CONSUMERS_ENABLED=false` gate on all 8 legacy consumers); new edge fn `inngest-event-router-serve` (app `crm-event-router`) hosts `currency-purchase-router` + `currency-purchase-item-router` replacing `CurrencyConsumer` (same predicate/dedup/award-timing, emits unchanged `currency/award`); unpublished outbox rows reset for drain. Cursor-based outbox consumption rejected — decision record in plan §7. See `requirements/Currency.md` §Executive Summary and `.cursor/plans/confluent-deactivation-refactor-20260707.md` §7–8.
- **Currency** — Added seller-referenced receipt upload attribution and seller-perspective earn factor groups/calculation for direct receipt approval; non-receipt event-worker seller pass remains a follow-up. See `requirements/Currency.md` §Seller Perspective Earning.

## 2026-07-06

- **Platform** — Added `gemini-generate` edge function + `callGemini` helper (`gemini-3.5-flash`); API key stored in Vault as `gemini_api_key`. See `requirements/REGISTRY_RENDER.md`.
- **Check-in** — Fixed duplicate-key error on repeat same-day check-ins: dropped `checkin_ledger` unique constraint on (user_id, merchant_id, checkin_id, checkin_date) — stale after the per-day guard was removed from `process_checkin` — replaced with a plain index. Fixed `fn_compute_checkin_streak.next_deadline`: NULL when the campaign has no time window (FE was showing a phantom deadline); when a window exists, computed at next `window_end_time` in the campaign timezone (was UTC, showed e.g. 06:59 instead of end-of-window). See `requirements/feature-docs/checkin.md` §7, §9.

## 2026-07-03

- **Docs** — New `requirements/BFF_Conventions.md`: consolidated conventions for admin (`bff_*`) and user (`bff_user_*`) BFF functions — auth blocks (merchant/user derived from token), payload/response structure, standard skeleton, and per-category guidance (list page, config page, submit, user read/action). No code changes.

## 2026-07-02

- **User export (`bff_admin_start_user_export`)** — Fixed batch creation failing on `bulk_import_batches.file_name` NOT NULL: export now auto-generates a placeholder `users_export_YYYYMMDD_HH24MISS.csv` at insert time (import already passed `p_file_name`). Added EXCEPTION handler matching import BFF.

## 2026-07-01

- **Spin Wheel (`bff_get_spin_wheel_details`)** — Fixed edit-load SQL error for segments with `earn_factor` outcomes: replaced invalid `ef.name` reference (column does not exist on `earn_factor`) with a synthesized `entity_label` from `earn_factor_type`, `earn_factor_amount`, and `target_currency` (ticket targets resolve ticket type name when present). Points/tickets/reward outcomes unchanged.
- **Spin Wheel** — Expanded `requirements/Spin_Wheel.md` §Presentation & `display_config`: admin vs user-app contract, endpoint payload matrix, suggested key vocabulary, image/i18n notes, and common misunderstandings (spin response omits segment `display_config`).

## 2026-06-29

- **Display Settings (profile card ticket field)** — Profile Card (`style_1` / `style_2`) supports `data_type: ticket` with required `ticket_type_id`. Save validation enforces merchant-scoped UUID; runtime overlay `fn_overlay_profile_card_user_values` injects per-user balance on `bff_get_display_blocks_cached` and `api_get_display_blocks_cached` (post-cache, not cached). Inactive/out-of-window ticket types and guest users return `value: 0`. Admin picker: `get_entity_options_by_type('ticket_type')`. See `requirements/Display_Settings.md` §Profile card data fields.

- **Check-in admin BFFs (`bff_get_checkin_details` + `bff_list_checkins`)** — Closed the admin-side CRUD gap: the loyalty-admin Check-in form now has all three BFFs (upsert + get + list). `bff_get_checkin_details(p_mode text DEFAULT 'edit', p_checkin_id uuid DEFAULT NULL)`: 'new' returns an empty template shaped exactly like the `bff_upsert_checkin` input contract (round-trip identity verified — every field the form sends to upsert, get returns); 'edit' loads the parent + outcomes + limits. Outcomes include `entity_name` (joined from `reward_master` for `reward`, `ticket_type` for `tickets`; `earn_factor` has no name column so it stays null). `bff_list_checkins()`: returns `{success, data:[...]}` with one row per campaign plus summary stats computed via LATERAL joins — `outcome_count`, `limit_count`, `total_checkins`, `unique_users`, `last_checkin_at` — so the FE list table renders without a second round-trip. Both SECURITY DEFINER, merchant from JWT via `get_current_merchant_id()`, RLS-safe. Smoke-tested: new-mode template, edit-mode full round-trip (upsert → get_details → field-by-field identity match across all 25 fields), list with stats (3 check-ins by 2 users → `total_checkins=3, unique_users=2`), edit-mode with NULL id, foreign-merchant get (rejected), foreign-merchant list (empty data, no leakage). Docs: `requirements/feature-docs/checkin.md` §9.2 has both signatures, §9.4 marks both as ✅ Live.

- **Check-in admin BFF (`bff_upsert_checkin`)** — Added the admin-side RPC that backs the loyalty-admin Check-in config form (FE was already wired and failing with "function not found"). `bff_upsert_checkin(p_config jsonb) -> jsonb`, SECURITY DEFINER, merchant from JWT via `get_current_merchant_id()`. Single-RPC upsert for the campaign header (`checkin`) plus **full-replace** of both children: `checkin_outcomes` and `transaction_limits WHERE entity_type='checkin'` (DELETE-all-then-INSERT on every call — outcomes/limits missing from the payload are deleted). Validates `name` / `frequency_type` (daily|weekly) / `condition_type` (consecutive|fixed_period_count|rolling_period_count) and returns `{success:false, error:"<field> required"}` on miss rather than throwing. Update path enforces merchant ownership (matched on `id + merchant_id`); foreign-merchant attempt returns `{success:false, error:"Check-in campaign not found or access denied"}`. Return shape: `{success:true, checkin_id}` / `{success:false, error}`. **Spec deviation surfaced:** the input spec listed a `time_value` field on limits, but the live `transaction_limits` schema has no such column (it uses `count` + `time_unit` only, with optional absolute `window_start`/`window_end` overrides) — the BFF does NOT accept `time_value`. `process_checkin` (the reader) already ignores it. Smoke-tested: create (1 checkin + 2 outcomes + 1 limit), update (replace outcomes, drop limit), empty children, merchant mismatch, all three validation cases. Docs: `requirements/feature-docs/checkin.md` §9.4 implementation status updated.

- **Check-in documentation (feature guide rewrite + backend spec)** — Replaced the archived `requirements/feature-docs/checkin.md` with a fresh 8-section feature guide reflecting the rewritten backend: three condition types (`consecutive`/`fixed_period_count`/`rolling_period_count`), multi-outcome dispatch via `fn_dispatch_outcome`, ledger-derived streaks (no `checkin_streaks` table), campaign-level eligibility mirroring `reward_master`, and participation caps via the central `transaction_limits` (`entity_type='checkin'`). Appended **Section 9: Backend Architecture & FE Contract** — full schema for `checkin`/`checkin_outcomes`/`checkin_ledger`, function signatures and return JSON shapes for `process_checkin` / `get_user_checkin_status` / `fn_compute_checkin_streak`, the dispatch path table (points/tickets/reward/earn_factor → chokepoints), error-code matrix, and implementation status/gaps (admin BFFs + member FE still unbuilt). Restored the guide to active status in `INDEX.md` (removed from the archived list). FE handoff can now proceed from §9.

## 2026-06-28

- **Check-in backend (condition_type/trigger_type to text)** — Converted `checkin.condition_type` and `checkin_outcomes.trigger_type` from DB enums to plain `text`, and renamed `condition_kind` → `condition_type` (matches the established `*_type` suffix convention). Value correctness is now controlled by `process_checkin`'s switch logic, not by DB type constraints — per workspace preference. Dropped enum types `checkin_condition_kind` and `checkin_outcome_trigger`. Updated both functions that read these columns (`process_checkin`, `get_user_checkin_status`); `fn_compute_checkin_streak` doesn't touch them (uses `frequency_type` only) so unchanged. Defaults preserved (`'consecutive'` / `'every_checkin'`). Smoke-tested end-to-end. Same plan: `.cursor/plans/central-outcome-dispatcher-plan-20260628.md`.
- **Check-in backend rewrite (multi-condition, multi-outcome, ledger-derived)** — Rebuilt the check-in engine to support the three requested condition shapes (consecutive streaks, fixed-period count, rolling-period count) and multiple outcomes per check-in, all dispatching through the existing `fn_dispatch_outcome` central dispatcher. **Schema:** two new enums (`checkin_condition_kind`: consecutive/fixed_period_count/rolling_period_count; `checkin_outcome_trigger`: every_checkin/streak_milestone/period_milestone). `checkin` (campaign header) gained reward-style eligibility arrays (`allowed_tier`, `allowed_persona`, `allowed_tags`, `allowed_birthmonth`) + condition config (`condition_kind`, `period_start`, `period_end`, `rolling_window_days`, `period_target_count`). New `checkin_outcomes` table — one row per configured outcome (uses `outcome_type_enum` from the dispatcher so points/tickets/reward/earn_factor all share the same vocabulary); `trigger_type` selects when it fires (`every_checkin` always; `streak_milestone` when streak is in `[streak_day_start, streak_day_end]`; `period_milestone` when period count == `period_count_target`, fire-once). Added `'checkin'` to `wallet_transaction_source_type` and `'checkin'` to `entity_type` (so participation caps reuse the central `transaction_limits` table — scope user/total, count, time_unit day/week/month/year/all_time). **Dropped legacy tables** `checkin_conditions` and `checkin_streaks` clean — no data migration (treated as new feature); `checkin_ledger` is the single source of truth, with `matched_outcomes` jsonb capturing every dispatched outcome (`{outcome_id, outcome_type, entity_id, amount, transaction_id, distribution_id}`). **Functions:** new `fn_compute_checkin_streak(p_user_id, p_checkin_id) -> TABLE(...)` pure helper (one definition of streak math, called by both process_checkin and get_user_checkin_status). Rewrote `process_checkin` (validate → eligibility → participation caps → compute streak/period from ledger → match outcomes by trigger → loop dispatch via `fn_dispatch_outcome` with per-outcome dedup key `'checkin:<checkin_id>:<outcome_id>:<user_id>'` → insert ledger row → return streaks + matched outcomes). Rewrote `get_user_checkin_status` (ledger-derived status: today_checked, current/longest streak, total check-ins, last/next dates, period_count vs target, upcoming milestones for FE progress display). **Per-outcome dedup key** prevents wallet-ledger dedup-index collisions when one check-in dispatches multiple currency outcomes (e.g. two `points` outcomes). Smoke-tested: first check-in fires only `every_checkin` (streak=1, milestone silent); day-3 streak fires both every_checkin + streak_milestone reward; fixed-period milestone fires exactly at `period_count_target` and never re-fires; two points outcomes in one check-in both succeed. Plan: `.cursor/plans/central-outcome-dispatcher-plan-20260628.md` §8. OUT OF SCOPE this thread: admin BFFs (`bff_upsert_checkin*`, `bff_get_checkin_details`, `bff_list_checkins`), FE, full `Checkin.md` requirement doc — those are follow-up work.
- **Central Outcome Dispatcher (Phase 1: foundation)** — Added one central outcome dispatcher every campaign-like feature delegates to. New `outcome_type_enum` (points|tickets|reward|earn_factor), new `outcome_distribution_log` table (central audit: one row per outcome attempt, success or failure, with wallet_ledger_id/redemption_id/earn_factor_user_id cross-refs), new `fn_dispatch_outcome(p_user_id, p_merchant_id, p_outcome_type, p_entity_id, p_amount, p_source_type, p_source_id, p_metadata, p_dedup_key) -> jsonb` (SECURITY DEFINER). Routes points/tickets through `chokepoint_post_wallet_transaction` (sync), reward through `redeem_reward_with_points(p_mode=>direct)`, earn_factor through direct `earn_factor_user` insert. Failure path catches exceptions and still writes the audit row (returns `{success:false, error}` instead of throwing). Smoke-tested all four outcome types end-to-end against live fixtures. Plan: `.cursor/plans/central-outcome-dispatcher-plan-20260628.md`.
- **Central Outcome Dispatcher (Phase 2: AMP delegation, backward compatible)** — Refactored `fn_execute_amp_action` so its five outcome branches (`award_points`, `award_tickets`, `award_currency`, `push_reward`, `assign_earn_factor`) delegate to `fn_dispatch_outcome` then re-wrap into the exact legacy return shape. **Signature unchanged. Return shapes unchanged for points/tickets/currency/earn_factor.** `workflow_log`, cost normalization (`entity_cost_normalization`), dedup threading, and the exception handler all preserved verbatim. Non-outcome actions (`assign_tag`, `remove_tag`, `assign_persona`, `submit_form`, `add_to_audience`, `remove_from_audience`) untouched. **One deliberate contract change**: `push_reward` now returns the dispatcher's uniform jsonb (`{success, transaction_id, redemption_id, distribution_id, ...}`) instead of the raw `redeem_reward_with_points` payload — confirmed no FE/Inngest/docs caller in this repo reads the legacy `result.title` / `result.data.redemptions[0]` keys; grep verified. Each AMP outcome action now ALSO writes an `outcome_distribution_log` row (bonus visibility). Backward-compat gate passed: ran all seven action types, diffed return shapes key-by-key, verified `workflow_log` rows + `outcome_distribution_log` rows. Plan: same.
- **Central Outcome Dispatcher (Phase 3: mission sync collapse)** — `fn_process_mission_outcomes` now routes ALL outcome types (points, tickets, reward) through `fn_dispatch_outcome` synchronously. **BREAKING behavior change (approved):** mission currency grants no longer go through the async `net.http_post → publish-currency-event` edge function — they now hit `chokepoint_post_wallet_transaction` directly, matching AMP and check-in. Wallet ledger rows are visible immediately after `fn_process_mission_outcomes` returns (previously depended on edge-function processing latency). `mission_log_outcome_distribution` is **frozen** as read-only history (no longer written); new audit lives in `outcome_distribution_log WHERE source_type='mission'`. Mission mechanics (claim-exclusivity check, `fn_check_claim_limits`, `mission_log_completion` cursor with `FOR UPDATE`, post-loop `is_claimed=true` + `outcomes_distributed=true`, `mission_progress` counter bumps) all preserved unchanged. The `publish-currency-event` edge function is now orphaned (no DB caller remains) — NOT deleted in this phase (out of scope). Verified end-to-end: claimed one standard mission with points + reward outcomes, confirmed completion marked claimed, progress counters bumped, two `outcome_distribution_log` rows with correct cross-refs, zero new `mission_log_outcome_distribution` rows. Plan: same. **CHANGELOG flag for ops:** if you have monitoring/alerting on `publish-currency-event` invocations, expect traffic to drop to zero going forward.
- **Central Outcome Dispatcher (Phase 4: check-in refactor + 2 latent bug fixes + IS NOT NULL footgun)** — `process_checkin` STEP 8 CASE block now routes all four outcome types through `fn_dispatch_outcome`. **Bug fix 1 (reward):** the old positional call `redeem_reward_with_points(p_user_id, v_condition.entity_id, 1, null)` was putting `null` in `p_user_id` and defaulting `p_mode` to NULL → would have tried to burn points the user may not have. Now goes through dispatcher with `p_mode:='direct'` (zero points deducted). **Bug fix 2 (earn_factor):** the old hardcoded `INTERVAL '7 days'` is replaced by the dispatcher's default 30-day window (configurable via `metadata.window_days`); the dead `ON CONFLICT (user_id, earn_factor_id)` branch (constraint never existed) is gone. **Bonus pre-existing bug fix:** the `IF v_condition IS NOT NULL` check was unreliable due to Postgres composite-type semantics (both `IS NULL` and `IS NOT NULL` return false when a row has mixed null/non-null fields — which is the normal case for `checkin_conditions` since it has nullable columns); replaced with `FOUND`. Streak math (STEP 5), condition matching (STEP 7), streak update (STEP 9), and ledger insert (STEP 10) all preserved. Tickets amount: now respects configured `outcome_amount` instead of hardcoded 1 (backward compatible — existing data is all 1). Source type kept as `'campaign'` (label already exists in `wallet_transaction_source_type`). Verified end-to-end: cross-references align on the same ledger id across `checkin_ledger.outcome_transaction_id` ↔ `outcome_distribution_log.wallet_ledger_id` ↔ `wallet_ledger.id`. Plan: same.

- **Platform / Plan / Feature scoping (direct-reader audit + upload_receipt fix)** — Audited every function touching `feature_config` for the default-on bug class (functions that read `enabled` directly and default missing to `false`, causing on-by-default features to show as off). Found 5 direct readers; classified by what they read: `bff_get_user_history_menu` (fixed prior entry), `bff_approve_receipt_upload` and `fn_merchant_deferred_crm_confirm` read **config only** (not `enabled`) — safe; `bff_set_upload_receipt_config` is a write — safe; trigger is cache invalidation — safe. **`bff_get_upload_receipt_config` was affected**: it read `enabled` directly and defaulted to `false`, so Munasoul would see receipts as disabled despite the feature being on-by-registry-default. Routed the `enabled` check through `fn_merchant_feature_resolve` (preserving the direct config-field reads for `entry_mode`/`auto_approve` which need their own defaults). Verified Munasoul now resolves `enabled=true` with operational config intact (`entry_mode=total_only`, `auto_approve=false`). The three functions built earlier this thread (`bff_get_admin_menu`, `bff_get_entity_history`, `bff_get_feature_entitlements`) already route through the resolver — confirmed unaffected. `fn_get_effective_earn_channels` already safe. Requirement doc: `requirements/Platform_Plan_Feature_Registry.md`.
- **Platform / Plan / Feature scoping (history menu via resolver)** — `bff_get_user_history_menu` was reading `feature_config` rows directly and defaulting missing entries to `enabled=false`, so any feature that was on-by-registry-default (i.e. had no explicit row) showed as off. Symptom: Munasoul (`loyalty_essentials`, expressed as off-rows only) saw only the Purchases tab; reward/tier/currency/campaigns were hidden despite resolving enabled. Rewrote the function to delegate eligibility entirely to `fn_merchant_feature_resolve` for both top-level tabs and dynamic sub-filters (campaign types, currency types). Single source of truth: now inherits grant-all, default-on, platform layer, and legacy naming from the resolver instead of re-implementing the merge. Also normalized the Tier tab's `feature_group` from legacy `'loyalty'` to registry `'tier'`. Verified: Munasoul sees Rewards/Tier/Currency/Receipts/Campaigns(top) + Missions/Points/Tickets(subs); referral/checkin/activity hidden; enterprise unchanged. Requirement doc: `requirements/Platform_Plan_Feature_Registry.md`.
- **Platform / Plan / Feature scoping (resolver default-on + bespoke plan)** — Patched `fn_merchant_feature_resolve` so a plan expressed purely as off-rows + registry defaults works without explicit on-rows. Two fixes: (1) feature groups now default to `enabled=true` (groups are containers; their declared `default_enabled` is NULL, previously causing group-level checks to return false); (2) the legacy path (`p_platform IS NULL`) now consults the registry default too, so live BFFs (`bff_get_user_history_menu`, `bff_get_upload_receipt_config`, etc.) inherit on-by-default for unseeded nodes. Leaf nodes continue to respect their declared `default_enabled` (e.g. `storefront/shopify_widget` stays false). Audited all 54 nodes: 10 had no row anywhere; all 10 correctly inherit their registry default after the patch; for enterprise merchants the change is a no-op (grant-all already forced everything true). Created bespoke plan `loyalty_essentials` (core platform, `grant_all=false`) with 27 off-rows — disables `forms`/`store`/`persona`/`member_intelligence`/`lifecycle`/`workspace` groups entirely, plus `campaign/{referral,checkin,activity}` and `earn_channel/{product_code_scan,marketplace_code}` leaves; leaves `reward`/`tier`/`currency`/`campaign/mission`/`earn_channel/upload_receipt` on via registry defaults. **Assigned Munasoul** (`32790d51-aab4-44de-a74c-19f790120ac7`) to it (both `merchant_master.plan_id` and `merchant_plan_assignment` core). Verified: every requested feature resolves enabled, every excluded feature disabled, `shopify_widget` correctly false. **Marketing automation and customer service modules have zero seeded features** — they're inert for every merchant, no off-row needed. Plan: `.cursor/plans/platform-plan-shopify-tiers-and-enterprise-grant-all.md`. Requirement doc: `requirements/Platform_Plan_Feature_Registry.md`.
- **Platform / Plan / Feature scoping (admin menu + entity-history gating)** — Extended plan gating into the admin nav and the user-history dispatcher. `admin_menu_config` gained nullable `feature_group` + `feature_key` (text) columns mapping menu items to a registry node; 30 items mapped to their gating feature, 16 left NULL (always-on infrastructure: AMP, analytics, assets, settings, roles, team, translation, operations, integrations). `bff_get_admin_menu` now ANDs a plan-feature check into every menu/submenu filter: an item renders only if the merchant's plan has the feature enabled OR `grant_all_features` is true OR the item has no mapping — existing role-permission filtering is preserved. `bff_get_entity_history` (the dispatcher behind the user-history tabs) gained a defensive entitlement guard: if the requested entity maps to a feature the merchant's plan doesn't have, it returns empty (closes the loop so BE rejects out-of-plan tab queries even if the FE requests them). Both are grant-all aware (inert for enterprise today). Verified: 30/30 gated items resolve enabled for enterprise. Regenerated `requirements/REGISTRY_SUPABASE.md` (1822 lines; new `Platform Plan Registry` domain pattern persisted to `scripts/regen_registry_supabase.sql`). Requirement doc: `requirements/Platform_Plan_Feature_Registry.md`.
- **Platform / Plan / Feature scoping (grant-all + tier seed + read-shaping)** — Added `merchant_plan.grant_all_features boolean NOT NULL DEFAULT false` and `merchant_plan.order_quota integer` (display-only, billing stays external). Reworked `fn_merchant_feature_resolve` with a grant-all short-circuit: when the resolved plan has `grant_all_features`, every node returns `enabled=true` + `limits={}` (unlimited) **while `config` still merges normally** (registry→platform→plan→merchant) so operational config is preserved (verified: `currency/points` keeps `expiry_mode/award_delay_type`). Set `enterprise.grant_all_features=true`, replacing its ~21 explicit `enabled=true` rows (left inert, not deleted — config rows still feed the merge). Seeded the **shopify** tier plans per the pricing matrix (`shopify_free/essential/standard/growth`): `order_quota` 200/500/2500/7500, `max_active_rewards` 5/10/25/100, and `enabled=false` deltas for higher-tier features (`currency/multiplier_by_product`+`expiry`, `reward/tier_reward_eligibility`, `tier/per_tier_earn_rate`+`per_tier_burn_rate`+`conditions_by_spend_orders`, `lifecycle/signup`+`birthday`+`anniversary`+`tier_change`). **Granted all 115 merchants full features**: set `merchant_master.plan_id=enterprise` for the 7 non-enterprise merchants and inserted `merchant_plan_assignment` (core, enterprise) for all 115. New BFF `bff_get_feature_entitlements(p_platform)` (uses `get_current_merchant_id()`, returns plan info + flat merged feature map for FE gating; verified 54/54 nodes resolve for enterprise). Made `bff_get_user_history_menu` grant-all aware (it already gated tabs/subtabs by plan; now ORs the flag into every eligibility check). DEFERRED: read-shaping of `bff_get_widget_settings` / `bff_get_user_missions`. Plan: `.cursor/plans/platform-plan-shopify-tiers-and-enterprise-grant-all.md`. Requirement doc: `requirements/Platform_Plan_Feature_Registry.md`.

## 2026-06-24

- **Platform / Plan / Feature scoping (taxonomy + resolver + seed)** — Completed the deferred items on the foundation below. Seeded `feature_registry` taxonomy (3 modules, 13 loyalty groups, 34 features, 7 sub-features per architecture §2.5/§6.1). Rewrote the resolver to platform-aware **field-merge**: `fn_merchant_feature_config`/`_enabled` gained `p_platform text DEFAULT NULL` (NULL = legacy plan-via-`merchant_master` behavior, no registry default — proven byte-identical to the old row-wins over all 49 live rows; impact analysis found the only 3 plan+merchant overlaps had zero divergent keys); added workhorse `fn_merchant_feature_resolve(...)→{enabled,limits,config}` and helper `fn_merchant_platform_plan(merchant,platform)`. Platform mode merges registry default → platform → plan (via `merchant_plan_assignment`) → merchant. Seeded plans: `core_{free,starter,growth,premium}` (feature-tiered) + `shopify_{free,essential,standard,growth}` (flat on features, volume-only). Seeded core plan deltas (`max_active_rewards` 10/25/100; `team_seats` 5/unlimited) and Shopify platform rows (hide `forms`/`earn_factor`/`multiplier`/`persona`/`user_type`, show `shopify_widget`, force lifecycle `currency=points`). Replaced `feature_config_scope_check` with `num_nonnulls(plan_id,merchant_id,platform_id)=1` and added `feature_config_platform_unique`. Transition note: `feature_config` still keyed by `feature_group`/`feature_key` text (no `feature_id` backfill yet); platform rows use registry group names (`earn`/`persona`/etc.), legacy core rows keep old names (`loyalty`). Requirement doc: `requirements/Platform_Plan_Feature_Registry.md`. Design: `.cursor/plans/platform-plan-feature-registry-architecture.md`.
- **Platform / Plan / Feature scoping** — Added the structural foundation for platform×plan feature scoping (Shopify). New `platform` catalog (seeded `core`, `shopify`); new single-table `feature_registry` (adjacency list: `node_type` ∈ module/feature_group/feature/sub_feature + `parent_id`, with `is_toggleable`/`limit_schema`/`config_schema`/`default_*`, parent-type integrity trigger); `merchant_plan.platform_id` added and backfilled (`enterprise`/`loyalty_only` → `core`); new `merchant_plan_assignment` (one plan per platform per merchant, merchant-isolated, platform-match trigger); `feature_config` gained additive `platform_id` + `feature_id` (FK → `feature_registry`) + `limits` jsonb. All additive — existing `fn_merchant_feature_config`/`_enabled` resolver unchanged and still reads `feature_group`/`feature_key`. DEFERRED (separate thread): registry taxonomy seed, platform-aware field-merge resolver, plan seeding (blocked on Shopify vs standalone plan-name reconciliation), feature_config delta seeding. Design: `.cursor/plans/platform-plan-feature-registry-architecture.md`. REGISTRY_SUPABASE.md regeneration pending.

## 2026-06-23

- **Tier** — Tier deletion no longer blocked by soft-deleted users. `user_accounts.tier_id` FK changed from `ON DELETE RESTRICT` to `ON DELETE SET NULL` (matches `tier_progress`/`tier_change_ledger`), and `admin_delete_tier` pre-check now filters `deleted_at IS NULL` so only active users block deletion. Pre-FK cleanup nulled ~1.45M orphan `user_accounts.tier_id` rows referencing non-existent `tier_master` IDs (concentrated in 7 merchants; likely MongoDB-migration artifacts) that prevented the FK from validating. Signature unchanged. See `requirements/Tier.md`.
- **Currency** — Fixed "Save failed" in Basic Points Config when removing all excluded products from a single (non per-tier) earn rate. `bff_upsert_basic_currency_config` deleted the `earn_conditions_group` inline before the earn rule was unlinked, tripping the `earn_factor.earn_conditions_group_id` `ON DELETE RESTRICT` FK; the empty-products branch now only nulls the link and defers group deletion to the existing end-of-function orphan cleanup. Signature unchanged. See `requirements/Currency.md`.
- **Purchase / Currency** — Fixed excluded-product earn rule awarding zero points on all line items. `api_create_purchase` now `LEFT JOIN LATERAL`s `product_sku_master` (was inner join) so unresolvable Shopify variants persist in the items payload with NULL `sku_id`/`sku_code` instead of being silently dropped; `evaluate_earn_conditions_core` replaces per-query `gen_random_uuid()` item correlation with `WITH ORDINALITY` position keys, fixing exclude-only and include+exclude combination paths on the marketplace order flow (`get_eligible_earn_factors_core`). Function signatures unchanged. See `requirements/Purchase_Transaction.md`.
- **Currency** — `get_user_summary()` now returns `points_expiring_on_next_date` (sum of `wallet_ledger.deductible_balance` expiring on `points_expiry_date`); refactored soonest-expiry-date lookup into a shared variable so the new field reuses one resolved date instead of a third subquery. Signature unchanged.

## 2026-06-22

- **Currency** — `inngest-currency-serve` v72: `currencyAward` filters doomed ticket rows (null `target_entity_id`) and runs award steps in parallel with SQLSTATE-based permanent/transient error handling, fixing a ~2.5 min points-bonus delay caused by a failing sibling ticket award; deactivated misconfigured `qa-install` ticket earn factor `393aa5df`. See `docs/CRITICAL_BUG_FIXES.md` (2026-06-22 head-of-line blocking).

## 2026-06-18

- **Currency / Shopify** — Shopify store credit ticket earns now enqueue `shopify-issue-store-credit` via `trg_enqueue_shopify_store_credit_issue` on `wallet_ledger` (`credit_platform = 'shopify'` only); edge fn calls GraphQL `storeCreditAccountCredit` and stamps `wallet_ledger.metadata`. See `docs/PRODUCT_FEATURE_CATALOG.md` (ticket credit types).
- **Tier** — `ensure_tier_progress` now writes `tier_progress.upgrade_progress_percent = NULL` (was `0`) for top-tier members with no next tier, consistent with the sibling `upgrade_*`/`next_tier_id` NULLs; backfilled 2,503 existing top-tier rows. Frontend should treat `next_tier_id IS NULL` as the max-tier state. See `requirements/Tier.md`.

# Project Changelog

Running summary of changes to the CX Platform (Loyalty + CS).
Each entry is a one-line summary that **links to the domain MD where full detail lives**.
This file is for fast recall — never duplicate detail here.

## Conventions

- **Newest at top.** Group by date (`## YYYY-MM-DD`), most recent date first.
- **One bullet per change.** Format: `**<Domain>** — <what changed in 1 sentence>. See \`requirements/<Doc>.md\` §<Section>.`
- **Always link the domain MD.** If no section anchor fits, link the file alone.
- **In scope:** schema changes, new/modified functions, new edge functions / Inngest / Render endpoints / MCP tools, behavior-affecting bug fixes, feature behavior changes.
- **Out of scope:** pure refactors with no behavior change, formatting, doc-only edits, dependency bumps that don't change behavior.
- **CS changes** use the `CS` prefix in the domain label and link `CS_*.md`.

## 2026-06-16

- **Reward** — `api_get_rewards_full_cached` now excludes rewards past their `redeem_window_end` (build-time `WHERE`/`EXISTS` predicate across data, total count, groups, and category counts); contract and merchant-scoped 300s cache unchanged. Per-user eligibility (tier/persona/tags/birthmonth) intentionally left to the frontend + redemption gate. See `requirements/Reward.md`.
- **Customer Import** — Event registration import sets `user_accounts.acquisition_source` to the event code on user create/update (when NULL). See `requirements/Customer_Import.md` §Event registration import — acquisition source.
- **Customer Import** — Import address fix: `fn_resolve_user_address_for_import` + `fn_upsert_user_address_from_import`; `bulk_upsert_customers_from_import` writes Thai geo as codes in `city`/`district`/`subdistrict`; backfill phases A/A2/B/C on `user_address`. See `requirements/Customer_Import.md` §User address on import.
- **Multi-Channel Product Reference** — new `product_sku_external_ref` bridge maps marketplace variant ids → CRM SKUs so Shopify (and future) orders earn without catalog sync; added `fn_resolve_product_sku_id`, `fn_marketplace_order_items_payload`, `bff_link_product_external_ref`; `api_create_purchase` and both claim functions now resolve items via the bridge (POS `sku_code` path unchanged). One `shopify-webhooks` normalizer one-liner deferred to CLI. See `requirements/Multi_Channel_Product_Reference.md`.

## 2026-06-15

- **Shopify / Merchant Plans** — Shopify merchant creation now assigns the Enterprise plan by default and existing active Shopify merchants missing `plan_id` were backfilled. See `docs/bff_user_history_rpc.md` §Troubleshooting.
- **Currency** — Delayed-day awards now support timezone-aware `currency_award_time` for `rolling_days`/legacy `scheduled`, and `bff_upsert_currency_config` preserves award time on partial saves. See `requirements/Currency.md` §Merchant Configuration for Delayed Awards.
- **Forms & User Profile** — Added `user_accounts.id_document_type` and backfilled it from existing `id_card` formats (`thai_national_id`, `passport`, `other`, or `NULL` for blanks). See `requirements/Forms.md` §Default Fields System.

## 2026-06-13

- **Store Credit** — internal frontline store credit top-up/spend: new `store_credit_promo` table, helpers, and mode-based BFFs `bff_store_credit_promo` + `bff_store_credit`; permissions `frontline_store_credit` and `store_credit_promo`. See `requirements/Store_Credit.md`.
- **Store Credit** — FE gap fix: spend/event-order returns scan `code` (`SC-XXXXXXXX`); `get` returns normalized `history[]` with reversal metadata; permission checks use `read`/`create`/`update` (not `view`); role backfill from `frontline_burn_points_discount`. Helpers `fn_generate_store_credit_spend_code`, `fn_store_credit_wallet_history`.

## 2026-06-12

- **Custom Webhooks** — `custom_webhook_drpong_zort_pos` now routes by channel: POS → Old CRM receipts API direct; BigCommerce `UPDATEORDER` Waiting/Success → Pipedream `eot6isanrrx84y3`; marketplace filtered. See `requirements/Custom_Webhooks.md` §Dr.PONG channel router.

## 2026-06-11

- **User / Acquisition** — Added `acquisition_source_master`, wired optional `acquisition_source` through `chokepoint_post_user_event` (first-touch on create; set-if-null on update), registration callers (`bff-auth-complete`, `bff_save_user_profile`, `bff_admin_create_member`, `api_create_or_update_user`, `bulk_upsert_customers_from_import`). See `user_accounts.acquisition_source`.

## 2026-06-04

- **Marketplace** — Fixed Confluent Kafka REST v3 produce payload in marketplace webhooks (`records[]` → single `{key,value}` per POST); Hookdeck producer API key required for REST auth (Render consumer key differs). See `.cursor/deploy/marketplace-webhook/README.md`.
- **Marketplace** — Deployed `webhook-marketplace-tiktok`, `webhook-marketplace-shopee`, `webhook-marketplace-lazada` Edge Functions to replace Hookdeck filter/transform/Kafka ingress (Lazada mapped from `seller_id` + `trade_order_id`). See `.cursor/deploy/marketplace-webhook/README.md`.
- **Event architecture** — Decommissioned Debezium CDC: dropped `crm_cdc_publication`, `crm_cdc_slot` (~321 GB WAL retention cleared), unscheduled `cdc-heartbeat` and `wal-lag-alert` crons after Confluent connector removal. Chokepoint→outbox→Kafka remains sole path. See `requirements/REGISTRY_RENDER.md` §Operational pg_cron jobs.

## 2026-05-28

- **Tier** — Fixed admin tier create defaulting every new tier to `ranking = 0` (blocked upgrades when all tiers shared the same rank). `bff_upsert_tier_with_conditions` now auto-appends `MAX(ranking)+1` when `ranking` is omitted on create, insert-at-slot shifts peers when an explicit ranking is sent, and update-with-ranking reorders intervening tiers; response includes `data.ranking`. `get_tier_conditions_by_type('new')` returns suggested next ranking. One-time repair renumbered merchants stuck with all-zero rankings. See `requirements/domains/tier.md` §Tier Ranking Contract and `requirements/Tier.md` §Ranking System.
- **Syngenta Event Registration Import** — Aligned with customer import: multi-step JSON RPCs (`bff_admin_start_event_registration_import` → `bff_admin_stage_user_import_rows` → `bff_admin_run_user_import` → `bff_admin_kickoff_event_registration_import` → poll progress). Removed monolithic `api_upload_event_registration_import_csv`. See `supabase/migrations/20260528160000_event_registration_import_customer_import_pattern.sql`.

## 2026-05-26

- **AMP Workflows** — Fixed rule-based workflow LINE/SMS/email delivery: `inngest-amp-serve` now authenticates to messaging-service via `get_messaging_auth_key()` (vault) instead of the edge env service-role key, matching CS callers. See `docs/AMP_LIFECYCLE_AUTOMATION.md` §Outbound messaging and `requirements/Notification_Service.md` §Shared delivery service.

## 2026-05-25

- **Tier / Frontline member create** — dropped erroneous FK `tier_change_ledger.created_by → user_accounts(id)` that blocked admin-initiated member create (Frontline `bff_admin_create_member` → initial entry tier → `chokepoint_post_tier_change` with `created_by = admin_users.id`). `user_id` already identifies the member; `created_by` is the actor (admin, member, or NULL). See `requirements/Tier.md` §Database Reference.
- **Rewards** — Fixed Front Line "Redeemed by" showing the admin who looked up a code instead of the member who self-redeemed; `api_get_redemption` now ignores lookup audit rows and falls back to ledger member; `redeem_reward_with_points` always writes redeem audit logs for member app redemptions. See `docs/CRITICAL_BUG_FIXES.md` §2026-05-25.

## 2026-05-21

- **Notification Service (Flex v2 + deep links)** — upgraded all 18 system LINE Flex templates (`notification_flex_v2*` migrations): hero image block, footer URI button, richer default body rows (e.g. redemption code on used/cancelled). `crm-event-processors` `2d31c17` enriches `detail_url` via loyalty-app contract (`https://{merchant_code}.rocket-loyalty.app/?page=…`) and `hero_image_url` from reward/tier/merchant branding before render. See `requirements/REGISTRY_RENDER.md` §NotificationConsumer.

## 2026-05-16

- **Notification Service v1 (DB layer + Render consumer)** — net-new feature: per-merchant configurable LINE Flex push notifications driven off the chokepoint event pipeline. **DB layer (live in `wkevmsedchftztoolkmi`):** four migrations applied — `notification_service_tables_v1` adds (1) `notification_event_catalog` (system-owned, no RLS, seed via `notification_service_catalog_seed_v1` — 16 rows: 11 default-ON `purchase.{completed,refunded}` + `currency.{earned,expired}` + `tier.{upgrade,initial}` + `signup.signup` + `redemption.{issued,used,entitlement_used,entitlement_expired}`, 5 default-OFF `currency.burned` + `purchase.cancelled` + `tier.downgrade` + `redemption.{cancelled,package_granted}`); (2) `merchant_notification_settings` (per-(merchant, event_key, sub_event) enabled-toggle + selected_fields jsonb override, RLS + merchant_isolation + set_updated_at trigger); (3) `notification_template` (flex_template jsonb with `${field}` placeholders + `_field` markers for picker-driven row visibility — system default = `merchant_id IS NULL`, per-merchant override = `merchant_id IS NOT NULL`, partial unique indexes on both; the 16 system defaults also seeded in `notification_service_catalog_seed_v1`); (4) `notification_log` (append-only outcome log — status `sent|failed|skipped` + skip_reason + flex_payload + line_message_response, UNIQUE `(merchant_id, event_key, sub_event, source_event_id)` is the idempotency anchor against Kafka redelivery double-sends). `notification_service_functions_v1` adds `bff_get_notification_settings()` (admin grid) + `bff_upsert_notification_settings(p_rows jsonb)` (validated mass upsert, rejects unknown events + selected_fields not in catalog allowlist) + `fn_resolve_notification_for_event(p_merchant_id, p_event_key, p_sub_event, p_user_id, p_source_event_id) → jsonb` (single-roundtrip resolver returning `should_send: false + skip_reason ∈ {INVALID_INPUT, NO_CATALOG, DISABLED, NO_USER, USER_NOT_FOUND, NO_LINE_ID, USER_CHANNEL_OFF, ALREADY_SENT, NO_TEMPLATE, NO_MERCHANT_TOKEN, ERROR}` or `should_send: true + line_id + channel_access_token + flex_template + selected_fields + source_topic`). `notification_service_resolver_lockdown_v1` REVOKEs EXECUTE on the resolver from PUBLIC/anon/authenticated and GRANTs only to service_role — critical because the verdict embeds a merchant's LINE Messaging API push token (channel_access_token from `merchant_credentials.credentials->>'messaging_channel_access_token'`); exposing the resolver via PostgREST would have leaked merchant push tokens to any holder of the anon key. All three RPCs smoke-tested against `wkevmsedchftztoolkmi`: resolver returned `should_send: true` for a real merchant+user with `line_id + channel_line=true + line_messaging` token, and correct skip reasons for the DISABLED / NO_CATALOG / NO_USER / NO_LINE_ID / USER_CHANNEL_OFF / NO_MERCHANT_TOKEN paths. **Consumer-side (Render `crm-event-processors` branch `feature/notification-consumer`, base commit `cd00f57`):** added `NotificationConsumer` registered in `src/index.ts` startup + shutdown sequences with own gating flag `NOTIFICATION_CONSUMER_ENABLED` (separate from `CHOKEPOINT_EVENTS_ENABLED` so notifications can be flipped independently of the rest of the chokepoint pipeline). `src/config.ts` adds `topics.redemptionEvents='crm.events.redemption'` + `consumer.notificationConcurrency` + `flags.notificationConsumerEnabled` + `notification.{linePushUrl (default https://api.line.me/v2/bot/message/push), messagingServiceUrl (optional proxy), messagingServiceApiKey}`. `src/clients/supabase.ts` appends `resolveNotificationForEvent`, `recordNotificationLog` (idempotent upsert on the unique tuple — duplicate logs on Kafka retry are silently swallowed), `fetchRewardName` + `fetchTierName` (in-memory 5-min TTL cache for enrichment lookups against `reward_master.name` / `tier_master.tier_name` — the resolver returns `reward_id`/`to_tier_id` only, names enriched in the consumer so the catalog can offer `reward_name`/`tier_name` as picker fields), and `chokepointDisableUserLineChannel` (PERFORM `chokepoint_post_user_event(p_event_type:='update', p_changes:='{channel_line: false}', p_actor:='{source: notification-consumer, reason: line_user_blocked_bot}')` — uses the user chokepoint so AMP + audit observers see the flip, NOT a direct UPDATE on user_accounts which would bypass the chokepoint pipeline). New `src/clients/line.ts` adds `pushLineFlex(channelAccessToken, lineUserId, flex, altText)` returning `{status: 'sent' | 'user_blocked' | 'rate_limited' | 'failed', httpStatus, responseBody, error?}` — routes through `messagingServiceUrl` when set (with `x-messaging-api-key` header) else direct to LINE, 5xx throws (Kafka redelivers, resolver's `ALREADY_SENT` backstop guards double-send), 429 returns `rate_limited`, 4xx with body containing "block" or "not a friend" returns `user_blocked`. New `src/utils/flex-template.ts` adds `renderFlexFromTemplate({template, payload, selectedFields, enrichments})` — recursive walker that drops any object whose `_field` marker is not in `selectedFields`, substitutes `${field_name}` placeholders from `{...payload, ...enrichments}`, and drops the entire `_field`-marked container if any descendant placeholder still goes unresolved after substitution (so a "Reward: ${reward_name}" row disappears entirely when enrichment fetched null, instead of shipping a half-rendered text element that LINE would 400 on); plus `defaultAltText(eventKey, subEvent)` returning a per-event human-readable altText (required by LINE for push notifications when the device can't render the bubble). New `src/consumers/notification-consumer.ts` subscribes to `[purchaseEvents, walletEvents, tierChangeEvents, userEvents, redemptionEvents]` when `NOTIFICATION_CONSUMER_ENABLED=true`, eachBatch with `p-limit(notificationConcurrency)`, per-message hardcoded event-mapping: `crm.events.purchase + event∈{completed,cancelled,refunded}` → `purchase.<event>` source_event_id=purchase_id; `crm.events.wallet + transaction_type='earn' + component='base'` → `currency.earned`, `transaction_type='burn'` → `currency.burned`, `transaction_type='expire'` → `currency.expired` (source_event_id=wallet_ledger_id); `crm.events.tier_change + change_type∈{upgrade,initial,downgrade}` → `tier.<change_type>` source_event_id=tier_change_ledger_id; `crm.events.user + event='create' + skip_cdc=false` → `signup.signup` source_event_id=user_id; `crm.events.redemption + event∈{issued,used,cancelled,entitlement_used,entitlement_expired}` → `redemption.<event>`, `event='package_entitlement_created'` → `redemption.package_granted` (source_event_id=redemption_id), `event∈{unmarked_used,entitlement_use_reversed,entitlement_total_adjusted}` dropped at consumer (admin-internal verbs, not in catalog). Redis dedup key `notification_dedup:{merchant}:{event_key}:{sub_event}:{source_event_id}` TTL 86_400 with `SET … EX … NX` atomic acquire (released on 5xx throw so Kafka redelivery can re-attempt). On `should_send=true`: enriches reward_name/tier_name only when those keys are in `selected_fields`, renders flex template, pushes to LINE, inserts notification_log row. On `should_send=false`: logs to notification_log with status='skipped' + skip_reason EXCEPT for the high-volume "expected silence" reasons `DISABLED|NO_CATALOG|ALREADY_SENT` which are dedup-absorbed but not logged (keeps the audit log signal-to-noise high). On 4xx "user blocked": flips `channel_line=false` via the user chokepoint helper so future events for this user short-circuit at the resolver's USER_CHANNEL_OFF guard. **TypeScript build clean** (`npm run build` passes under `strict: true`). Flex renderer smoke-tested against the live system template for `redemption.issued` covering 3 cases (all fields present → all rows render; selected reward_name with missing enrichment → reward_name row dropped + other rows survive; nothing selected → only static header renders). **Not yet deployed to Render** — branch is committed but not pushed/redeployed. Activation sequence when ready: (1) push branch + open PR, (2) set Render env vars `NOTIFICATION_CONCURRENCY=5` + `LINE_PUSH_URL=https://api.line.me/v2/bot/message/push` (default works; only set `MESSAGING_SERVICE_URL`/`MESSAGING_SERVICE_API_KEY` if proxying), (3) deploy with `NOTIFICATION_CONSUMER_ENABLED=false`, (4) configure a test merchant's `messaging_channel_access_token` + a test user's `line_id` + `channel_line=true`, (5) flip `NOTIFICATION_CONSUMER_ENABLED=true` and watch `notification_log` fill in. **Build-time scope NOT covered (deferred):** (a) admin UI for the picker (`bff_get_notification_settings` + `bff_upsert_notification_settings` are the BFFs, but the React UI consuming them is a separate frontend workstream); (b) merchant-override template editor — the schema supports it (`notification_template.merchant_id IS NOT NULL` row with same `(event_key, sub_event)` beats the system default via NULLS LAST in the resolver) but no BFF surface exists yet for merchants to author/edit overrides; (c) per-channel fan-out (email/SMS/push) — current resolver hardcodes the LINE-only happy path (`channel_line=true` + `line_id IS NOT NULL` + LINE messaging token); adding email/SMS would extend `notification_event_catalog` with a `channel` column and split the resolver per-channel. See `requirements/REGISTRY_SUPABASE.md` §Notification Service and `requirements/REGISTRY_RENDER.md`.
- **Chokepoint → Kafka pipeline (Phase R-2 — consumer-side wiring for `crm.events.redemption`)** — extended `AmpConsumer` and `OutcomeAttributionConsumer` in `crm-event-processors/` to subscribe to the new redemption topic when `CHOKEPOINT_EVENTS_ENABLED=true`. `config.ts` adds `topics.redemptionEvents='crm.events.redemption'` with the partition-key comment block updated to note redemption's `COALESCE(user_id, redemption_id)` fallback for anonymous (parking-style) rows. `AmpConsumer` extends `ChokepointEvent` with the redemption-specific fields (`redemption_id`/`redemption_code`/`reward_id`/`qty`/`used_qty`/`used_status`/`cancelled`/`fulfillment_status`/`package_assignment_id`/`qty_change`), widens existing `source_id?: string` → `string | null` (the redemption payload sets `source_id` from a nullable column), adds `redemptionEvents` to `isChokepointEventTopic()`, adds the topic to the `subscribe()` list, and adds a dispatch branch in `handleChokepointEventMessage()` that fires for **all 9 event verbs** with `trigger_table='reward_redemptions_ledger'` + `trigger_operation='INSERT'` + `record_data` carrying the event verb + redemption shape. Dedup key `amp:reward_redemptions_ledger:${event}:${redemption_id}` includes the verb so each lifecycle step (issued → used → cancelled) on the same row dispatches as a distinct AMP trigger; AMP workflow rules filter on `record_data.event` to select the verb they care about. `OutcomeAttributionConsumer` extends `ChokepointEvent` with `redemption_id`/`reward_id`/`qty`/`used_qty`/`qty_change`, adds the topic, and classifies **4 of 9** verbs as attributable outcomes (the rest are operational): `issued` → `reward_redeemed`, `used`+`entitlement_used` → `reward_used`, `cancelled` → `reward_cancelled`. Dedup key for redemption deviates from the standard `oattr:<eventType>:<sourceId>` to `oattr:${eventType}:${redemption_id}:${event}:${used_qty ?? 0}` so multi-use entitlement consumption (multiple `entitlement_used` events on the same redemption_id with different `used_qty` values) doesn't collapse to a single dedup entry within the 600s window. Customers opt into redemption attribution by adding rows with `event_type IN ('reward_redeemed','reward_used','reward_cancelled')` to `amp_agent_outcome`; until configured, redemption events are received and dedup-checked but produce no attribution rows (no behavior change). Typecheck + full `tsc` build pass; no ESLint configured. Phase R-2 is **not yet active end-to-end** — code is committed but: (a) Confluent topic `crm.events.redemption` is not yet provisioned on `pkc-ox31np.ap-southeast-7.aws.confluent.cloud` (6 partitions, 7d retention, gzip, delete — `crm.events.*` ACL prefix already grants WRITE+DESCRIBE to the `chokepoint-publisher` API key, no new credentials), and (b) `crm-event-processors` is not yet redeployed on Render. Outbox is currently accumulating errored rows (2 as of the change) with `last_error='This server does not host this topic-partition'` from the worker because the topic doesn't exist. Once topic is provisioned + service redeployed, the backlog drains and AMP starts dispatching on redemption lifecycle events. See `.cursor/plans/chokepoint-redemption-event.md` §R-2 and `requirements/REGISTRY_RENDER.md`.
- **Chokepoint → Kafka pipeline (R-1 cleanup — inline redemption payload helper)** — dropped `fn_build_redemption_event_payload(reward_redemptions_ledger, text, jsonb) → jsonb` and inlined its `jsonb_strip_nulls(jsonb_build_object(...)) || COALESCE(v_extras, '{}')` body into `trigger_emit_redemption_event()`. The helper had exactly one caller (the trigger) and diverged from the precedent set by the other 4 chokepoints (`chokepoint_post_purchase_event` / `_wallet_transaction` / `_tier_change` / `_user_event`), all of which build their payload inline before calling `fn_chokepoint_emit_event`. Pure refactor — payload schema, field set, partition key (`COALESCE(user_id::text, id::text)`), source string (`'trigger_emit_redemption_event'`), and skip-emit GUC behavior are byte-identical to the helper-based version; only the function-boundary indirection is removed. Validated via in-transaction smoke (`used_status false→true` emitted `event='used'` with 17 keys, `true→false` emitted `event='unmarked_used'` with 16 keys reflecting `jsonb_strip_nulls` dropping the now-NULL `used_at`) before applying. Outbox was empty for `crm.events.redemption` at the time of cut (0 rows lifetime), so no in-flight payload-shape risk. Migration `chokepoint_redemption_inline_payload_v1`. See `.cursor/plans/chokepoint-redemption-event.md` §R-1.
- **Chokepoint → Kafka pipeline (Phase R-1 — redemption emit, DB layer)** — added a 6th event topic `crm.events.redemption` to the chokepoint pipeline, but architected as a **table-level chokepoint via AFTER trigger** instead of a function-level `chokepoint_post_redemption_event(...)`. Rationale: unlike purchase/wallet/tier_change/user, redemption writes (a) hit a single table (`reward_redemptions_ledger` + audit-only `redemption_usage_log`) with no fan-out to consolidate, and (b) every lifecycle verb is column-diff-derivable from `(used_status, used_qty, qty, cancelled, source_type)`, so a function-level chokepoint would only shift the discriminator from "function name" to a `p_event` field without reducing complexity. The strong precedent on this same table is `validate_reward_store_stock_on_use`, an existing AFTER trigger covering the same column set. Migration `chokepoint_redemption_emit_trigger_v1` adds: (1) `fn_build_redemption_event_payload(reward_redemptions_ledger, text, jsonb) → jsonb` STABLE / SECURITY INVOKER — single source of truth for the payload schema (event/merchant_id/user_id/redemption_id/redemption_code/reward_id/qty/used_qty/used_at/used_store_id/redeemed_store_id/fulfillment_status/use_expire_date/redeemed_at/redeemed_status/used_status/cancelled/cancelled_at/cancelled_reason/cancelled_by/source_type/source_id/package_assignment_id/promo_code/promo_code_id/external_ref_id/points_deducted/occurred_at/source); (2) `trigger_emit_redemption_event()` SECURITY DEFINER — column-diff classifier producing `issued` / `package_entitlement_created` / `used` / `unmarked_used` / `entitlement_used` / `entitlement_use_reversed` / `entitlement_total_adjusted` / `entitlement_expired` / `cancelled`, partition key `COALESCE(user_id::text, id::text)`, wrapped in `EXCEPTION WHEN OTHERS → RAISE NOTICE` so a misbehaving outbox can never block a redemption write; (3) two triggers — `emit_redemption_event_after_insert` AFTER INSERT and `emit_redemption_event_after_update` AFTER UPDATE OF used_status, used_qty, qty, cancelled. Backfill hatch: trigger reads session GUC `redemption.skip_emit`; both `bulk_import_redemptions_chunk` overloads were CREATE OR REPLACEd to set this GUC to `'true'` at the top of their bodies (only when caller hadn't already set it explicitly), so historical imports don't replay AMP/OutcomeAttribution side effects against today's flags — caller can opt back in for live emits via `PERFORM set_config('redemption.skip_emit','false',true)` before calling. Function bodies of the other 12 redemption-touching paths (`redeem_reward_with_points`, `admin_redeem_and_use`, `fn_create_package_entitlements`, `api_mark_redemption_used`, `admin_mark_redemptions_used`, `api_use_entitlement` → `fn_use_entitlement`, `api_cancel_redemption`, `admin_unmark_redemption_used`, `bff_admin_adjust_entitlement` → `fn_adjust_entitlement_total`, `fn_reverse_entitlement_use`, `fn_expire_package_entitlements`) are **untouched** — they write to `reward_redemptions_ledger` as they always have; the trigger picks them all up automatically and inherits future write paths too. Smoke validated in transaction with synthetic redemptions covering all 9 event branches (issued, package_entitlement_created, used, unmarked_used, entitlement_used×2, entitlement_use_reversed, entitlement_total_adjusted, entitlement_expired, cancelled, plus skip_emit honored) before applying; live trigger re-verified post-migration (3-of-3 issued→used→cancelled emits with correct `partition_key=user_id` and `source='trigger_emit_redemption_event'`). **Phase R-2 (Confluent topic provisioning + AMP / OutcomeAttribution dual-read in `crm-event-processors`) is the next phase and must land before significant organic redemption traffic to avoid `last_error`/`publish_attempts` accumulation in `chokepoint_event_outbox` for the new topic** (the `crm.events.*` ACL prefix already grants WRITE+DESCRIBE to the existing `chokepoint-publisher` API key; topic config matches the other 5 — 6 partitions, 7d retention, gzip, delete cleanup). No CDC decommission applies here — `reward_redemptions_ledger` was never in `crm_cdc_publication`, so this is **net-new downstream signal** rather than a cut-over (no 24h soak gate, no Step 11 equivalent). See `.cursor/plans/chokepoint-redemption-event.md` and `requirements/Reward.md` §Redemption.

## 2026-05-15

- **Currency / `inngest-currency-serve` loop hardening (v54)** — fixed the latent cascade-abort bug where one bad `earn_factor` row threw inside `step.run("award-${i}", …)` and aborted every sibling award in the same Inngest run (and on every Inngest retry, since the bad row was always at the same `i`). New CRM had this fire in production via `earn_factor b3006117-deb1-448a-914b-6174db8883be` (rate / ticket / 1000) which was the first iteration on every `currency/award` event for that merchant — symptom was masked by setting that row to `active_status=false`, but any other merchant with a misconfigured factor would have hit the same cascade. v54 wraps both `currencyAward`'s `award-${i}` step and `currencyReversal`'s `check-balance-${i}` + `create-reversal-${i}` steps in per-iteration try/catch — failures are recorded into `results[]` with a `failed` status and full error context (`step`, `index`, `currency`, `component`, `target_entity_id`, `amount`, `error` for awards; `award_id`, `step`, `error`, `attempted_amount`, `proportion` for reversals), the loop continues to the next sibling, and the workflow now completes with `status ∈ {awarded, partial_success, all_failed}` plus `failure_count`/`total_count` in `inngest_workflow_log.metadata`. Function-level `try/catch` (around RPC errors on `calc_currency_for_source` / `get-merchant-config` / etc.) and the `idempotency` key + `check-already-awarded` short-circuit are unchanged so re-firing the same event remains a no-op for sources that already have any successful sibling. Intentional trade-off: failed sibling rows are not auto-retried by Inngest after this fix — operators fix the root cause and manually re-fire after clearing the per-row `dedup_key {source_type}_{source_id}_{user_id}_award_{i}` partial wins; per-row retry is a larger separate workstream. Deployed via Supabase MCP `deploy_edge_function` with explicit `verify_jwt:false` (same trap that bit `receipt-preview-v2` / `shopify-webhooks` — default is `true` and would silently break Inngest's `INNGEST_SIGNING_KEY` verification). Source code lives only in the deployed Supabase function (no in-tree mirror under `supabase/functions/`); future agents read it via Supabase MCP `get_edge_function`. See `docs/CRITICAL_BUG_FIXES.md` §2026-05-15 — `inngest-currency-serve` Loop Hardening.
- **Loyalty Redemption / Shopify (follow-up: v4 — accept 3rd cart-attribute key variant)** — `shopify-webhooks` v3 (deployed earlier today) didn't fire on the first 3 paid test orders after deploy because the storefront widget shipped a third key spelling that didn't match either of the variants v3 accepted. Spec was `_loyalty_intent_id` (leading single underscore, matching the convention of the existing `_loyalty_reprice_token` attribute the widget already writes). Initial widget impl shipped `loyalty_intent_id__` (trailing double underscores — typo). After the dev was asked to flip to canonical, they shipped `loyalty_intent_id` (bare — no underscores). Verified across 3 paid orders on `quickstart-ef112207.myshopify.com`: #1027 (08:04 UTC, customer 10011815510321 = `kritsana@rocket.in.th`, fresh redeem at 08:03 UTC intent `6e08469a-…`, cart attribute `loyalty_intent_id: 6e08469a-…` — bare key); #1028 (08:48 UTC, same customer, **no `customAttributes` at all**, but the metafield was still `status:"active"` from the unconsumed `6e08469a` so the storefront Shopify Function happily applied the same `Loyalty Redemption -10 THB` line again — exact bug reproduction); #1029 (08:51 UTC, customer 9856173539633, fresh redeem intent `58f48fe4-…`, cart attribute `loyalty_intent_id: 58f48fe4-…` — bare key, different customer). v4 widens `LOYALTY_INTENT_KEYS` to `["_loyalty_intent_id", "loyalty_intent_id", "loyalty_intent_id__"]` (canonical first → preferred when multiple are present, then current production variant, then original typo) and warns when any non-canonical key fires (`[shopify-webhooks] non-canonical intent attribute key in use` with `used_key`/`canonical_key` fields) so the dev can converge on `_loyalty_intent_id` and we can drop the other two after a quiet week of zero warns. **Side observation worth flagging to the storefront team:** order #1028 had ZERO custom attributes — meaning the widget's `/cart/update.js` write only happens when the customer actively redeems in the current session; it doesn't re-stamp on cart load when an existing active metafield is rendering the discount. This is fine in steady state (because once we consume on the redeem-session order, subsequent sessions won't get a discount line at all — assuming the storefront Function gates on `value.status === "active"`), but it means each unconsumed metafield can be silently re-used across sessions until something consumes it. The v3 deploy missed all 3 of these orders because of the key mismatch — those metafields are now stranded `active` and will need to be cleared either by the next redeem (overwrites with new intent) or by a manual `customerUpdate` flip; not auto-cleaning to avoid confusing in-flight QA. v4 also redeployed with explicit `verify_jwt:false` (same MCP trap as v3 — `deploy_edge_function` defaults `verify_jwt:true` and silently re-flipped JWT-on; caught and corrected on first response). Verification path: dev places a fresh redeem + paid order; expected log line `[shopify-webhooks] orders/paid quickstart-ef112207.myshopify.com order <id> status=paid matched=<uuid> claim=<code> consume=ok` plus the warn line for non-canonical key; metafield should flip from `status:"active"` to `status:"consumed"` with `consumed_at`/`order_id` written; next cart should NOT show the Loyalty Redemption line (gated by storefront Function checking `status === "active"`).
- **Loyalty Redemption / Shopify** — fixed the bug where a points→discount entitlement (Shopify customer metafield `loyalty.discount_entitlement`, written by `shopify-points-to-discount`) stayed `status:"active"` after the customer placed an order, so the same −X THB "Loyalty Redemption" line kept auto-applying to subsequent carts (single test customer on `quickstart-ef112207.myshopify.com` had the metafield silently overwritten on each re-redeem with no consume audit trail anywhere — only the burn ledger row remained, which is why the spend-side balance went down but the discount kept appearing). Linkage from order → entitlement is via a cart attribute the storefront widget stamps after a successful redeem (carries forward natively as `order.note_attributes` per Shopify's `cart.attributes → note_attributes` pipe — no plumbing needed beyond a single `/cart/update.js` write). Backend now reads both `_loyalty_intent_id` (canonical, leading single underscore matching `_loyalty_reprice_token` convention) and `loyalty_intent_id__` (legacy / typo from initial widget impl, will deprecate after warn logs drain) and the value must be the same UUID stored in `wallet_ledger.source_id` of the redemption burn row (also embedded in the `dedup_key` `reward_redemption_<intent>_<user>_<merchant>_base_burn`). `shopify-webhooks` v3 — only path changed — adds an independent consume step on `orders/paid` (other 4 topics untouched) that runs **after** the marketplace earn-back path completes and is wrapped in its own try/catch so a Shopify Admin API hiccup can't fail the order capture: extracts intent from `note_attributes` (regex-validated UUID, prefers canonical key), requires a `Loyalty Redemption` discount line on the order (`order.discount_applications[].title` case-insensitive — same title the storefront Shopify Function emits), resolves customer GID from `order.customer.admin_graphql_api_id` (falls back to `gid://shopify/Customer/${customer.id}`), then GraphQL-reads `customer(id:){metafield(namespace:"loyalty",key:"discount_entitlement"){value}}`, validates `parsed.status === "active"` (no-op + return `success:true reason:"already_consumed_or_inactive"` otherwise — idempotent under Shopify's webhook retries) AND `parsed.intent_id === intentId` (defense against the metafield having been overwritten by a newer redeem between checkout and webhook delivery — returns `intent_id_mismatch` so the newer intent isn't accidentally consumed by an older order), and on match issues `customerUpdate` with `metafields:[{namespace:"loyalty",key:"discount_entitlement",type:"json",value:{...prev, status:"consumed", consumed_at, order_id}}]`. Helpers ported from `shopify-points-to-discount`: `shopifyGraphql` with `isNearExpiry`/`refreshShopifyToken` so the consume path survives offline-token expiry the same way the redeem path does (writes refreshed credentials back to `merchant_credentials.credentials`). MCP `deploy_edge_function` defaults `verify_jwt:true` and silently re-flipped JWT-on on the first deploy (same trap as `receipt-preview-v2`) — caught and redeployed v3 with explicit `verify_jwt:false`. Verified live state for `kritsana@rocket.in.th` on Shopify Test Merchant: latest order #1019 (`7352356569393`, paid 2026-05-15 06:44 UTC) carried `loyalty_intent_id__: 15bd960d-b7b1-48fd-96bd-7f1dddad89ba` in `note_attributes` matching `wallet_ledger.id=f5e6905d source_id=15bd960d` burn row (-10 points THB), but the metafield was already overwritten by a *new* redeem at 06:58 UTC (`f26b8f18-...`, 6 THB) so the consume window for the old intent was already lost — next paid order on this customer that carries the new intent will exercise the deployed path. **Storefront dependencies that still need to ship:** (1) widget dev needs to rename the cart-attribute key from `loyalty_intent_id__` to `_loyalty_intent_id` to match the canonical naming and let us drop the legacy fallback after a quiet week; (2) the storefront Shopify Function that reads the metafield and emits the `Loyalty Redemption` discount line MUST gate on `value.status === "active"` (not just presence-of-metafield) for the consume to actually stop the discount on the next cart — if it doesn't, the metafield flips to `consumed` correctly but the discount keeps applying client-side and the bug appears unfixed; we don't have access to the Function source so can't verify, flagging for the storefront dev. **Out of scope for this change (deferred):** Option C from the design discussion — writing a `reward_redemptions_ledger` row at redeem time (source_type would need a new `'shopify_metafield_entitlement'` value plus `fn_use_entitlement`/`fn_reverse_entitlement_use` allowlist relaxation from the current `('package_assignment','persona_entitlement')` gate) so we have an in-DB audit row independent of Shopify's metafield, and then calling `fn_use_entitlement` in the consume path; refund-on-cancel/refund (would need to write a `chokepoint_post_wallet_transaction` reverse with `source_type='redemption_cancellation'` per the `reversePointsBurn` helper in `shopify-points-to-discount`, AND flip metafield back to `status:"active"`); double-redeem race (today the second redeem silently overwrites the metafield AND points are burned twice — neither this fix nor any existing code reverses the first burn; gating the redeem fn on "no active entitlement exists OR auto-refund the prior intent" is the right call but is its own surface). See `requirements/Reward.md` and `requirements/Ecommerce_Marketplace_Integration.md`.
- **Marketplace Integration / Shopify** — Shopify orders now flow into the loyalty pipeline without merchants ever touching a webhook URL. Two new edge functions: `shopify-webhooks` (public, no JWT) receives `orders/create|paid|fulfilled|cancelled|updated`, verifies HMAC against `merchant_credentials.credentials.api_secret` (single shared Shopify app secret), normalizes the order JSON into the `upsert_marketplace_order` shape, calls `fn_match_marketplace_user` to find a CRM member by `marketplace_external_ids->>'shopify'` → email (case-insensitive) → tel (digits-only), and on match calls the new `fn_claim_marketplace_order_service(p_merchant_id, p_platform, p_order_sn, p_user_id, ...)` — a service-role twin of `claim_marketplace_order` that accepts explicit `p_merchant_id`/`p_user_id` so webhook handlers (no `auth.uid()`) can claim into `purchase_ledger` and award currency through the existing earn engine; `shopify-register-webhooks` (JWT-protected, idempotent) calls the Shopify Admin API to register the 5 order topics → `https://wkevmsedchftztoolkmi.supabase.co/functions/v1/shopify-webhooks`, with `replace_others=true` so re-registers heal stale rows, and stamps `merchant_credentials.webhook_url` + `credentials.webhook_endpoint`/`webhook_topics`/`webhook_registered_at`. `auth-shopify-admin` was updated to fire-and-forget the registration call after `bootstrapOfflineTokenIfNeeded` succeeds OR whenever `credentials.webhook_endpoint IS NULL` on login (self-healing for the 6 previously-installed merchants). Status mapping extended: `map_marketplace_status` and `get_claimable_statuses` now have a `shopify` branch (`paid|fulfilled` → completed; `voided|cancelled` → cancelled; `refunded` → refunded; default claim threshold `'paid'`), and `claim_marketplace_order` got the same `WHEN 'shopify' THEN 'paid'` default. `order_ledger_mkp.platform` and `order_items_ledger_mkp.platform` CHECK constraints were extended to include `'shopify'`. Backfill: registration ran for 5 of 6 active `shopify_app` rows (`aprify-admin-2`, `quickstart-ef112207`, `0nhwb1-zc`, `edb1gj-bi`, `merchant-test-6621`); the sixth (`8mqr0k-qs.myshopify.com`, `health_status='reauth_required'`) is intentionally skipped and will self-register on next admin re-auth via the `webhook_endpoint IS NULL` self-heal path. End-to-end smoke-tested with two synthesized-and-HMAC-signed `orders/paid` deliveries against `quickstart-ef112207.myshopify.com`: unmatched-email path produced order row only (no claim, no points); matched-email path (`prakan@rocket.in.th`) produced `order_ledger_mkp` (synced=true, claimed_user_id stamped) + `purchase_ledger MKP-SHOPIFY-9999000002` ($120, source=`marketplace_shopify`, processing_method=`queue`) + `wallet_ledger` earn entry. Smoke-test `order_ledger_mkp` rows deleted; the +1-point wallet entry on the test merchant was left intact as proof-of-life. See `requirements/Ecommerce_Marketplace_Integration.md` (note: that doc is stale — `requirements/REGISTRY_RENDER.md` and `requirements/REGISTRY_SUPABASE.md` are authoritative).
- **Chokepoint → Kafka pipeline (Phase 2 — production activation, all 4 chokepoints)** — the four queued A5 migrations from yesterday's Phase 1+B groundwork landed and the pipeline is live in dual-run. Migrations applied in risk order: `chokepoint_post_purchase_event_emit_v1` (12 event branches each gated on `p_payload->>'_skip_emit'<>'true'` so backfills + recursive chokepoint-to-chokepoint calls don't double-emit; parent emits join `user_id`+`earn_currency` from `purchase_ledger` at the end of each branch with partition_key=`purchase_id` so parent + items co-partition on Kafka, item emits also keyed on `purchase_id`), `chokepoint_post_wallet_transaction_emit_v1` (one emit per call after the existing `INSERT … RETURNING id INTO v_wallet_id`, partition_key=`p_user_id` for per-user ordering, payload includes `currency`/`component`/`transaction_type`/`amount`/`source_type`/`source_id`/`target_entity_id`/`description` mirroring the legacy CDC shape), `chokepoint_post_tier_change_emit_v1` (one emit at end, partition_key=`p_user_id`, payload includes `from_tier_id`/`to_tier_id`/`change_type`/`change_reason`/`metadata`/`skip_cdc` so the AMP consumer's `WHERE !skip_cdc` filter reproduces from the chokepoint side), `chokepoint_post_user_event_emit_v1` (3 emit sites covering all 4 branches: dedicated emit in the `create` branch with auto-tier-assigned `tier_id` snapshot, dedicated emit in the `hard_delete` branch with `user_after=NULL` since the row is gone, shared trailing emit for `update` + `soft_delete` that reads the post-mutation row including the auto-scrubbed PII for soft_delete; partition_key=`user_id`, payload includes `event`/`user_event_type`/`changes`/`actor`/`skip_cdc`/`user_after` so AMP can filter `event='create' AND !skip_cdc` mirroring legacy `op='c' AND NOT skip_cdc`). Each migration validated by an isolated smoke harness DO block before moving to the next: B1 purchase smoke asserted exactly 3 outbox rows for a 2-line `created` event (1 parent + 2 items); B2 wallet smoke asserted 1 emit with correct `currency='points'`/`component='adjustment'`/`source_type='manual'` (plan's harness used invalid enum value `'adjustment'::wallet_transaction_source_type` which doesn't exist — closest valid is `'manual'`, fixed in execution); B3 tier_change smoke asserted 1 emit with `change_type='upgrade'`/`skip_cdc=true`; A5d smoke extended beyond the plan's create-only B4 to a full-branch sweep covering create+update+soft_delete+hard_delete on two synthetic users — verified `user_after.firstname='SmokeAllUpdated'` after update, `user_after.firstname IS NULL` + `user_after.deleted_at IS NOT NULL` after the auto-scrub on soft_delete, and `user_after IS JSON null` on hard_delete. Render-side activation completed with both flags flipped to `true` (`OUTBOX_PUBLISHER_ENABLED` then `CHOKEPOINT_EVENTS_ENABLED`); worker drain validated end-to-end via a synthetic helper-direct insert (`fn_chokepoint_emit_event` called from a top-level statement so the row commits and `pg_notify` actually delivers to the worker — earlier DO-block-wrapped attempt rolled back because the surrounding RAISE EXCEPTION reverted the INSERT; not a worker bug, a test-design bug) which the worker drained in **235 ms** end-to-end including KafkaJS publish (sub-second proves LISTEN/NOTIFY is wired, not just the 1s polling fallback). Step 9 dual-pipeline correctness validated via synthetic `chokepoint_post_user_event('create', merchant=New CRM, skip_cdc=false)` — both rows (`crm.events.user` create + `crm.events.tier_change` initial) published in the same worker batch at **756 ms**, no errors, `publish_attempts=1`, recursive chokepoint chain produced exactly 1 `tier_change_ledger` + 1 `tier_progress` row (no double via the recursive call), and zero spurious `workflow_log`/`inngest_workflow_log` dispatches (none expected — no `user_accounts` workflow trigger configured for New CRM). Redis dedup absorption between Debezium and chokepoint pipelines was **not** exercised by the synthetic test (the only event types that would actually trigger a workflow on New CRM are `wallet_ledger:INSERT` via `points_earned` triggers and `purchase_ledger:INSERT` via `purchase_completed` triggers, both of which would fire 3-5 active workflows including LINE messages — defer to organic traffic). Pipeline is now in 24h soak. SLO gate for Phase 3 decommission: `unpublished_stale=0` + `errored=0` + Confluent rate ≈ chokepoint call rate + zero double-awards in `wallet_ledger` + zero duplicate dispatches in `workflow_log`/`inngest_workflow_log`. After 24h healthy, single `ALTER PUBLICATION crm_cdc_publication DROP TABLE purchase_ledger, wallet_ledger, tier_change_ledger, user_accounts;` cuts over to chokepoint as sole source. `form_submissions` and external `amp_workflow_log` table (consumer-side, not the in-DB `workflow_log`) remain on Debezium pending future chokepoints. See `.cursor/plans/chokepoint-kafka-publisher-morning-checklist.md` for the full execution log.

## 2026-05-14

- **Chokepoint → Kafka pipeline (Phase 1+B, all 4 chokepoints)** — scope expanded from purchase-only to all four existing chokepoints (`chokepoint_post_purchase_event`, `chokepoint_post_wallet_transaction`, `chokepoint_post_tier_change`, `chokepoint_post_user_event`) in one cut-over. Phase 1 DB infra (outbox table + helper + cron + view) is chokepoint-agnostic — no additional migrations needed. Three new topic constants added to `crm-event-processors/src/config.ts` (`crm.events.wallet`, `crm.events.tier_change`, `crm.events.user`) bringing the total to five (plus existing `crm.events.purchase` + `crm.events.purchase_item`). Three additional consumers received the same gated dual-read pattern that landed for currency + AMP last night: `tier-consumer.ts` (now reads `crm.events.purchase` + `crm.events.wallet` with `transaction_type='earn'` filter mirrored on chokepoint payload, dedup key `tier:{userId}:{merchantId}` unchanged so dual-run absorbs); `mission-consumer.ts` (adds `extractChokepointSourceInfo` helper that maps purchase events on `status='completed'` filter and wallet events on `transaction_type='earn'` filter to legacy `purchase` / `points_earned` / `tickets_earned` condition types — dedup key `mission:{conditionType}:{sourceId}:{missionId}` unchanged); `outcome-attribution-consumer.ts` (adds `handleChokepointEventMessage` that maps purchase `status='completed'` to `purchase_completed`, wallet `transaction_type='earn'/'redeem'` to `points_earned`/`points_redeemed`, dedup key `oattr:<eventType>:<sourceId>` unchanged). `amp-consumer.ts` extended to subscribe to all five chokepoint topics with per-topic dispatch into the existing `dispatchToWorkflowTrigger` shape: `wallet_ledger` (filter `transaction_type='earn'`, dedup `amp:wallet_ledger:{wallet_ledger_id}`), `tier_change_ledger` (filter `!skip_cdc`, dedup `amp:tier_change_ledger:{tier_change_ledger_id}`), `user_accounts` (filter `event='create' AND !skip_cdc`, dedup `amp:user_accounts:{user_id}`, reads post-mutation row from new `user_after` payload field). `RewardConsumer` and `MarketplaceConsumer` unchanged — they don't read any of the four chokepoint surfaces. `currency-consumer.ts` doesn't need extension — it's a producer of wallet writes via `chokepoint_post_wallet_transaction`, never a consumer of `wallet_ledger`. Morning checklist (`.cursor/plans/chokepoint-kafka-publisher-morning-checklist.md`) rewritten end-to-end to cover all 4 chokepoints: 5 Confluent topics provisioned in step 1, single `chokepoint-publisher` API key with WRITE on `crm.events.*` prefix in step 2, A5 split into 4 sequential migrations (A5a purchase, A5b wallet, A5c tier, A5d user — apply in that order so highest-risk chokepoint is first to validate, lowest-risk last) each with its own smoke harness DO block, decommission step 11 drops all 4 tables (`purchase_ledger`, `wallet_ledger`, `tier_change_ledger`, `user_accounts`) from `crm_cdc_publication` in one ALTER PUBLICATION. README's Consumers table now lists all 7 consumers with legacy + chokepoint topic columns. Out of scope: `form_submissions` and `amp_workflow_log` stay on Debezium until chokepoints are built for those domains. Original 2026-05-14 entry below covers the underlying DB infrastructure.
- **Chokepoint → Kafka pipeline (Phase 1, infra-only)** — additive DB infra landed for the silver-tier outbox→worker→Kafka path that will replace the Debezium CDC of `crm.public.purchase_ledger`. Four migrations: `chokepoint_event_outbox_table_v1` (new `public.chokepoint_event_outbox` table with bigserial PK, topic / partition_key / payload jsonb / created_at / published_at / publish_attempts / last_error columns, CHECK constraint enforcing `topic ~ '^crm\.events\.[a-z_]+$'`, two partial indexes on unpublished + cleanup); `chokepoint_emit_event_helper_v1` (new `fn_chokepoint_emit_event(p_topic text, p_partition_key text, p_payload jsonb) → bigint` SECURITY DEFINER helper that validates topic, INSERTs the row, and `pg_notify('chokepoint_outbox_new', id)` for low-latency worker wake-up; EXECUTE granted only to postgres + service_role); `chokepoint_outbox_cleanup_cron_v1` (`pg_cron` job `chokepoint_outbox_cleanup` daily 03:00 UTC that DELETEs rows where `published_at < now() - 7d`); `chokepoint_outbox_health_view_v1` (per-topic + `__total__` view `v_chokepoint_outbox_health` with unpublished count, stale (>5min) count, max attempts, errored count, oldest unpublished age, table size). All four are purely additive — `chokepoint_post_purchase_event` is **untouched**, so no chokepoint emits to the outbox yet (zero behavior change in production). The Render-side worker (`crm-event-processors/src/workers/outbox-publisher.ts`) and the consumer-side dual-read changes for `currency-consumer.ts` + `amp-consumer.ts` (gated by `CHOKEPOINT_EVENTS_ENABLED`) are drafted in the worktree and ship on the next deploy. Smoke-verified: helper INSERTs 3 synthetic rows correctly, view returns the right per-topic + total stats, bad topic correctly rejected with sqlstate 22023, cron job registered (jobid 51, active). Activation requires manual user steps: provision Confluent topics `crm.events.purchase` + `crm.events.purchase_item`, mint a dedicated `chokepoint-publisher` API key with WRITE on `crm.events.*`, set `OUTBOX_PUBLISHER_ENABLED=true` + `SUPABASE_DB_DIRECT_URL` on Render, then apply the queued Phase 2 migration that wires `fn_chokepoint_emit_event` calls into every event branch of `chokepoint_post_purchase_event`. See `.cursor/plans/chokepoint-kafka-publisher.md` for the full plan, design rationale (silver tier chosen over gold/Debezium-SMT because of prior 27 GB WAL slot bloat experience — see thread d0b94926), and the morning checklist in `.cursor/plans/chokepoint-kafka-publisher-morning-checklist.md`.
- **Purchase Transaction / Syngenta Events** — hotfix for `api_create_manual_purchase_order`: it was stamping `auth.uid()` (the admin's `auth.users.id`) into both `purchase_ledger.completed_by_user_id` and `purchase_items_ledger.completed_by_user_id` whenever every line item arrived with `quantity_completed = quantity` — i.e. exactly the new "ทั้งหมด / pick up all" cashier shortcut on event orders. Both columns are FK'd to `user_accounts(id)` (the member table), and admin ids live in `admin_users`, so every such request died with `23503: insert or update on table "purchase_items_ledger" violates foreign key constraint`. Verified zero rows ever made it past the FK (`SELECT count(*) ... LEFT JOIN user_accounts ... WHERE completed_by_user_id IS NOT NULL AND ua.id IS NULL` returned `0` on both tables), so no backfill was required. Fix: removed the `v_caller_user_id := auth.uid()` assignment and write `NULL` into both `completed_by_user_id` slots in the chokepoint payload (header + each item). `completed_by_source = 'admin_manual_order'` is preserved on the rollup-completed path, and the FE-supplied `metadata.created_by_admin_user_id` continues to carry the audit trail. Public signature unchanged; only the Syngenta pickup-all path is unblocked, all other paths produce identical rows. Verified end-to-end via a rolled-back `DO` block that called the function with a 2-unit pickup-all line through `chokepoint_post_purchase_event('created', ...)` and asserted parent + item rows are born `status='completed'`, `completed_by_user_id IS NULL`, `completed_by_source='admin_manual_order'`. See `requirements/Purchase_Transaction.md`.
- **BigCommerce Storefront API** — `api_bigcommerce_get_user_profile` was returning **HTTP 200 with an empty body** for any user where one of its supporting CTEs was empty. The function stitched 5 CTEs together with `CROSS JOIN`, so a single empty CTE collapsed the entire `SELECT` to zero rows and `SELECT … INTO v_result` left `v_result` as SQL NULL → PostgREST rendered that as an empty body. Two production failure modes triggered this: (a) `tier_data` empty when `user_accounts.tier_id` is NULL or references a deleted tier — affects ~203k users (1,034 in the last 7 days) including the recently-signed-up cohort surfaced in the bug report; (b) `earn_rate` empty when the merchant has no `active_status=true` row in `earn_factor` for `(target_currency='points', earn_factor_type='rate')` — affects 15 of 23 merchants holding ~1.9M users (Yuedpao, Futurepark, Top Charoen, Dr.PONG, Kao Smile Club, VISTRA, Dookkith, Her Hyness, Seiko, Jorakay, Boonsiri, Merge, Kovet, BMW, Euro Creations) for whom every call returned empty. Rewrote the function to anchor on `user_data` (the only required CTE) and `LEFT JOIN` `tier_data`, `earn_rate`, `merchant_data`, `all_tiers`, then COALESCE all tier-derived fields (`tierName ''`, `pointThreshold 0`, `color #000000`, `icon star`, `benefits []`, `card_design {}`, `burnRateEnabled false`, `maxDiscountInBaht 0`, `isDefault false`) and earn-rate-derived fields (`points 0`, `bahtSpent 1` when no rate). `tier_data`'s `point_threshold` is now a scalar subquery filtered to `active_status = true ORDER BY created_at ASC LIMIT 1` (was previously a LEFT JOIN that returned multiple rows when a tier had both active and inactive upgrade conditions, with non-deterministic row pickup). Added explicit `IF v_result IS NULL THEN RETURN '{"error":"User profile not found","code":"USER_NOT_FOUND"}' END IF` so a non-existent user under a valid merchant gets a typed error instead of an empty body. Healthy-user response is byte-identical (verified via `jsonb` deep-equal on `3f9eda27-…` before and after, 7221-byte body unchanged); FE callers see no shape change. No migration or backfill required. Surface: BigCommerce storefront RPC, function-defined (no `requirements/domains/` source doc; documented in `internal_knowledge_blocks` under `bigcommerce-storefront-api`).
- **CRM Knowledge / Packages & Persona Entitlements** — seeded two new feature items in `internal_knowledge_feature_items` under the `loyalty` domain (`packages`, `persona-entitlements`) plus the canonical 5-block set per feature in `internal_knowledge_blocks` (`overview` + `rules` + `frontend_journey:admin_fe` + `frontend_journey:user_fe` + `technical_reference:be`). Created two focused source-of-truth deep docs split out from the bundled `requirements/Package_Contract_Benefit.md`: `requirements/Package.md` (package_master / package_items / package_assignment / runtime ledger / `bff_upsert_package_with_items` / `bff_admin_assign_package` / `get_user_packages` / `select_elective_items` / `api_use_entitlement` / `fn_create_package_entitlements` mandatory-only materialization / validity precedence days→date→effective_to / no scheduled `cron.job` for `fn_expire_package_entitlements`) and `requirements/Persona_Entitlement.md` (persona_entitlement catalog with the package | reward | benefit discriminator / `fn_auto_assign_on_persona` switch / `user_benefit` runtime / read-time validity for persona-sourced benefits via `fn_evaluate_user_benefits` / highest-value-per-category precedence via `fn_resolve_benefit_precedence`). Both deep docs flag two live-state corrections to the bundled spec: (a) `trg_auto_assign_on_persona_change` on `user_accounts` is **not** installed today (the only triggers there are `set_updated_at_user_accounts` and `trg_user_accounts_check_store_merchant`), so persona-driven auto-grants only fire when something explicitly calls `fn_auto_assign_on_persona`; (b) `fn_auto_assign_on_persona`'s `entitlement_type='reward'` branch sets `use_expire_date = now()` instead of `now() + validity_days::interval`, effectively expiring direct-reward grants immediately for any reward configured with `validity_days`. No DB schema changes. Block embeddings will be backfilled by the existing `trg_internal_knowledge_blocks_enqueue_embedding_insert` queue worker. See `requirements/Package.md`, `requirements/Persona_Entitlement.md`, and (for the bundled history) `requirements/Package_Contract_Benefit.md`.
- **Activity-Based Earning** — admins can now **edit + recalculate currency** on already-approved or rejected activity uploads. New BFF `bff_edit_activity_upload(p_upload_id, p_field_values, p_reason, p_language) → jsonb` accepts a status of `approved` or `rejected` (rejects `pending` — that path stays on `bff_approve_activity_upload`), revalidates fields via `fn_validate_activity_field_values`, re-checks approval limits via `fn_check_activity_approval_limits` (excludes the current row), recomputes per-`(currency, target_entity_id)` target totals via `fn_calculate_activity_currency`, diffs against the current net read from `wallet_ledger` (`source_type='activity' AND source_id=upload_id`), and posts one `chokepoint_post_wallet_transaction` call per non-zero delta — `component='base'`+`transaction_type='earn'` when transitioning from rejected, `component='adjustment'`+`transaction_type='earn'` for delta-up, `component='reversal'`+`transaction_type='burn'` for delta-down. Burns flow through the existing `trg_fifo_burn_tracking` AFTER-INSERT trigger (oldest-expiry-first across all earn lots — pure FIFO, not targeted-by-source). Partial-cover clamping: when `current_balance < |Δ|` the function clamps `amount` to the available balance and records `unreversed_amount` + `skipped_reason ∈ {zero_balance, partial_balance}` in both `wallet_ledger.metadata` and `activity_upload_ledger.edit_history`. Explicit dedup keys (`activity_<upload>_v<edit_version>_<currency>_<target_or_'none'>`) prevent collisions with the unique index `idx_wallet_ledger_dedup_key`. Two new columns on `activity_upload_ledger`: `edit_version integer NOT NULL DEFAULT 0` and `edit_history jsonb NOT NULL DEFAULT '[]'` — appended-only audit log carrying admin id/name, reason, previous/new field values, previous/new totals, per-currency deltas, ledger ids, and skipped reasons. Editing a rejected upload re-approves it (sets `status='approved'`, stamps `approved_at`/`approved_by`, clears `rejected_at`/`rejected_by`/`rejection_reason`). No time-window cap. Sibling reader `bff_get_activity_uploads` now surfaces `edit_version` and `edit_history` in its response so admin FE can render the audit log without a second read. Wallet writes go through the Phase-1 chokepoint — zero direct `wallet_ledger` inserts in the new BFF. Verified via post-deploy smoke: function existence, signature `(uuid, jsonb, text, text)`, body contains `chokepoint_post_wallet_transaction`, zero direct ledger writes, both columns NOT NULL with correct defaults, list BFF body references both new fields. See `requirements/Activity_Based_Earning.md` and `requirements/ACTIVITY_WALLET_INTEGRATION.md` §Edit-and-Recalculate.
- **Syngenta Events / Purchase Transaction** — admin-driven per-line-item completion on event orders (with bounded reversal). Three pieces shipped together: (1) New chokepoint event `item_completion_set` that takes **absolute target `quantity_completed` values** per item (not deltas), forward-rolls items to `completed` and stamps `completed_at/by_user_id/by_source` when target reaches `quantity`, **clears** completion stamps and flips items back to `pending` when target drops below `quantity`, and rolls the parent forward to `completed` only when all items terminal (never reverses parent). Hard-rejects when parent is `completed` (`PARENT_COMPLETED_USE_REFUND`) or in a terminal state (`PARENT_TERMINAL_STATE`). Sits alongside the existing additive-delta `item_quantity_completed` event used by `bff_seller_complete_purchase_items` (unchanged); the new event is the operator-override path. (2) `chokepoint_post_purchase_event('created', ...)` now passes per-item completion fields through (`status`, `quantity_completed`, `completed_at`, `completed_by_user_id`, `completed_by_source` on each item entry override the row-level fallback when present) so a fresh order can be born with mixed item states. Backward-compatible — existing callers that don't pass these fields keep getting the row-level fallback. (3) New BFF `bff_admin_set_event_order_item_completion(p_transaction_number text, p_line_items jsonb) → jsonb` wraps the new chokepoint event with event-team auth (`fn_event_caller_role` ∈ `owner | coordinator | superadmin` or it returns `FORBIDDEN`) and per-line validation (codes: `PURCHASE_NOT_FOUND`, `NOT_EVENT_ORDER`, `ITEM_NOT_FOUND`, `ITEM_TERMINAL_STATE`, `INVALID_QUANTITY_COMPLETED`, `INVALID_STATUS_COMBINATION`). Stamps `completed_by_source = 'admin_set_completion'` and `completed_by_user_id = auth.uid()`. (4) `api_create_manual_purchase_order` now accepts optional per-item `quantity_completed` (`0..quantity`, default `0`) and `status` (`pending | completed`, default derived from quantity_completed). Validates `INVALID_ITEM_COMPLETION` when out-of-range or when status disagrees with quantity_completed. Computes parent rollup: if all items end up completed, the parent purchase is created with `status = completed` (so currency awarding fires from the queue worker on the `earn_currency = true` path); else parent is born `pending`. Stamps parent `completed_by_source = 'admin_manual_order'`, `completed_by_user_id = auth.uid()` when rolled up. (5) `bff_get_event_order_details` items projection now includes `status`, `quantity_completed`, `completed_at`, `completed_by_source` (additive-only — existing FE callers ignore the new fields). The pre-existing data-integrity trigger `trg_purchase_complete_cascade_items` (parent → item completion cascade on parent UPDATE only) interacts cleanly: the new event always sets the parent before children are already terminal, so the cascade is a no-op on the rollup path; on the create path the cascade does not fire (INSERT-only trigger condition). Currency awarding wallet entries fire only when parent transitions `pending → completed`; reducing `quantity_completed` on a still-pending parent never creates or reverses a wallet entry. The Syngenta salesperson can now mark "picked up at the event" line items at create time and edit them after creation, with bounded operator correction (e.g. typo from 5 → 2) until the parent rolls to `completed`. Verified via four `apply_migration` deploys (chokepoint, api_create_manual_purchase_order, bff_admin_set_event_order_item_completion, bff_get_event_order_details), function-existence + body-content smoke checks, three error-path checks against `chokepoint('item_completion_set', ...)` (missing merchant, missing payload, purchase not found), and a read of three real event-order line-item rows confirming the new fields are populated. See `requirements/Purchase_Transaction.md` §Single Write Chokepoint and `requirements/Syngenta_Events.md` §Orders.
- **Currency / earn-studio BFFs** — exposed the per-line-item earning configuration through admin BFFs so loyalty-admin can edit it. Patched 8 functions: (a) the three earn_factor readers `bff_get_earn_factor_details`, `bff_get_earn_factor_group_details`, and `bff_get_earn_factors_by_group` (list view used to render factor cards) now all return `award_scope` and `allowed_item_statuses` in the per-factor JSON; (b) `bff_upsert_earn_factor_group` parses both keys per factor (with `INVALID_AWARD_SCOPE` validation), writes them through the INSERT column list and UPDATE SET, and uses `COALESCE(parsed, existing-value)` on update so an omitted key is a no-op (mirrors the existing `allowed_purchase_statuses` pattern); (c) `bff_admin_get_earn_channels` adds `default_award_scope` to each channel row and adds a `default_award_scope` option list under `options` (`[{label: 'Per Purchase', value: 'purchase'}, {label: 'Per Line Item', value: 'purchase_item'}]`); (d) `bff_upsert_earn_channel` and `bff_upsert_single_earn_channel` both parse and write `default_award_scope` with the same `INVALID_AWARD_SCOPE` validation + COALESCE-no-op semantics; (e) defensive: `bff_get_basic_currency_config` now returns `mode='not_basic'` with reason `'Group contains per-line-item factors; edit in earn-studio'` when the resolved group has any factor with `award_scope='purchase_item'`, preventing the basic-mode UI from silently stripping per-item scope on save. Member-facing `bff_get_earn_channels` and the AMP picker `bff_get_amp_action_options` were **not** patched — the default scope is an admin-only concept and the cached public payload / AMP dropdown shape stays unchanged. Public signatures and response envelopes for all 8 functions are additive-only (only new keys added). Verified via three rollback DO-block smoke harnesses against `cloudfriends`: 5/5 earn_factor read+write tests (mixed-scope create, group-detail read, factor-detail read, COALESCE no-op on update, INVALID_AWARD_SCOPE rejection), 6/6 earn_channel tests (create with explicit scope, admin read surfaces field + options dropdown, COALESCE no-op, explicit flip, INVALID_AWARD_SCOPE rejection, default `purchase` on omitted key), and list-view + basic-config guard test (per-item factor renders in `bff_get_earn_factors_by_group`, basic-mode declares not_basic when group has per-item factors). Registry signatures unchanged so no regen needed. See `requirements/Purchase_Transaction.md` and `requirements/architecture/event-chokepoints.md` for the underlying per-line-item earning feature this configuration drives.
- **Signup & Login** — signup codes now support **store binding**. Added nullable `signup_codes.store_id` (FK `store_master`) and nullable `user_accounts.store_id` (FK `store_master`), each guarded by a shared `BEFORE INSERT/UPDATE` trigger function `trigger_check_store_merchant_match` that rejects cross-merchant references. Pools opt in via `user_field_config.config.external_code.store_binding ∈ {off, optional, required}` (default `off`). `bff_upload_signup_codes` now accepts a per-row `store_code`, runs an all-or-nothing validation pass that returns `STORE_VALIDATION_FAILED` with per-row `STORE_BINDING_DISABLED` / `STORE_BINDING_REQUIRED` / `STORE_NOT_FOUND` errors before any writes, and persists the resolved `store_id` (overwriting on update). `bff_validate_signup_code` adds `data.store = { id, code, name }` (also embedded in `data.preview_metadata.store`) and two new pre-claim errors `NOT_SELLER` / `STORE_CONFLICT` when the caller is authenticated. `fn_consume_signup_code` enforces a hard rule for store-bound codes: requires `user_accounts.user_type='seller'` (`SIGNUP_CODE:NOT_SELLER`), and applies **first-wins** linkage — sets `user_accounts.store_id` only when null, idempotently no-ops on same-store re-consume, raises `SIGNUP_CODE:STORE_CONFLICT` on a different store; returns `linked_store_id` in the response envelope. `bff_list_signup_codes` joins `store_master` and now returns `store_id` / `store_code` / `store_name` per item plus a top-level `data.store_binding` reflecting the pool's mode. Public function signatures unchanged. Verified end-to-end via a rolled-back DO-block smoke test exercising cross-merchant trigger guard, buyer-blocked-with-NOT_SELLER, seller-first-link, different-store-conflict, idempotent re-consume, and user_accounts trigger guard (6/6 PASS). See `requirements/Signup_Login.md` §Signup Code System.
- **Purchase Transaction / Currency** — Phase A+B of per-line-item currency earning landed (purchase-line-item-earning). `chokepoint_post_purchase_event` is now the sole canonical writer of `purchase_items_ledger` as well as `purchase_ledger`. The `line_completed` event was deleted atomically and replaced with 5 new item-mutation events: `item_quantity_completed` (idempotent quantity-completed increments with parent rollup to `completed` when all items terminal), `item_cancelled`, `item_refunded`, `items_added`, `items_replaced`. The 4 direct-write callers of `purchase_items_ledger` were rewritten in the same change set: `bff_seller_complete_purchase_items` (uses `item_quantity_completed`), `api_update_purchase` ×2 overloads (use `items_replaced` and read `new_total_amount` from the event response), `claim_marketplace_order` (now passes items into `api_create_purchase` via the chokepoint's `created` branch), `fn_apply_event_promos` (uses `items_added` for engine freebies + pool-pick items). The data-integrity trigger `trg_purchase_complete_cascade_items` is preserved unchanged; the dead `trigger_process_purchase_currency` function was dropped. Phase B added config plumbing: `earn_factor.award_scope text NOT NULL DEFAULT 'purchase' CHECK (...)`, `earn_factor.allowed_item_statuses text[] NOT NULL DEFAULT '{completed}' CHECK (<@ {pending, completed})`, `earn_channel.default_award_scope`, `purchase_ledger.award_scope` (nullable per-purchase override), `purchase_items_ledger.currency_processed_at timestamptz NULL`, `purchase_items_ledger.currency_error text NULL`. Indexes added: `idx_purchase_items_ledger_txn_status (transaction_id, status)` and partial `idx_purchase_items_ledger_unprocessed (merchant_id, status) WHERE currency_processed_at IS NULL`. `wallet_transaction_source_type` enum gained `'purchase_item'`. New function `calc_currency_for_purchase_item(p_item_id uuid)` computes whole-item awards on basis `pli.line_total`, passes `p_purchase_status=NULL` to `calc_currency_core` to bypass the parent-status filter, then filters at the wrapper for `ef.award_scope = 'purchase_item' AND item.status = ANY(ef.allowed_item_statuses)`. `calc_currency_for_transaction` was tightened to filter `ef.award_scope = 'purchase'`. `calc_currency_for_source` got a new `'purchase_item'` dispatch branch. **No pro-rata** — partial-quantity completions do not award currency; the award fires only when the whole item flips to `'completed'`. Header-level discount is ignored in item mode; basis is `purchase_items_ledger.line_total` directly. Multi-status award dedup is one-per-`(item_id, earn_factor_id)` enforced at the wallet writer via existing Redis dedup. Verification: writer regex on `purchase_items_ledger` now returns exactly two functions — the chokepoint (canonical writer) and `trigger_cascade_purchase_complete_to_items` (data-integrity trigger function); a 10-test smoke harness inside a roll-back DO block exercises all 5 new events, parent rollup, status guards, and quantity overflow rejection. Consumer side (`Rocket-CRM/crm-event-processors` PR #3) is ready to subscribe to the new `crm.events.purchase_item` topic gated by `CHOKEPOINT_EVENTS_ENABLED` (default off until the publisher lands). Per-item earning is **dormant until the chokepoint→Kafka publisher is built** (plan: `.cursor/plans/chokepoint-kafka-publisher.md`). See `requirements/Purchase_Transaction.md` and `requirements/architecture/event-chokepoints.md`.
- **Syngenta Events** — new domain doc + two end-user features. (1) **Participating retailers per event**: new `event_store_allowlist (event_id, store_id, …)` table with `UNIQUE (event_id, store_id)`; owner-only management via `bff_list_event_stores`, `bff_search_admin_stores`, `bff_upsert_event_stores`. Allowlist is **FE-binding metadata only** — `api_create_manual_purchase_order` does not validate against it; FE binds the order-creation store dropdown when `is_scoped = true`. (2) **Coordinator invites**: owner-only management of the existing `coordinator_email[]` array via `bff_list_event_team`, `bff_search_admin_users_for_event`, `bff_invite_event_coordinator(p_event_id, p_admin_user_ids uuid[])` (multi-add, server resolves `admin_user_id → email`, skips already-owner / already-coordinator), `bff_remove_event_coordinator(p_event_id, p_admin_user_id)`. Invites are restricted to existing `admin_users` rows in the merchant. (3) **New helper** `fn_event_caller_role(p_event_id) -> text` returns `owner | coordinator | superadmin | none` (case-insensitive email match against `owner_email[]` / `coordinator_email[]`; superadmin bypass). (4) **Behavior change** — three previously open BFFs (`bff_list_event_products`, `bff_upsert_event_products`, `bff_list_event_orders`) are now gated to `owner | coordinator | superadmin` and return `403 FORBIDDEN` for callers outside the team. Previously: any merchant admin could read/edit any event's products and orders. FE callers outside an event-detail context need to handle the new error or route to a different BFF. See `requirements/Syngenta_Events.md`.
- **User Accounts** — hotfix v90 of `bff-auth-complete` restored the `auth_user_id = id` backfill on every existing-user login branch (centralized helper `backfillAuthUserIdIfMissing` placed before JWT signing, routed through `chokepoint_post_user_event` `update` event). The Phase 1 chokepoint rewrite (v89, earlier today) had inadvertently dropped this backfill, leaving legacy rows with `auth_user_id IS NULL` invisible to `get_user_summary` (which filters `WHERE auth_user_id = auth.uid()`). Symptom in production: after LINE OAuth redirect for a legacy account, frontend's `POST /rest/v1/rpc/get_user_summary` returned `(failed) net::ERR_CONNECTION_CLOSED` (no index on `user_accounts.auth_user_id` → seq scan on 2.2M rows → upstream timeout in some plans), the LoginDrawer stayed open, and only a full page refresh recovered (frontend used cached `user_account` from `bff-auth-complete`'s response, bypassing `get_user_summary`). One stuck user (`1db681a2-…`) was repaired in-place via a direct chokepoint `update` call. **Outstanding:** 2,145,894 rows still carry `auth_user_id IS NULL` (will auto-heal on next login under v90); `user_accounts.auth_user_id` has no index; both deferred pending approval.
- **User Accounts** — Phase 1 of the user-account event chokepoint pattern landed. New `chokepoint_post_user_event(p_event_type text, p_merchant_id uuid, p_user_id uuid, p_changes jsonb, p_skip_side_effects boolean, p_skip_cdc boolean, p_actor jsonb, p_dedup_key text, p_metadata jsonb) → user_accounts` is now the unconditional sole writer for `user_accounts`; it dispatches on a 4-event taxonomy (`create` / `update` / `soft_delete` / `hard_delete`) and accepts a whitelist `p_changes` JSON payload that drives both INSERTs and column-by-column UPDATEs. Eighteen in-DB writer functions were rewritten in one rolling migration set to route through it (`mark_signup_form_complete`, `fn_seller_resolve_user_type`, `bff_change_user_tel`, `bff_admin_change_member_mobile`, `assign_persona`, `claim_marketplace_order`, `fn_consume_signup_code`, `bff_admin_rollback_user_import`, `fn_execute_amp_action` `assign_persona` branch, `apply_tier_upgrade`, `ensure_tier_progress`, `admin_remove_user`, `bff_admin_create_member`, `bff_admin_update_member_profile`, `bff_save_user_profile`, `bulk_upsert_customers_from_import`, `api_create_or_update_user`, `api_update_user`); public signatures and response envelopes are unchanged. Two edge functions were redeployed (`auth-register` and `bff-auth-complete` — the latter preserved `verify_jwt=false` after an accidental flip and recovery), and `auth-line-login` was tombstoned with an HTTP 410 (zero active callers confirmed via GitHub audit across `weweb-syngenta` and other frontend repos; `weweb-syngenta` itself was confirmed fully retired). The four logic-bearing triggers on `user_accounts` were dropped along with their backing functions and their logic was ported directly into the chokepoint: `assign_entry_tier_on_signup` + `assign_entry_tier` (entry-tier resolution → `create` branch); `fn_sync_user_type_from_persona` + `trg_sync_user_type_from_persona` (persona→user_type derivation → `create` and `update`); `after_user_signup_tier_setup` + `after_user_signup_tier` (initial `tier_progress` row + cascade to `chokepoint_post_tier_change` for the initial-tier ledger entry → `create` post-write block); `trigger_auto_assign_on_persona_change` + `trg_auto_assign_on_persona_change` (persona-change benefit cancellation + `fn_auto_assign_on_persona` + `process_tier_event` → `update` post-write block). The data-integrity trigger `set_updated_at_user_accounts` (BEFORE UPDATE) was preserved unchanged. Tier interaction: the chokepoint explicitly cascades to `chokepoint_post_tier_change` on `tier_id` flips (with derived `tier_change_type` when not supplied via `p_metadata`), so callers that previously updated `user_accounts.tier_id` directly (`apply_tier_upgrade`, `ensure_tier_progress`) now route through the chokepoint and rely on it to write the `tier_change_ledger` row — eliminating the double-write risk. `soft_delete` auto-scrubs PII unconditionally (firstname/lastname/fullname/email/tel/line_id/birth_date/id_card/gender/image/external_user_id/marketplace_external_ids + `deleted_at = now()`); caller payload cannot un-scrub. The legacy `app.skip_side_effects` GUC was replaced by explicit `p_skip_side_effects` and `p_skip_cdc` arguments. Cleanup hardening: dropped two permissive RLS policies (`user_accounts_merchant_insert`, `user_accounts_merchant_update`) and revoked `INSERT`/`UPDATE`/`DELETE` from `anon` and `authenticated` (`postgres` and `service_role` retain write privileges); non-chokepoint writes are now blocked by both convention and grants. Two legacy `UPDATE user_accounts SET auth_user_id = ... WHERE auth_user_id IS NULL` backfill statements in `bff_save_user_profile` and `bulk_upsert_customers_from_import` were folded into the same chokepoint update payload (no separate write). Verification: `pg_get_functiondef(...) ~* 'INSERT INTO\|UPDATE … SET\|DELETE FROM (public\.)?user_accounts'` now returns exactly one function (the chokepoint); `information_schema.triggers` for `user_accounts` returns only `set_updated_at_user_accounts`; `pg_policies` returns only SELECT and service_role policies; `role_table_grants` for `anon`/`authenticated` returns no INSERT/UPDATE/DELETE; a DO-block round-trip smoke test through the `update` branch returned successfully. Kafka publish from inside the chokepoint and CDC decommissioning of `crm.public.user_accounts` are Phase 2, out of scope. See `requirements/architecture/event-chokepoints.md` and `requirements/Signup_and_Login.md`.

## 2026-05-13

- **Purchase Transaction** — Phase 1 of the purchase event chokepoint pattern landed. New `chokepoint_post_purchase_event(p_event text, p_merchant_id uuid, p_payload jsonb) → jsonb` is now the single canonical writer for `purchase_ledger`; it dispatches on a 7-event taxonomy (`created`, `updated`, `completed`, `line_completed`, `cancelled`, `refunded`, `promo_applied`) and accepts a `jsonb` payload that intentionally supports batched `rows[]` so `bulk_insert_purchases_with_items` can push a whole chunk through one set-based chokepoint call instead of the legacy per-row plpgsql loop. Ten in-DB writers were rewritten in one atomic change set to route through it (`api_create_purchase`, `api_create_manual_purchase_order`, both `api_update_purchase` overloads, `bulk_insert_purchases_with_items`, `bff_seller_complete_purchase_by_transaction_number`, `bff_seller_complete_purchase_items`, `fn_recompute_purchase_status`, `fn_apply_event_promos`, `refund_purchase`); their public signatures and response envelopes are unchanged. One edge function (`purchase-orchestrator`) was redeployed to drop its direct `.from('purchase_ledger').insert/update(...)` writes in favor of `supabase.rpc('chokepoint_post_purchase_event', …)`. The dev fixture `test_currency_flow(numeric, text)` was the only remaining direct writer outside the chokepoint and was dropped. Both existing `purchase_ledger` triggers were preserved unchanged: `trg_purchase_complete_cascade_items` (data-integrity cascade to child items, AFTER UPDATE STATEMENT) and `trg_referral_purchase` (post-write side effect that does not write `purchase_ledger`, AFTER INSERT/UPDATE ROW); the latter is flagged for Phase 2 relocation into the chokepoint. Edge-function audit confirmed `forward-receipt-with-points`, `claim-codes-award-points-create-receipt`, and `inngest-bulk-import-purchase-serve` never wrote `purchase_ledger` directly. Verification: a stricter regex (`pg_get_functiondef(...) ~* 'INSERT|UPDATE.*(public\.)?purchase_ledger\M'`) now returns exactly one function (the chokepoint), and an end-to-end DO-block smoke test inside a rolled-back transaction exercised every event plus every public wrapper (`api_create_purchase`, `api_update_purchase`, `refund_purchase`, `bulk_insert_purchases_with_items`) and confirmed row counts, completion stamps, cascade-trigger fire on `completed`, refund debit + parent flip, and bulk 3-rows-3-items in one call all behave identically to pre-migration. Kafka publish from inside the chokepoint and CDC decommissioning of `crm.public.purchase_ledger` are Phase 2, out of scope. See `requirements/Purchase_Transaction.md` §Architectural Overview and `requirements/architecture/event-chokepoints.md`.
- **Tier** — Phase 1 of the tier event chokepoint pattern landed. New `chokepoint_post_tier_change(p_user_id, p_merchant_id, p_from_tier_id, p_to_tier_id, p_change_type, p_change_reason, p_metadata, p_created_by, p_triggered_by_condition_id) → uuid` is now the single canonical writer for `tier_change_ledger`. Three former direct writers (`apply_tier_upgrade`, `ensure_tier_progress`, and the trigger function `after_user_signup_tier_setup`) were rewritten in a single atomic migration (`chokepoint_post_tier_change_phase1`) to `PERFORM` the chokepoint instead of inserting directly — names and non-ledger logic preserved verbatim, so behavior is unchanged. Pre-flight discovery corrected the brief's premise: `evaluate_user_tier_status` was a pure recommendation engine that never wrote the ledger; the real writer set was three functions, not one. No wrappers and no aliases — verification regex `pg_get_functiondef(...) ~* 'INSERT INTO (public\.)?tier_change_ledger'` now returns exactly one function (the chokepoint). Zero triggers exist on `tier_change_ledger`, so nothing required relocation; the two related-table triggers on `user_accounts` (`after_user_signup_tier` AFTER INSERT, `trg_auto_assign_on_persona_change` AFTER UPDATE) were preserved unchanged and now route through the chokepoint via the rewritten writer functions. No edge function or Render worker redeploy required — `crm-event-processors`' `TierConsumer` and `tier-daily-batch.ts` already call DB delegators (`process_tier_event`, `tier_apply_due_pending_upgrades`, `evaluate_and_apply_chunk`) which inherit chokepoint compliance automatically. Two historical orphan change_reason strings (`'Batch evaluation on 2026-05-02'` 37,514 rows in one second; `'Auto-assigned entry tier (safeguard)'` 77 rows over 15 dates in 2026-03 → 2026-04) were verified to have no current code source — dead historical paths only. Anon/auth raw INSERT/UPDATE/DELETE grants on `tier_change_ledger` remain (RLS blocks non-admin INSERT today) — flagged for Phase 2 hardening. Kafka publish from inside the chokepoint and CDC decommissioning of `crm.public.tier_change_ledger` are Phase 2, out of scope. See `requirements/Tier.md` §New in 5.1 and `requirements/architecture/event-chokepoints.md`.
- **Currency / Wallet** — Phase 1 of the wallet event chokepoint pattern landed. `post_wallet_transaction` (12-arg signature) was renamed to `chokepoint_post_wallet_transaction` and is now the single canonical writer for `wallet_ledger`. All 15 in-DB callers (`admin_remove_user`, `api_cancel_redemption`, `bff_admin_adjust_currency`, `bff_approve_activity_upload`, `bff_create_point_discount_burn`, `bff_reverse_point_discount_burn`, `bulk_import_currency`, `fn_execute_amp_action`, `fn_import_wallet_balance`, `process_checkin`, `process_currency_expiry`, `redeem_reward_with_points`, `reverse_points`, `reverse_tickets`, `test_wallet_system`) were rewritten in a single atomic migration; the old name was dropped (no wrapper, no alias). Two edge functions with direct `supabase.rpc("post_wallet_transaction", …)` were redeployed against the new name: `inngest-currency-serve` (currencyAward + currencyReversal) and `shopify-points-to-discount` (main burn + reversePointsBurn). All four wallet_ledger triggers (`set_dedup_key_on_wallet`, `trg_fifo_burn_tracking`, `trg_mission_eval_queue_wallet`, `trg_mission_eval_realtime_wallet`) were preserved unchanged to keep Phase 1 behavior identical except for the function name change. Pre-flight verified zero direct `INSERT INTO wallet_ledger` writers across every schema; the dead-code PGMQ wallet queue path (`enqueue_wallet_transaction` / `process_wallet_queue_batch` / nonexistent `q_wallet_queue`) was left untouched. Kafka publish from inside the chokepoint and CDC decommissioning are Phase 2, out of scope. See `requirements/Currency.md` §Architecture (v2.0) and `requirements/architecture/event-chokepoints.md`.
- **Agent Context Architecture** — executed Phase 1 trim + Phase 4b always-applied split (audit-driven). Trimmed `00-core.mdc` and inlined three discovery imperatives (registry-first, grep-not-full-read, batch-independent tool calls) so they're applied every turn instead of waiting for a requestable rule to be loaded — audit (last 50 threads) showed `16-tool-batching.mdc` loading in only 10% of threads and `12-doc-search.mdc` in 6%, while sequential single-tool turn runs hit 17 calls in the worst case. Split `15-crm-knowledge-mcp.mdc` into always-applied retrieval (~514 tokens) + requestable writes/audit (`15a-crm-knowledge-writes.mdc`, ~570 tokens) so the lean SQL templates only load when authoring/auditing knowledge. Slimmed `12-doc-search.mdc` and `16-tool-batching.mdc` to be examples/patterns references now that imperatives live in 00-core. Updated `08-context-lookup.mdc` to a 4-step procedure with batched Step 1, absorbed the Skip-Read List, and demoted slug files to fallback. Net always-applied savings: ~700–800 tokens per turn replay. See `.cursor/plans/agent-context-architecture.md` for full plan + audit data.
- **Agent Context Architecture** — Phase 6 partial: redirected `requirements/domains/_index.md` Domain Map to point at authoritative `requirements/<Domain>.md` paths instead of slug files. Agents grepping `_index.md` for keywords now route directly to the authoritative doc. Physical slug deletion deferred — `purchase-transaction.md` (and likely others) carry unique business-rule summaries not yet merged into authoritative; each needs a careful per-domain merge pass. See `requirements/domains/_index.md` and `.cursor/plans/agent-context-architecture.md` §Phase 6.
- **Agent Context Architecture** — pinned per-project MCP allowlist in `.cursor/mcp.json` (`supabase`, `crm-knowledge`, `github`). `cursor-ide-browser` (~6,193 tokens of MCP descriptors per turn) still requires manual disable in Cursor Settings → MCP workspace toggle — single largest token leak remaining.
- **Agent Context Architecture** — updated authoritative `requirements/AGENT_CONTEXT_ARCHITECTURE.md` to reflect the audit-driven shifts: the Cross-Cutting Behavioral Rules table now distinguishes always-applied imperatives (tool batching, doc grep-first, registry-first) from requestable behaviors, with an added "Architectural lesson" section explaining why behavioral rules must be Layer 0 (audit showed requestable behavioral rules load in only 6–10% of threads vs. noun-shaped requestable rules which load reliably). Reference Implementation section updated with current Layer 5 list (adds `15a-crm-knowledge-writes`), Routing in this repo updated to note discovery imperatives now live in 00-core, MCP profile section reflects `.cursor/mcp.json` pin + cursor-ide-browser Settings note, added an Always-applied per-turn budget table (~1,607 tokens) and a Known Follow-ups section. See `requirements/AGENT_CONTEXT_ARCHITECTURE.md` §Cross-Cutting Behavioral Rules and §Reference Implementation: Supabase CRM.
- **CRM Knowledge MCP** — surfaced "surveys" terminology by enriching aliases on the existing canonical features instead of creating a parallel `surveys` node. `forms` aliases now include `surveys`, `survey`, `custom surveys`, `user surveys`, `survey form/forms`, `survey completion reward`, `completion reward`, `profile survey` (kept existing `data capture`, `profile forms`, `member surveys`). `csat-and-feedback` aliases now include `surveys`, `survey`, `satisfaction survey`, `post-conversation survey`, `post-chat survey`, `feedback survey`, `nps survey`, `csat survey`, `customer survey/surveys` (kept existing `csat`, `customer feedback`, `satisfaction surveys`, `nps`, `customer satisfaction`, `post conversation survey`). Bare `surveys` now resolves to BOTH features (intentional disambiguation); domain-specific terms route precisely (`nps survey` → CSAT only, `survey completion reward` → Forms only). No new feature item, no new blocks.
- **CRM Knowledge MCP** — added `reward-sourcing-services` sub-feature (parent: `rewards`) with 4 consolidated blocks (`overview`, `rules`, `frontend_journey`, `technical_reference`) covering pay-per-use partner catalog economics, fulfillment SLAs, partner onboarding/reconciliation, illustrative category bands, the **Reward Strategy team** that designs each merchant's reward mix to integrate with user-base lifestyle and design utility, and the platform-vs-service boundary. Closes the gap where downstream agents searching for "reward sourcing" / "RaaS" / "partner fulfillment" got nothing; resolver now matches via `name_partial` and 13 curated aliases (`raas`, `reward as a service`, `partner fulfillment`, `partner network rewards`, `reward strategy team`, etc.). Source feature guide rewritten to drop client-specific framing and add the Reward Strategy team. See `requirements/feature-docs/reward-sourcing-services.md`.
- **Purchase Transaction** — added per-line **partial pickup quantity tracking** on `purchase_items_ledger`. New column `quantity_completed numeric NOT NULL DEFAULT 0` with CHECK `0 ≤ quantity_completed ≤ quantity`; backfilled 1.99M historical rows (`quantity_completed = quantity` where `status = 'completed'`, else `0`). Status is now **derived** from `quantity_completed` at every write site — `quantity_completed = quantity` ⇒ `'completed'`, anything less ⇒ `'pending'` (no separate "partial" enum value; items stay `pending` until fully picked up, header stays `pending` while any line is partial). `quantity_completed` is **monotonic non-decreasing** within a line's lifecycle. Updated writers to set the field at insert: `api_create_purchase` and `bulk_insert_purchases_with_items` (set to `quantity` when row is born `completed`, else `0`), `claim_marketplace_order` (always `quantity` since marketplace orders are born completed). Cascade trigger `trigger_cascade_purchase_complete_to_items` now also sets `quantity_completed = quantity` when force-completing children. **BREAKING**: `bff_seller_complete_purchase_items` signature changed from `(p_transaction_number text, p_line_item_ids uuid[])` to `(p_transaction_number text, p_line_items jsonb)` of `[{line_item_id, quantity_completed}]` (absolute new totals, not deltas; idempotent on retry). Validates monotonic non-decreasing (`MONOTONIC_VIOLATION`), within-range (`QUANTITY_EXCEEDS_ORDER`), and non-locked (`LINE_ITEM_LOCKED` for cancelled/refunded lines); returns per-item snapshot in `data.items[]` with `ordered_quantity`, `quantity_completed`, `status`. `bff_seller_get_purchase_by_transaction_number` now exposes `quantity_completed` and `quantity_remaining` per line. Earning (currency / tier / mission) is unchanged — still keyed off `quantity` (the ordered amount). FE seller pickup screen needs to ship the new request shape in lockstep. See `requirements/Purchase_Transaction.md` §Core Tables and Structure → purchase_items_ledger and §Seller Self-Service Functions, and `requirements/domains/purchase-transaction.md` §Key Business Rules.

## 2026-05-12

- **Event Promotion** — `bff_upsert_event_promo_config` converted from delete-and-rebuild to **diff-based upsert keyed on `event_promo_rule.id` and `event_promo_rule_outcome.id`**. Rules and outcomes with an `id` in the payload are UPDATED in place; entries without `id` are INSERTED; entries present in DB but missing from the payload are DELETED. Rule deletes are blocked only when `event_promo_claim` rows exist for that rule — returns `fn_response_error('Cannot Delete Rules', '<comma-joined rule names>', 'RULE_HAS_CLAIMS')`. Updates to claimed rules' non-identity fields (`rule_name`, `rule_order`, `condition_definition`, all `outcome_*` fields including the legacy mirror columns) now succeed; outcomes can be added/updated/removed on a claimed rule (no FK from claims to outcomes). Outcome rewrite uses a +1,000,000 `outcome_order` shift to avoid transient `(rule_id, outcome_order)` unique-constraint conflicts. New error codes: `RULE_HAS_CLAIMS`, `INVALID_RULE_ID`, `INVALID_OUTCOME_ID`. Fixes the prior FK-violation regression where any save on a claimed promo failed with a generic error. `bff_upsert_event_promo_with_rules` legacy wrapper unchanged (delegates `p_rules` through). See `requirements/Event_Promotion.md` §`bff_upsert_event_promo_config`.
- **Tier** — fixed eight bugs across tier admin functions in one migration. `bff_upsert_tier_with_conditions` now writes `upgrade_evaluation_timing` and `upgrade_evaluation_value` on upgrade-branch INSERT and UPDATE (previously silently dropped → every row in `tier_conditions` had NULL since the columns were added; `evaluate_user_tier_status` was operating on NULL inputs); UPDATE clauses for `frequency` / `window_start_field` / both new fields switched to "key present" pattern (`CASE WHEN v_condition ? 'key' THEN … ELSE existing END`) so partial-update payloads no longer null out unsent fields; `tier_conditions` UPDATE WHERE clause now includes `merchant_id` for defense-in-depth. `get_tier_conditions_by_type` now returns the two new fields in upgrade rows and **no longer filters out `active_status = false` rows** — previously inactive conditions were hidden from the FE and then hard-deleted by the upsert's `DELETE … NOT IN v_upgrade_ids` clause on the next save (silent data loss for any toggled-inactive condition). `get_merchant_tiers` now includes `active_status`, `upgrade_evaluation_timing`, `upgrade_evaluation_value` in the conditions array and gained an internal cross-merchant guard (`get_current_merchant_id() IS NULL OR = p_merchant_id`) to block PostgREST callers from listing other merchants' tiers. `reorder_tiers` gained the same caller-merchant ownership check at the top of the function. Maintain branch deliberately untouched for the upgrade-only fields. No FE call signatures or response envelopes changed; FE must be updated to (a) read the two new fields from the get response into form state and (b) round-trip inactive condition rows in the save payload so they are preserved. See `requirements/domains/tier.md` §Tier Conditions Round-Trip Contract.
- **Event Promotion / Purchase Transaction** — added user-selectable **tiered freebie pool** rule type. New `condition.type='tiered_freebie_pool'` (cap basis `invoice_total | fixed`, ascending spend brackets per SKU) paired with marker outcome `outcome_type='freebie_pool'`; CHECK constraints on `event_promo_rule.outcome_type` and `event_promo_rule_outcome.outcome_type` extended. `fn_calc_event_promos` now returns `freebie_pools[]` (always present) with resolved cap, sorted options, per-bracket `unlocked` flag, and `unit_price` from `event_product_config` then `product_sku_master.price`. `fn_evaluate_promo_condition` returns `true` for `tiered_freebie_pool`. `bff_upsert_event_promo_config` enforces pool/outcome pairing invariants (`INVALID_PROMO_RULE`). `api_create_manual_purchase_order` gained optional `p_chosen_freebies jsonb`; picks are revalidated server-side against canonical pools (cap, unlock, max_qty, rule existence) with structured failure `{code: 'POOL_VALIDATION_FAILED', details: { reason }}` (subcodes `POOL_CAP_EXCEEDED`, `POOL_SKU_NOT_UNLOCKED`, `POOL_RULE_NOT_FOUND`, `POOL_SKU_NOT_IN_POOL`, `POOL_QTY_EXCEEDS_MAX`, `POOL_INVALID_PICK`). `fn_apply_event_promos` accepts `freebie_pool_picks[]` in `p_promo_result`: one `purchase_items_ledger` row per pick (`item_type='freebie'`), one `event_promo_claim` per pool rule with `quantity_claimed = SUM(picks)`, picks merged into `purchase_ledger.reward_list.freebies[]` with `source='user_choice'` (engine-granted entries unchanged). Pool picks do not affect bill `discount_amount` / `final_amount`. `bff_preview_event_promos` is unchanged (passes through). See `requirements/domains/event-promotion.md` §Tiered Freebie Pool and `requirements/domains/purchase-transaction.md` §Supabase Functions.
- **Authentication** — `bff-auth-complete` LINE/tel finders' tiebreaker upgraded from `(created_at ASC, id ASC)` to a five-level ORDER BY: `is_signup_form_complete DESC NULLS LAST, persona_id DESC NULLS LAST, updated_at DESC NULLS LAST, created_at ASC, id ASC`. Fixes the regression where an end-user with mixed-state duplicates (e.g., one Syngenta LINE id had 9 active rows, oldest from 2025-07 was an incomplete test record, two newer rows were complete retailer profiles) was permanently bounced to the registration form because v86 picked the oldest incomplete row. New ordering still preserves "oldest wins" for the legacy-only case (all-incomplete duplicates) so JWT `sub` stays stable for the 153k Syngenta legacy cohort on first login. Deployed as `bff-auth-complete v87`. See `requirements/Authentication.md` §User Lookup & Duplicate Handling.
- **Authentication** — `bff-auth-complete` LINE/tel lookups now use `.maybeSingle()` with deterministic `(created_at ASC, id ASC)` ordering instead of `.single()`, so end-users with legacy duplicate `user_accounts` rows can log in and always receive the same JWT `sub` (oldest row wins). Added `normalizeLineId` (maps `''`, `'undefined'`, `'null'`, whitespace → `null`) so the function no longer writes empty-string `line_id` values. Partial unique index on `(merchant_id, line_id)` is **not** yet applied — that requires a separate backfill of ~13k duplicate rows. See `requirements/Authentication.md` §User Lookup & Duplicate Handling.
- **Authentication** — `bff_save_user_profile` now looks up the calling user by `id = auth.uid()` instead of `auth_user_id = auth.uid()`. The JWT `sub` is already `user_accounts.id`, and the previous join failed silently for the ~153k legacy/imported Syngenta rows where `auth_user_id IS NULL` — those calls were falling into the INSERT branch and creating sibling `user_accounts` rows with `is_signup_form_complete=true`, while the next login (correctly resolving by `line_id`) returned the original incomplete row and re-showed the registration form. Closes the regression for the entire legacy-import cohort with no data backfill required. The UPDATE branch now also self-heals `auth_user_id = COALESCE(auth_user_id, auth.uid())`, and the (rare) INSERT branch pins `id = auth.uid()`. See `requirements/Authentication.md` §Profile Save — Lookup by `id`.

## 2026-05-11

- **Admin Panel / Authentication** — Admin invitations now support email-only, phone-only, or email+phone identity; accept matches Supabase Auth email OR normalized phone. See `requirements/domains/admin-panel.md` §Supabase Functions (FE-callable).
- **Loyalty / Internal knowledge** — added sub-feature `reward-sourcing-services` (Reward Sourcing & Partner Fulfillment) and regenerated internal knowledge seed from curated `requirements/feature-docs/reward-sourcing-services.md` (partner pay-per-use model, admin journey, SLAs, platform mapping). See `requirements/feature-docs/reward-sourcing-services.md` and `requirements/domains/reward.md` §Curated knowledge.

## 2026-05-10

- **Customer Import / Export** — `bff_admin_start_user_import` now validates `match_on` against `field_selection` at batch-creation time (returns `MATCH_KEY_NOT_IN_TEMPLATE` with `data.missing_match_keys` instead of failing every row at stage time with `NO_MATCH_KEY`), and rejects empty `match_on` with `NO_MATCH_KEY_CONFIGURED`. Default `match_on` lowered to `ARRAY['tel']` so the FE can rely on a sane default that matches the most common consumer-CSV column. `bff_admin_stage_user_import_rows` `NO_MATCH_KEY` message now names the expected `user_accounts_<key>` column so merchants can self-correct without support. See `requirements/domains/customer-import.md` §Match keys (dedup) and §Error codes.
- **Customer Import / Export** — every import run now creates a fresh `form_submissions` row per (form, user) pair instead of upserting a single bulk submission, preserving full per-run history. Dropped `idx_form_submissions_form_user_bulk_uniq` and added a non-unique `(form_id, user_id, created_at DESC)` index. New submissions carry `submission_number = 'IMPORT-<batch_id>-<csv_row>'`, `source_id = batch_id`, `submitted_at = NOW()`. Added address-code support: importable keys `user_address_province_code`, `user_address_district_code`, `user_address_subdistrict_code` write through to `user_address.{province_code,district_code,subdistrict_code}`; when only the name is supplied the importer best-effort resolves the code from `address_th_*` masters (TH or EN), falling back to NULL on no match. Default fields (`user_accounts`, `user_address`, `user_wallet`, `tier_id`) keep upsert semantics. `bff_admin_get_importable_fields` and `bff_admin_get_user_import_template_csv` updated to surface the three address-code keys. See `requirements/domains/customer-import.md` §Address fields — name vs code and §Business rules.
- **Forms & User Profile** — `bff_get_form_details` now returns `data.form.allow_bulk_import` and `data.metadata.allow_bulk_import_locked` (true for USER_PROFILE); `bff_upsert_form` accepts and persists `form.allow_bulk_import` (USER_PROFILE forced to true regardless of payload). Lets the form-admin FE round-trip the bulk-import opt-in toggle introduced by Customer Import. See `requirements/domains/forms-user-profile.md` and `requirements/domains/customer-import.md`.
- **Customer Import / Export** — added bulk customer import + export feature with two modes (`ongoing`, `migration`). New tables `user_import_staging` (per-row JSONB with status + errors), `user_io_schemas` (saved field-selection presets); new column `form_templates.allow_bulk_import` (USER_PROFILE auto-true; other forms must opt in); extended `bulk_import_batches` with `import_mode`, `match_on`, `field_selection`, `chunk_size`, `last_processed_row_num`, `failed_count`, `skipped_count`, `file_url`. Rewrote `bulk_upsert_customers_from_import` for multi-form support (any form via `form_<code>_<field_key>` columns; legacy `user_profile_*` alias kept), configurable match keys (`tel | line_id | email | external_user_id | member_code | mongo_id`), array via pipe-delimited cells (`gym|yoga`) → `array_value`, JSON cells → `object_value`. Migration mode sets `skip_cdc=true` on all 4 CDC-published tables and a session GUC `app.skip_side_effects=true` that 5 triggers now honour (`after_user_signup_tier_setup`, `trigger_auto_assign_on_persona_change`, `queue_mission_evaluation_batch`, `trigger_mission_evaluation_realtime`, `fn_sync_user_type_from_persona`); fixed `assign_entry_tier_on_signup` so it no longer overwrites a caller-provided `tier_id`. New chunked async runner `process_user_import_chunk` (FOR UPDATE SKIP LOCKED, idempotent) called by extended `inngest-bulk-import-customers-serve` edge function. Optional post-migration backfills via `process_user_import_postprocess` (tier_progress, persona benefits, mission re-eval). New BFFs: `bff_admin_get_importable_fields`, `bff_admin_get_user_import_template_csv`, `bff_admin_save_import_schema`, `bff_admin_list_import_schemas`, `bff_admin_delete_import_schema`, `bff_admin_start_user_import`, `bff_admin_stage_user_import_rows`, `bff_admin_run_user_import`, `bff_admin_get_user_import_progress`, `bff_admin_list_user_imports`, `bff_admin_cancel_user_import`, `bff_admin_rollback_user_import`, `bff_admin_run_user_import_postprocess`, `bff_admin_start_user_export`. CSV uses two-row header (machine keys + human labels). See `requirements/domains/customer-import.md`.
- **Display Settings** — promoted points unit name + symbol from a per-widget config field to a **merchant-level** field on `merchant_display_settings` (new columns `points_unit_label`, `points_symbol_type`, `points_symbol_icon`, `points_symbol_image_url`). The merchant's chosen label ("Coins", "Stars", "แต้ม") is used by the widget AND LIFF mobile app AND admin history pages — single source of truth. Dropped `point` block from widget `config` JSONB and `widget_config_template.config_default`; `fn_validate_widget_config` now rejects payloads containing a `point` key with a `DEPRECATED` error. Widget read RPCs (`admin_get_widget_settings`, `bff_get_widget_settings`, `api_get_widget_settings_cached`) now surface points display alongside `primary_color` in the `resolved` block via new helper `fn_resolve_widget_merchant_display`. Added `admin_upsert_merchant_points_display(p_label, p_symbol_type, p_symbol_icon, p_symbol_image_url)` for targeted writes (validates 12-icon list + cross-field rule). Added AFTER trigger on `merchant_display_settings` to invalidate widget cache on any change — fixes a latent bug where `primary_color` updates weren't propagating to widget cache either. See `requirements/domains/display-settings.md` §Widget Display Settings.
- **Display Settings** — added widget display settings sub-domain for customer-facing widget surfaces (Shopify v1, multi-widget-ready via `widget_type`). New tables `merchant_widget_settings` (one row per `(merchant_id, widget_type)`, in-place edits mirroring brand-theme pattern) and `widget_config_template` (catalogue, mirrors `display_block_template`; carries `storage_bucket`/`storage_path_template` so FE never hardcodes the bucket — defaults to `images` to match main display settings). Primary color is sourced from `merchant_display_settings.primary_color` (single source of truth) — widget config only stores `theme.mode`, header/coins layout, brand mark. Added `admin_get_widget_settings`, `admin_upsert_widget_settings`, `admin_reset_widget_settings`, `bff_get_widget_settings`, `api_get_widget_settings_cached`, `get_widget_settings_menu`, `fn_resolve_widget_storage`, `fn_validate_widget_config`, `fn_widget_config_with_defaults`, `fn_invalidate_widget_settings_cache`, plus 2 triggers (validation + cache invalidation). **No per-language translation overlay** — matches `merchant_display_settings`. If localization is later required, register `merchant_widget_settings` as an `entity_type` in the existing `translation_api` / `/translation` admin page; do not build a parallel pipeline. See `requirements/domains/display-settings.md` §Widget Display Settings.
- **Purchase Transaction** — sellers (users with `user_type = 'seller'`) can now read and complete their assigned purchases via three new BFF RPCs (`bff_seller_get_purchase_by_transaction_number`, `bff_seller_complete_purchase_by_transaction_number`, `bff_seller_complete_purchase_items`). See `requirements/Purchase_Transaction.md` §Function Categories & Architecture → Seller Self-Service Functions (BFF).
- **Purchase Transaction** — added per-line-item status tracking on `purchase_items_ledger` and completion-actor stamping on both `purchase_ledger` and `purchase_items_ledger` (`completed_by_user_id`, `completed_by_source`, `completed_at`). Backfilled 1.99M historical line items to mirror their completed parents. See `requirements/Purchase_Transaction.md` §Core Tables and Structure (rows 1 and 2).
- **Purchase Transaction** — parent → children completion cascade is a single **statement-level** trigger (`trg_purchase_complete_cascade_items`) using transition tables; children → parent rollup is inline via `fn_recompute_purchase_status` so there is **no** row-level trigger on the high-volume `purchase_items_ledger` table. Bulk scripts can opt out with `SET LOCAL app.purchase_cascade_skip = 'on';`. See `requirements/Purchase_Transaction.md` §Trigger Architecture → Parent → Children Completion Cascade.
- **Purchase Transaction** — `purchase_ledger.seller_id` now has a `FOREIGN KEY ... REFERENCES user_accounts(id) ... NOT VALID` constraint; 26 historical orphans were grandfathered. New and updated rows are enforced. No CDC consumer redeploy required because the column is reused (not renamed). See `requirements/domains/purchase-transaction.md` §Key Business Rules.
- **Purchase Transaction** — six writers patched to maintain the `completed_*` invariant: `api_create_purchase`, both `api_update_purchase` overloads, `bulk_insert_purchases_with_items`, `claim_marketplace_order`, `refund_purchase`. Children inserted under a completed parent now get `status = 'completed'` from the start. See `requirements/Purchase_Transaction.md` §Core Tables and Structure (purchase_items_ledger Status Cascade Rules).
- **Project** — added project-wide changelog. See `requirements/CHANGELOG.md` and `.cursor/rules/06-update-docs.mdc`.

## 2026-05-19

- **AMP AI Agents** — Fixed `bff_list_agents` and legacy `bff_get_agent_full(uuid)` to count/load actions from `action_caller_config` instead of removed `amp_agent_action` table; seeded Yuanta-specific action configs (point ranges, LINE-only messaging, tag allowlists, earn-factor defaults) for all three demo agents. See `docs/AMP - MCP for Loyalty Actions.md`.
- **AMP Analytics (Yuanta Demo)** — Seeded 30-day mock `workflow_log` executions/actions/costs and `amp_agent_performance_summary` rows so AMP Analytics overview charts populate for Yuanta Demo merchant.
- **Notification Service** — added authoritative design doc for LINE Flex push v1: architecture (chokepoint → Kafka → NotificationConsumer → LINE), 4 tables, 3 RPCs, event catalog defaults, consumer topic mapping, Syngenta event-order completion caveat, Render env vars, and admin BFF contract. See `requirements/Notification_Service.md`.
## 2026-05-21

- **Notification Service** — Added catalog rows `purchase/created` and `purchase_item/completed` with default LINE flex templates; deployed `NotificationConsumer` subscribing to `crm.events.purchase_item` and mapping purchase created + line-item completion events. See `requirements/REGISTRY_SUPABASE.md` §Notification Service.
- **Currency** — Ticket earns/burns now split into one `wallet_ledger` row per unit with human-readable codes (`WT-` / `WTB-`); pooled ticket types auto-assign pool codes via `fn_assign_ticket_codes` per unit. `bff_get_currency_history` exposes wallet and pool codes. See `requirements/Currency.md` §Ticket Codes.

## 2026-05-27

- **Tag & Persona** — Added `bff_get_tags()` (full `tag_master` rows + `user_count`) and `bff_upsert_tag(p_data jsonb)` (create/update by optional `id`) so the `/user-tags` admin catalog page can list and mutate tags; delete remains `admin_delete_tag`. See `requirements/Tag_and_Persona.md`.

## 2026-05-28

- **Integrations** — Added four public `custom_webhook_*` edge functions (HookDeck replacement) with filter/transform/forward and audit table `custom_webhook_events`. See `requirements/Custom_Webhooks.md`.
- **Internal Knowledge / Writing Principles** — Seeded 11 writing playbooks (138 blocks) under `writing-principles`; added domain/root RPC filters (`p_knowledge_domain`, `p_root_slug`) plus helpers `internal_knowledge_resolve_item_domain` and `internal_knowledge_feature_ids_under_root`. See `docs/WRITING_PRINCIPLES_MCP.md`.
- **Writing Principles MCP** — Phase 2/3: crm-knowledge v1.2.0 adds `writing_*` tools with domain-scoped RPC filters; Supabase CRM adds `writing-principles` mcp.json entry and `.cursor/rules/19-writing-principles-mcp.mdc`. Issue a `writing:read` token with writing slug allowlist. See `docs/WRITING_PRINCIPLES_MCP.md`.

## 2026-05-29

- **Earn Channel** — Receipt upload approval now supports 3 entry modes via `feature_config.config->>'receipt_entry_mode'` (`line_item_amount` default, `line_item_derived` = qty × `product_sku_master.price`, `total_only`). `bff_approve_receipt_upload` gained `p_receipt_total` + mode resolution; added `bff_get_upload_receipt_config` / `bff_set_upload_receipt_config`. See `requirements/Earn_Channel.md` §Upload Receipt — Admin Approval & Entry Modes.

## 2026-06-08

- **Signup/Login** — Auto-assign 8-digit `member_code` on user create via `fn_derive_member_code` + `chokepoint_post_user_event`; bulk import Pass 1 dup validation for supplied codes. No backfill. See `requirements/Signup_Login.md` (§`bff-auth-complete`).

## 2026-06-11

- **Signup/Login** — Added admin CRUD BFFs for `acquisition_source_master`: `bff_list_acquisition_sources`, `bff_get_acquisition_source_details`, `bff_upsert_acquisition_source`, `bff_delete_acquisition_source`. See `requirements/Signup_Login.md` §Acquisition Source.

## 2026-06-12

- **CS Channels** — Lazada CS chat integration: `marketplace-lazada-exchange-token` made multi-app (`app` param, scope-aware upsert), new `webhook-lazada-cs` edge fn on shared `cs-ingest` engine, `marketplace-token-refresh` per-app secret routing + transient-failure retention, Lazada IM send adapter in `messaging-service`. See `requirements/CS_Channels.md` §Lazada Chat Integration.

## 2026-06-14

- **Campaign Activity Grouping** — Added `campaign`, `campaign_sub`, `campaign_activity_mapping` tables and BFFs for loyalty admin report grouping (`bff_list_campaigns`, `bff_list_campaign_activity_mappings`, `bff_get_campaign_details`, `bff_upsert_campaign_with_mappings`, `bff_delete_campaign`, `bff_search_campaign_activity_entities`). See `requirements/Campaign_Activity_Grouping.md`.

## 2026-06-14

- **Campaign Activity Grouping** — Added `campaign`, `campaign_sub`, `campaign_activity_mapping` tables and BFFs for loyalty admin report grouping. See `requirements/Campaign_Activity_Grouping.md`.

- **Campaign Activity Grouping** — Activities can map directly to campaign (`campaign_sub_id` nullable); upsert accepts `p_activities`; list/get return direct mappings. See `requirements/Campaign_Activity_Grouping.md`.

- **Campaign Activity Grouping** — Introduced named `campaign_activity` layer with `campaign_activity_entity` for many entity links per activity; dropped `campaign_activity_mapping`. See `requirements/Campaign_Activity_Grouping.md`.

- **Campaign Activity Grouping** — Renamed `campaign_activity_entity` to `campaign_mapping`; renamed BFFs to `bff_list_campaign_mappings`, `bff_upsert_campaign`, `bff_search_campaign_mapping_entities`; JSON field `mappings` replaces `entities`. See `requirements/Campaign_Activity_Grouping.md`.
- **Earn Channels** — registry override rows now seed deterministic `earn_channel.id` via `fn_earn_channel_effective_id`, preventing object-translation orphaning when a computed default becomes a physical override. See `requirements/Earn_Channel_Canonical.md` §Backend Gaps.
- **Earn Channels** — added `bff_reset_earn_channel_override(p_channel_id)` to delete registry override rows and restore computed platform defaults; admin list now exposes `can_reset`. See `requirements/Earn_Channel_Canonical.md` §Write Semantics.

## 2026-06-16

- **Earn Channels** — dropped FK from `purchase_ledger` / `purchase_receipt_upload` to `earn_channel`; removed `HAS_DEPENDENCIES` guard from delete/reset BFFs so legacy channels can be removed freely. See `requirements/Earn_Channel_Canonical.md` §Write Semantics.
- **Currency / Storefront Widget** — `get_user_summary` now returns optional `points_expiry_date` (earliest upcoming wallet-ledger expiry) and `points_expiry_amount` (points expiring within 30 days), populated only when `merchant_master.points_expiry_active` is true; reuses `bff_admin_get_member_360` expiry logic. Additive jsonb keys; no existing field changed.
- **Tier (Storefront)** — Fixed empty tier data for new Shopify members: `shopify-proxy` `findOrCreateUser` now creates brand-new members via `chokepoint_post_user_event('create')` instead of a bare `user_accounts` insert, so they get entry tier + full `tier_progress` + member_code + acquisition_source + CDC emit (was: `tier_id`/progress NULL until first earn). Edge fn `shopify-proxy` v54. See `requirements/REGISTRY_SUPABASE.md`.
- **Tier (Storefront)** — `chokepoint_post_user_event` create path now calls `ensure_tier_progress` (full progress: next tier, 0% baseline, deadline) instead of writing a stub `tier_progress` (current tier only); applies to all single-create channels (gated by `p_skip_side_effects`, so bulk import unchanged). See `requirements/REGISTRY_SUPABASE.md`.
- **Tier (Storefront)** — Fixed single-transaction tier-upgrade progress: `ensure_tier_progress` now derives next tier and progress from the post-evaluation (effective) tier, not the pre-upgrade tier (was: next_tier = the tier just reached, progress 100%). See `requirements/REGISTRY_SUPABASE.md`.
- **Tier (Storefront)** — `get_user_summary` read hardening (contract unchanged): emits the freshly-computed next tier instead of a stale stored `next_tier_id`, and only trusts stored `upgrade_progress_percent` when stored next tier matches the fresh next tier (prevents the impossible "1000/1000 @ 100%"). See `requirements/REGISTRY_SUPABASE.md`.
- **Tier (Storefront)** — `get_user_burn_rate` now resolves the member by `auth_user_id = auth.uid()` (matching `get_user_summary`) instead of `id = auth.uid()`, fixing disabled/`none` burn rate for channels where `id <> auth_user_id`. Contract unchanged. See `requirements/REGISTRY_SUPABASE.md`.

## 2026-06-22

- **Currency / Basic Currency Config** — `bff_upsert_basic_currency_config` now persists `earn_factor.allowed_purchase_statuses` on every rate factor (single-rate and per-tier) and every multiplier factor, filtered to `{pending, processing, completed}` with `ARRAY['completed']` fallback (was: column always defaulted to `['completed']` because the RPC never set it). `bff_get_basic_currency_config` now returns `allowed_purchase_statuses` at `earn_rate`, each `rates[]` item, and each `multipliers.items[]` entry from the stored factor; empty/default response includes `["completed"]`. Signatures unchanged. See `requirements/REGISTRY_SUPABASE.md` §`bff_upsert_basic_currency_config`.
- **Currency / Award Timing** — `bff_upsert_currency_config` now treats Award Timing as an atomic unit: when `currency_award_delay_type` is supplied, fields not meaningful under the new type (`days`/`minutes`/`time`) are zeroed via CASE, preventing stale carryover (e.g. switching to `immediate` no longer leaves `days=7`). `NULLIF(..., '')` guards prevent empty-string cast exceptions from silently rolling back the UPDATE. One-time backfill zeroed stale values on already-`immediate` merchants. Signature unchanged. See `docs/CRITICAL_BUG_FIXES.md` §2026-06-22.
- **Currency / Award Timing** — `inngest-currency-serve` v71 trusts `event.data.award_datetime` from `CurrencyConsumer` verbatim (was: recomputed via cached `get-merchant-config` edge function and silently defaulted to `now` on any config gap → immediate awards despite Delayed setting). Removed `getMerchantConfig` step, hardcoded `TIMEZONE_OFFSETS` map (no DST), and `calculateAwardDatetime` helper. Throws loudly if `award_datetime` missing. `currencyReversal` unchanged. See `docs/CRITICAL_BUG_FIXES.md` §2026-06-22.
- **User Profile** — Added `bff_user_update_birth_date(p_birth_date date)` for end-user (LINE app / web) self-service update of own birth date on `user_accounts`. Identity derived from `get_current_user_id()` (matched against `user_accounts.auth_user_id`) — no `p_user_id` accepted to prevent cross-user edits. `SECURITY DEFINER`, `NULL` birth_date clears the value, future dates allowed. NOTE: name uses `bff_` prefix per owner direction despite end-user (not admin) auth context. See `requirements/REGISTRY_SUPABASE.md` §`bff_user_update_birth_date`.
- **User Profile** — `get_user_summary` now returns `birth_date` (ISO date string or null) on the user-facing summary payload, sourced from `user_accounts.birth_date` written by `bff_user_update_birth_date`. Additive jsonb key; no existing field changed. See `requirements/REGISTRY_SUPABASE.md` §`get_user_summary`.

## 2026-06-29

- **Check-in** — Option B ledger-only streaks: fixed Frontline crash by removing `checkin_streaks` dependency from `bff_admin_get_member_360`; `reset_broken_streaks` is now a no-op; dropped `checkin_outcomes.active_status` and removed outcome active/inactive from process/status/BFF paths (participation limits unchanged). See `requirements/REGISTRY_SUPABASE.md` §Checkin.
- **Check-in** — Check-in BFF paths no longer expose/accept active_status on child outcomes or limits; process_checkin enforces all configured rows. Legacy checkin limits with active_status=false backfilled to true. See `requirements/REGISTRY_SUPABASE.md` §Checkin.
- **Check-in / Frontline** — `bff_admin_get_member_360` activity_stream check-in branch now derives points amount from `matched_outcomes` jsonb (drops reference to removed `checkin_ledger.outcome_amount`). See `requirements/REGISTRY_SUPABASE.md` §Checkin.
- **Admin / Frontline** — Registered `frontline_checkin` in `fn_admin_role_resource_catalog` and seeded `admin_role_permissions` for system `frontline` (read/create/update) and `owner` roles; admin-only permission guards on `process_checkin` and `get_user_checkin_status` (member self-check-in unchanged). Enables Check-in tab on Front-Line member page via `bff_get_admin_profile` permissions. See `requirements/REGISTRY_SUPABASE.md` §Authentication.

## 2026-06-30

- **AMP Workflows** — Fixed scheduled lifecycle dispatch (`schedule_type` → `type` normalization + backfill `next_run_at`), Thai phone normalization in `fn_amp_find_matching_users`, and `bff_amp_batch_run` now dispatches via `amp-dispatch-workflow-batch` (was list-only). `inngest-amp-serve` v37 reloads enriched user context at send time so `{{user.firstname}}` resolves. See `docs/AMP_LIFECYCLE_AUTOMATION.md`.

- **Spin Wheel** — DB backend for weighted-random spin wheel campaigns: schema (`spin_wheel`, `spin_wheel_segment`, `spin_wheel_segment_outcome`, `spin_wheel_log`), enums (`wallet_transaction_source_type.spin_wheel`, `entity_type.spin_wheel`), engine `fn_spin_wheel`, user read `get_user_spin_wheel_status`, admin BFF trio + delete. Grants delegate to `fn_dispatch_outcome`; spend via `chokepoint_post_wallet_transaction`. See `requirements/Spin_Wheel.md`.
- **Spin Wheel** — User auth hardening: added `bff_user_spin_wheel` and `bff_user_get_spin_wheel_status` (identity from `get_current_user_id()`); revoked `EXECUTE` on `fn_spin_wheel` and `get_user_spin_wheel_status` for `anon`/`authenticated`. See `requirements/Spin_Wheel.md` §User-Facing API.

## 2026-07-02

- **CS Channels** — New `webhook-doopage-cs` edge fn (skeleton): receives DooPage partner-forwarded Shopee chat webhooks, static bearer auth (`DOOPAGE_WEBHOOK_TOKEN`), logs to `custom_webhook_events`; `cs-ingest` mapping pending DooPage payload doc. See `requirements/CS_Channels.md` §DooPage Chat Integration.

## 2026-07-04

- **Check-in** — Fixed empty check-in history on Client and Frontline: `bff_get_campaign_history` (`p_filter = 'checkin'`) now reads `checkin_ledger.matched_outcomes` instead of dropped columns `outcome_amount`, `outcome_entity`, and `condition_id`. See `requirements/REGISTRY_SUPABASE.md` §Checkin.
- **Content Library** — Removed per-resource channel restriction: `fn_resolve_resource_for_delivery` no longer returns `CHANNEL_NOT_ALLOWED`; `cs_fn_search_resources` / `cs_fn_load_available_resources` drop `allowed_channels` filter+output; `bff_upsert_resource_content` (both overloads) ignores `p_allowed_channels` (writes NULL); existing values nulled. See `requirements/Resource_Content.md`.
- **Content Library** — Rich-content button actions: new `fn_resolve_rich_content_actions` + `fn_member_app_deep_link` rewrite `reward`/`survey` button actions to member-app `uri` deep links at delivery time (inside `fn_resolve_resource_for_delivery`, before flex rendering); `webhook-line` now ingests LINE `postback` events (convention `src=resource&resource_id=<uuid>&action=<text>`). See `requirements/Resource_Content.md` §Button Actions.
- **AMP Workflows** — Fixed audience member pagination, chunked `bff_amp_batch_run` dispatch, added `workflow_log` composite indexes, and soft-exit audience memberships during user removal. See `requirements/AMP_Workflows.md` §Audience Membership and §Batch Dispatch.
- **AMP Workflows** — Fixed broken column references in `bff_get_audience_members`: `user_accounts.full_name`/`phone_number` (never existed) → `fullname`/`tel` aliased to the same response keys; the function previously always returned a caught `ERROR` for any audience with members.
- **AMP Workflows** — Condition contract v2: `bff_get_workflow_collections` now returns a curated contract (input widgets, per-field operators, option lists, entity refs `tier`/`persona`/`store`/`package`/`product_sku`/`form_template`, curated aggregates, logical de-duplicated `store`/`sku` fields, Forms & Surveys collection); `fn_evaluate_amp_condition_group` adds `form_submission`/`form_answer` group types plus operators not_contains/starts_with/ends_with/is_empty/is_not_empty/is_true/is_false/in/not_in/is_null/is_not_null via new shared builder `fn_amp_build_condition_clause`; fixes missing alias on non-join aggregate filters. See `requirements/AMP_Condition_Contract.md`.
- **AMP Workflows** — Condition metadata v3: added 6 curated collections (`user_wallet`, `mission_log_completion`, `reward_redemptions_ledger`, `user_tags`, `tier_change_ledger`, `checkin_ledger`); new entity refs `mission`, `reward`, `tag`, `checkin`; `user_wallet` inserted as 2nd collection in picker order. Evaluator unchanged (standard user_id scoping). See `requirements/AMP_Condition_Contract.md` §Collection catalog.

## 2026-07-05

- **AMP Workflows** — Condition engine aligned to a single semantics: `fn_amp_find_matching_users` now evaluates ALL condition groups with `groups_operator` (and/or) via new `fn_amp_compile_condition_group`, supports form condition types, and shares `fn_amp_build_condition_clause` (tel normalization both paths, unknown operators raise); aggregate fn + collection whitelists (`fn_amp_assert_collection`) close the `%I`/`%s` injection surface; zero-row aggregates never match in either path; re-enrollment governed by `workflow_master.config->>'allow_re_enrollment'` (new helper `fn_amp_user_already_enrolled` for the edge layer). See `requirements/AMP_Workflows.md` §Condition Evaluation.
- **AMP Workflows** — Node stats FE/BE contract fix: `bff_get_amp_workflow_node_stats` adds `data[]` with `entered_count`/`completed_count`/`error_count` (keeps `nodes[]` spec keys); both node-stats and `bff_get_amp_node_users` now filter `workflow_log` by `merchant_id`. See `requirements/AMP_Workflows.md` §Node Stats Contract.
- **AMP Workflows** — Scheduled cron `fn_amp_run_due_scheduled_workflows` now dispatches in 500-user chunks via new shared `fn_amp_dispatch_batch_chunks` (also used by `bff_amp_batch_run`); previously one unchunked pg_net POST. See `requirements/AMP_Workflows.md` §Batch Dispatch.
- **AMP Workflows** — `bff_activate_audience(p_run_backfill => true)` now runs the backfill internally via `bff_amp_batch_run` and embeds the result under `backfill` (previously returned "trigger externally" without acting). See `requirements/AMP_Workflows.md` §Audience Membership.
- **AMP Workflows** — Collection allowlist v4: `fn_amp_assert_collection` extended from 7 to all 13 metadata collections + `amp_audience_member` pseudo-collection; audience membership semantics (`is_member_of`/`is_not_member_of`, active = `exited_at IS NULL`, merchant scope via `amp_audience_master` join) implemented identically in batch compiler (`fn_amp_compile_audience_membership`) and per-user evaluator (`fn_amp_eval_audience_membership`); collection-aware aggregate `time_field` default via `fn_amp_default_time_field` (`mission_log_completion`→`completed_at`, `checkin_ledger`→`checkin_timestamp`). See `requirements/AMP_Workflows.md` §Condition Evaluation.
- **AMP Workflows** — `inngest-amp-serve` v39: re-enrollment gate wired (per-user skip via `fn_amp_user_already_enrolled` inside memoized log-start step, logs `execution_skipped`); non-simple and audience-membership condition groups now delegate to `fn_evaluate_amp_condition_group` RPC. See `requirements/AMP_Workflows.md` §Condition Evaluation.
- **AMP Workflows** — New `bff_amp_preview_condition(p_groups, p_groups_operator)` pre-activation preview: compiled-only execution, count capped at 10,000, display-only `sql_text`. See `requirements/AMP_Workflows.md` §Condition Preview.
- **AMP Workflows** — Static vs dynamic audiences: `amp_audience_master.audience_type` (default dynamic); static activation backfills without activating realtime triggers; new `bff_refresh_audience` (full re-snapshot) on `fn_amp_resnapshot_audience`; `bff_create_audience` gains `p_audience_type`; hourly pg_cron `amp-reconcile-dynamic-audiences` reconciles active dynamic audiences (exit non-matchers, add missed matchers). See `requirements/AMP_Workflows.md` §Static vs Dynamic Audiences and §Dynamic Audience Reconciliation.
- **AMP Workflows** — New `bff_get_audience_analytics(p_audience_id)`: current size + entered/exited/net for last 7/30 days, Asia/Bangkok day-anchored windows from `amp_audience_member`. See `requirements/AMP_Workflows.md` §Audience Analytics.
- **Customer Export** — Implemented async CSV export worker: `process_user_export_chunk`, `fn_user_export_count`, `fn_emit_inngest_event`, updated `bff_admin_start_user_export`; edge fn `admin-user-export-csv` (Inngest `export/users-csv`); private Storage bucket `exports`. See `requirements/Customer_Export.md`.
- **Customer Export** — Source mode for AMP exports: `bff_admin_start_user_export` gains optional `p_source` (`audience` / `workflow` / `workflow_node` / `condition`); new `fn_user_export_source_sql` compiles it into the export query (snapshot-bounded via `metadata.snapshot_at`; condition source reuses `fn_amp_compile_condition_group`, evaluated live); wired into `fn_user_export_count` + `process_user_export_chunk`; old 3-arg BFF overload dropped. See `requirements/Customer_Export.md` §Source mode.
- **Customer Export** — Central tracking hub support: `bff_admin_list_user_imports` gains `p_type text DEFAULT 'users'` (`users` | `users_export` | `all`; invalid → `INVALID_TYPE`) and returns `import_type` per row, so the FE `/imports` hub can list export batches alongside imports (default unchanged — existing imports page unaffected); old 6-arg overload dropped. See `requirements/Customer_Export.md` §Central tracking.
- **AMP Workflows** — Message engagement tracking live: per-recipient tracked links (`amp_tracked_link`, `amp_engagement_event`; wrapped by `fn_amp_wrap_tracked_links` in `inngest-amp-serve` v40), new public edge fns `link-redirect` (click + 302) and `engagement-beacon` (page_view/page_time/scroll_depth from member app); engagement metrics added to `bff_get_amp_workflow_node_stats`, `bff_amp_analytics_workflow` (+`daily_clicks`), `bff_amp_analytics_user_timeline`; new `bff_get_resource_content_engagement` for shared-content rollups. LINE per-message read/open descoped (aggregate-only platform limitation). See `requirements/AMP_Workflows.md` §Message Engagement Tracking.
- **AMP Workflows** — Engagement analytics contract v2 (FE review): `amp_tracked_link.link_label` captured from flex button labels at wrap time; node-stats `engagement` is `null` for non-message nodes and adds `messages_sent`; `bff_amp_analytics_workflow` adds date-scoped `message_nodes[]` + `messages_sent`, drops never-populated `delivered`/`opened` from `action_funnel`; new `bff_get_amp_node_link_stats(p_workflow_id, p_node_id, p_from_date?, p_to_date?)` per-destination-URL breakdown; `bff_get_resource_content_engagement` adds `links[]` and enriches `by_workflow[]` (`workflow_is_active`, `last_sent_at`); user timeline emits enriched `click` events with embedded page metrics; `engaged_sessions` defined as distinct (token, session) with ≥1 beacon event. See `requirements/AMP - Rule Based.md` §Engagement Tracking.

## 2026-07-06

- **AMP Workflows** — New `content_engagement` condition group type for condition splits: branch on link clicked / time-on-page / scroll-depth of tracked content, scoped to a message node, this workflow (current run via `inngest_run_id`), or any workflow; split-only (excluded from audiences/entry/preview); evaluator + builder metadata migrations, `inngest-amp-serve` v41 context injection. See `requirements/AMP_Condition_Contract.md` §4.5.
- **AMP Workflows** — `inngest-amp-serve` v42: LINE/SMS/email action nodes with Content Library `resource_id` now resolve delivery via `fn_resolve_resource_for_delivery` (rich_content → `fn_render_blocks_as_line_flex` for LINE flex). Fixes `"No message content"` when the builder picker sets `resource_id` without inline text. See `docs/AMP_LIFECYCLE_AUTOMATION.md` §Outbound messaging.
- **User Profile / Forms** — Bulk profile-form saves no longer fire ~140 synchronous Upstash HTTP deletes inside row triggers; `bff_admin_upsert_user_profile_config` and `bff_upsert_form` set transaction-local `app.defer_profile_cache_invalidation`, triggers skip per-row invalidation, and a single end-of-RPC `fn_invalidate_user_profile_template_cache` runs (2s HTTP cap). Fixes BMW `statement_timeout` on `/user-profile-form-settings` Save.

## 2026-07-07

- **Purchase Transaction** — Receipt approve fix: `bff_approve_receipt_upload` seller-earning loop switched from `enqueue_wallet_transaction` (dead PGMQ `wallet_queue` — never created, no consumer) to synchronous `chokepoint_post_wallet_transaction` with dedup key. See `requirements/Purchase_Transaction.md` §Receipt approval seller earning.
- **Receipt Upload** — Multi-page receipt model: `purchase_receipt_upload.images text[]` added (backfilled from `image`); `upload_receipts` array overloads now insert ONE row per receipt with all page URLs (was one row per image + batch_id) and return a single `receipt_upload_id`; `bff_approve_receipt_upload` passes all pages to `purchase_ledger.images`; `v_receipt_upload_list` exposes `images`; `bff_get_upload_receipt_history` adds `metadata.images` + `image_count`. FuturePark OCR flow untouched. See `requirements/Receipt_Upload_Earning.md`.
- **Reward** — `get_my_reward_prices` now aligns with `calculate_redemption_points_fast` (tags + specificity matching, `require_points_match` handling); adds backward-compatible `match_type` and `redeemable` fields while preserving `reward_id`, `reward_name`, `my_price`, `fallback_price`.
- **Reward / Infra** — Confluent account deactivated (unrecoverable): user redemptions were DOWN (crm-api → Kafka `reward_redemptions` → RewardConsumer path dead). Deployed `inngest-reward-serve` edge function (Inngest `reward/redemption.requested`, concurrency 20, retries 4, `p_event_id` idempotency, `redemption_failed` broadcast parity, Shopify discount sync). Pending: `crm-api` switch from KafkaJS to `inngest.send` + Inngest app sync. Full impact map + de-Kafka plan: `.cursor/plans/confluent-deactivation-refactor-20260707.md`.

## 2026-07-08

- **Infra / Confluent deactivation** — De-Kafka cutover complete for all P0/P1 pipelines except AMP router: redemption (`crm-api` → `inngest-reward-serve`), chokepoint fan-out (`OutboxPublisher` → `inngest-event-router-serve` v2 with 14 routers — currency/tier/mission/outcome-attribution/notification; outbox 0 unpublished), marketplace webhooks (`MARKETPLACE_INGEST_MODE=inngest` → `inngest-marketplace-serve`). Open: AMP `AmpConsumer` port, Kafka env cleanup, Inngest SDK bumps on legacy serve fns, orphan TikTok shop `7494921743206681041` (no `merchant_credentials`). See `.cursor/plans/confluent-deactivation-refactor-20260707.md`.
- **Bulk Import Currency** — `bulk_import_currency` v2 (`p_validate_only`, `p_skip_emit`, `p_row_offset`, per-row dedup keys `bulk_{batch}_row_{n}`; old 4-param overload dropped) + `inngest-bulk-import-currency-serve` v47 (real validate-then-chunk flow; fixes early-return that skipped chunking and the completed-status update). 18 stuck `processing` batches reconciled to `completed` from `wallet_ledger`, 1 zero-ledger batch marked `failed`. New domain doc. See `requirements/Bulk_Import_Currency.md`.

## 2026-07-08

- **AMP / Infra** — AMP realtime dispatch ported off dead Kafka: `inngest-event-router-serve` v3 adds 5 AMP routers (`amp-{purchase,purchase-item,wallet,tier,user}-router`, 19 fns total) replacing `AmpConsumer`; legacy fire criteria + Redis dedup keys preserved as Inngest idempotency; `amp-user-router` burst-hardened for signup imports (batchEvents 100/5s + throttle 60/min + concurrency 5, all keyed per merchant); Inngest app `crm-event-router` re-synced. CDC-only trigger sources (form_submissions, amp_workflow_log add_to_audience) remain dead — flagged as open item. See `requirements/REGISTRY_RENDER.md` E: inngest-event-router-serve and `docs/AMP_LIFECYCLE_AUTOMATION.md` §Realtime Event Path.
- **Mission** — Pre-launch review fixes (Syngenta): `mission-purchase-router` forwards `amount`/`seller_id`/`store_*` again (missing `amount` in the Confluent→Inngest port zeroed all purchase-sum progress); new `mission-purchase-item-router` + enriched `chokepoint_post_purchase_event` item payloads (sku/product/category/brand/line amount) feed SKU-scoped conditions; `fn_evaluate_mission_conditions` scopes item-filtered conditions to `purchase_item` events (no header/item double count); `fn_update_mission_progress` loop-reset call fixed (crashed every loop completion); `bff_claim_mission` admin check uses `active_status`; dropped `trg_mission_eval_queue_wallet` (queue truncated) + `trg_mission_eval_realtime_wallet` (double-eval); `bff_upsert_mission` rejects dead condition types and no longer blocks on inline MV refresh (new minutely `refresh-mission-conditions-mv-fast` cron); `test` mission deactivated, backup table dropped; order `ORD20260707000727` backfilled. See `requirements/Mission.md` §Architecture, §Event Flow, §Operational Notes.

## 2026-07-09

- **Marketplace / Credentials** — Multi-shop order OAuth: exchange fns upsert by `(merchant_id, service_name, external_id, environment)` + scope (never overwrite another shop); unique indexes split order vs CS; TikTok multi-row-per-grant + sibling token refresh; `get_shop_credentials` / `get_merchant_marketplace_channels` exclude CS scope. See `requirements/CS_Channels.md` §Lazada Chat Integration and `.cursor/plans/marketplace-multi-shop-credentials-redesign-20260709.md`.
- **Authentication** — `auth-shopify-admin` v52: hand-minted Rocket JWT now includes merchant_id in top-level, user_metadata, and app_metadata (active_merchant_id) so get_current_merchant_id / FE match normal admin tokens; existing sessions until re-login. See `requirements/Authentication.md` §Shopify Admin.
- **Marketplace / TikTok** — Post-exchange shop picker: `marketplace-tiktok-exchange-token` v86 + table `marketplace_oauth_pending`; 2+ shops returns `needs_shop_selection`/`pending_id` (no credential rows yet); confirm with `pending_id`+`shop_ids`. Single-shop grant still auto-connects. See `requirements/REGISTRY_RENDER.md` E: marketplace-tiktok-exchange-token.

## 2026-07-12

- **Reward** — Shipping-reward delivery address: new member BFFs `bff_get_user_address` / `bff_save_user_address` (+ `fn_get_user_address_snapshot`), `reward_redemptions_ledger.delivery_address` jsonb snapshot column; `redeem_reward_with_points` stamps snapshot + `fulfillment_status='pending'` for `fulfillment_method='shipping'` and rejects member redemptions without a saved address (`ADDRESS_REQUIRED`); `user_address` RLS scoped to own-row/admin. See `requirements/Reward.md` §Shipping Fulfillment & Delivery Address.

## 2026-07-13

- **Receipt Upload** — Admin seller reference on approval: new `bff_find_seller_by_code`; `bff_approve_receipt_upload` adds optional `p_seller_code` (backward compatible) to resolve/attach `seller_id` on preview/approve. See `requirements/Receipt_Upload_Earning.md` §bff_approve_receipt_upload / §bff_find_seller_by_code.
- **Activity Log & Attribution / RFM / Funnel** — Ausiris RFP gap features: unified activity log (`activity_type_master`/`activity_field_def`/`activity_ledger` + `fn_log_user_activity`/`api_log_user_activity`), write-once acquisition columns on `user_accounts`, marketing campaigns + ROI attribution (`mkt_campaign`/`mkt_campaign_identifier`/`mkt_purchase_attribution`/`mkt_config`, cron `mkt-attribution-compute`, `v_mkt_campaign_roi`), RFM scoring (`rfm_config`/`rfm_user_score`, cron `rfm-daily-compute`), funnels as audience stages (`funnel_master`/`funnel_transition_ledger`, `fn_funnel_reconcile` in the hourly reconcile); new AMP condition collections `activity_ledger`/`rfm_user_score` + typed jsonb predicates; `bff_admin_get_member_360` adds rfm/acquisition/activity; loyalty-admin pages `/marketing-campaigns`, `/funnels`, `/rfm-settings`, `/activity-types` + Customer 360 + menu. See `requirements/Activity_Attribution.md`, `requirements/RFM_Scoring.md`, `requirements/Funnel.md`.

## 2026-07-14

- **RFM Scoring** — Admin segment detail + member export: `bff_report_rfm_segment` / `bff_report_rfm_segment_rows` (membership from latest `rfm_user_score`; period metrics from purchases; max export limit 5000). See `requirements/RFM_Scoring.md` §Functions / §Admin UI.

## 2026-07-15

- **Tier** — `get_user_summary` adds label-only `upgrade_metric_label` / `upgrade_ticket_type_id` / `maintain_metric_label` / `maintain_ticket_type_id` when metric is `ticket` (oldest active non-credit `ticket_type`); progress still sums all ticket earns. See `requirements/Tier.md` §Calculation Sources and §`get_user_summary` (ticket metric labels).
- **Receipt Upload** — Configurable member channel/store selection on upload-receipt earn channels (`config.receipt_selection.mode`: none|channel|store_only); `upload_receipts` accepts/persists `p_store_id`; approve falls back to uploaded store; list/view return derived `store_channel_*` for Admin pre-select. See `requirements/Receipt_Upload_Earning.md` §upload_receipts / §bff_approve_receipt_upload and `requirements/Earn_Channel.md` §Receipt Upload Config.
- **Receipt Upload** — New `bff_list_stores_for_receipt_upload`: paginated + searchable store picker (cursor on store_name/id, optional channel filter); usable by member and admin via `get_current_merchant_id()`. See `requirements/Receipt_Upload_Earning.md` §bff_list_stores_for_receipt_upload.
- **Marketplace / Credentials** — Durable OAuth connect audit: table `marketplace_connect_events` + instrumented `marketplace-shopee-exchange-token` / `marketplace-lazada-exchange-token` / `marketplace-tiktok-exchange-token` (started/success/error/needs_shop_selection; no tokens). Upsert key unchanged: `(merchant_id, service_name, external_id, environment)` where scope IS NULL. See `requirements/REGISTRY_RENDER.md` §Marketplace.

## 2026-07-16

- **Reward** — `bff_get_reward_history` `description` now prefers `promo_code` over generated `code` so Use Reward from history / LINE Notice shows the usable promo. See `requirements/Reward.md` §API Integration / Core Redemption Functions.

## 2026-07-19

- **Referral** — Consolidated to member_code invites, signup-only activation, `referral_outcomes` (inviter+invitee) via `fn_dispatch_outcome`, limits on `transaction_limits`; admin/user BFFs added; dropped invite-code table, first-purchase path, and orphaned currency publish. See `requirements/Referral.md`.
- **Referral** — Program on/off is the single active earn_channel (`method_type=referral`); dropped `merchant_master.referral_active`; general config no longer exposes referral toggles. See `requirements/Referral.md`.
- **Referral** — `fn_process_referral_signup` returns stable `code` values for FE i18n (`REFERRAL_INACTIVE`, `INVALID_INVITE_CODE`, `SELF_REFERRAL`, `ALREADY_REFERRED`, `LIMIT_EXCEEDED`, …). See `requirements/Referral.md`.
- **Referral / Customer 360** — `bff_admin_get_member_360` referral slice aligned to member_code model: invite from `user_accounts.member_code`, drop `referral_codes`/`first_purchase_at` reads, keep `first_purchase: 0` for FE payload shape. See `requirements/Referral.md` §Admin BFFs.
- **Referral** — Rewrote `requirements/Referral.md` as authoritative ownership split (Earn Channels = on/off; Referral settings = outcomes/limits/ledger), member apply contract, stable error codes; Earn_Channel.md notes referral channel is program gate.
- **Referral / Earn Channel** — `fn_get_effective_earn_channels` always surfaces `campaign:referral` (fixes ghost inactive row + duplicate-create); invite/apply still require channel `active=true`. See `requirements/Referral.md` §Program on / off.

## 2026-07-21
- **Analytics / BQ dashboards** — Full registry smoke: quoted reserved `AS \`rows\`` (all `.rows` + points movement); all 12 named reports pass against serving_loyalty. See `docs/BQ_PARTITION_AND_CLUSTERING.md`.
- **Analytics / BQ dashboards** — Fixed `analytics-query` BQ SQL: purchases `AS \`new\`` reserved word; redemptions `reward.category_id` ARRAY unnest; points movement partition-pruned; snapshot FE shape + bounded expiry scan; summary emits `burned`+`burnt`. See `docs/BQ_PARTITION_AND_CLUSTERING.md`.

- **Event Promotion** — Scalable freebie qty now `floor(set_count) * outcome_quantity` via `fn_calc_promo_set_count` for `product_qty_threshold` / `product_combo` / `spend_threshold` (compound non-scalable); `bff_list_event_products` effective_price falls back to 0. See `requirements/Event_Promotion.md` §Business Rules / §Internal Functions.
