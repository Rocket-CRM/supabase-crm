# CS Rules Engine (Feature Spec)

> Per-domain reference. Read ONLY when working on this domain. For the keyword → file map, see `_index.md`.

**Keywords:** rules, trigger, condition, action, auto_reply, auto_assign, auto_tag, auto_escalate, keyword_detection, intent_detection, sentiment_detection, rule_builder

**Source:** `CS_Rules_Engine.md`

**Covers:** Deterministic if-then automation. Trigger events, condition filters, action types, rule management UI. Uses shared `workflow_master` tables with `domain='cs'`. This is the feature spec — `CS_Feature_Spec.md` §3.1 covers how CS uses the shared workflow tables.

---
